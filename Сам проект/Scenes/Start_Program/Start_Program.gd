extends Node

#Версия оформления кода 0.2.0.0
#Стартовая сцена — точка входа в приложение. Собирает стартовый экран полностью
#через код (4 блока: чек-лист предзапусковых проверок, иконка компании, кнопки
#поддержки/настроек, окно информации) и ждёт кнопку перехода на Home
#mzs7
##2026-08-17 - mzs7 - убрана заглушка (Label "Стартовая сцена" + таймер 1.5с).
##	Вместо неё — 4 блока стартового экрана, собираемые через код (см. функции
##	ниже) через Class_UI.fC_SyncNodesWithConfig_Ch (old={} -> new=конфиг экрана,
##	то есть все ноды экрана создаются одним проходом UniversalBypass — см.
##	changelog fC_SyncNodesWithConfig_Ch в Class_UI.gd)
##2026-08-17 - mzs7 - переход на Home больше не по таймеру, а по кнопке
##	"Войти" (InfoBoxProceedButton) — она появляется только когда ВСЕ 5
##	проверок в vLD_CheckStatuses_D станут "passed" (см. _fC_UpdateOverallProgress_Ch)
##2026-08-17 - mzs7 - из 5 проверок чек-листа по-настоящему сейчас работает
##	только "Logger" (честно проверяет Class_Logger.FileLogger после
##	fxG_StartingLogger_CrTr, см. _ready()). Остальные 4 — заглушки со статусом
##	"waiting" ("Ожидает проверки"), реальная логика для них не входит в эту
##	задачу. Статус любой проверки можно выставить снаружи/вручную через
##	fC_SetCheckStatus_Ch(Ключ, "passed"/"failed"/"waiting", Сообщение)
##	⚠️ ДОПОЛНЕНО 2026-08-17 (см. запись ниже): "Interface" тоже стала настоящей
##	проверкой — см. _fC_VerifyInterfaceBuilt_Bv
##2026-08-17 - mzs7 - добавлена fC_SwitchOrientation_Ch — переключает
##	vLS_Orientation_S ("Vertical"/"Horizontal") и пересобирает экран через тот
##	же fC_SyncNodesWithConfig_Ch (old=текущий конфиг -> new=конфиг под новую
##	ориентацию). Механизм готов, реального переключателя в настройках пока нет —
##	это отдельная задача (Save/Settings.json сейчас пустой)
##2026-08-17 - mzs7 - _ready() переписан на жёсткую последовательность с
##	остановкой программы при провале: 1) запускаем и честно проверяем логгер —
##	ИНТЕРФЕЙС ВООБЩЕ НЕ СТРОИТСЯ, пока логгер не подтверждён (нечем было бы
##	показать его провал через собственный чек-лист — самого чек-листа ещё нет);
##	2) если логгер не запустился — fC_FatalStartupError_Ch: OS.alert (нативное
##	окно ОС, не зависит от нашего Class_UI) + get_tree().quit(); 3) если логгер
##	жив — строим экран (это и есть проверка "Interface"), затем честно проверяем
##	результат через _fC_VerifyInterfaceBuilt_Bv (сверяет число реально созданных
##	нод в vSD_AllNodes_D с числом нод-конфигов в собранном дереве через
##	Class_UI._fC_IsNodeConfig); провал — та же fC_FatalStartupError_Ch
##2026-08-17 - mzs7 - добавлен 4-й статус проверки "checking" ("…", жёлтый) —
##	промежуточный между "waiting" (ещё не начали) и "passed"/"failed" (уже
##	знаем результат). Сейчас его успевает показать только "Interface" (ставится
##	ДО _fC_BuildScreen_Ch, чтобы строка чек-листа сразу создалась с "…", а не
##	мелькнула "waiting"); у "Logger" результат уже известен ДО того, как строка
##	вообще существует (проверяется до сборки экрана), поэтому для него "checking"
##	увидеть негде — это ожидаемо, не баг. Готово для будущих Server/Update/
##	ThirdPartyFiles — сетевой код (следующий этап) будет ставить "checking" перед
##	стартом запроса и passed/failed по его завершении
##2026-08-17 - mzs7 - ⚠️ ПРОВЕРЕНО (не потребовало изменений): кнопки Support/
##	Settings не затемняют друг друга при открытии модалки — TopButtons создаются
##	В КОНЦЕ конфига (после Modal, см. _fC_BuildScreenConfig_Cr), поэтому обе
##	кнопки всегда рисуются ПОВЕРХ полупрозрачного ModalOverlay, а не под ним;
##	сам ModalOverlay физически не перекрывает их пиксели. Нажатие НЕ той кнопки,
##	что открыла окно, сейчас просто игнорируется (см. хвост _fC_ToggleModal_Ch) —
##	кнопка не серая (не задизейблена), но и не переключает модалку на себя
##2026-08-17 - mzs7 - конфиг экрана переведён на ВЛОЖЕННЫЙ формат с явным
##	__Children_D (см. Class_UI.gd, changelog fC_Mass_Creating_Node_CrTr): дети
##	ноды лежат не вперемешку с __Data_D/__Inspector_D/__Node_D, а в отдельном
##	ключе __Children_D рядом с ними — {__Data_D:{...}, __Inspector_D:{...},
##	__Node_D:{...}, __Children_D:{ИмяРебёнка: {... тот же формат ...}}}.
##	Промежуточные группы-обёртки "CheckList"/"CompanyIcon"/"InfoBox"/"Modal"/
##	"TopButtons" (без собственной ноды) убраны — теперь каждый словарь в дереве
##	это либо настоящая нода, либо корень всего конфига; так по построению не
##	может быть детей без родителей. Имя и путь ноды больше не пишутся руками —
##	их выставляет сам Class_UI по ключу словаря и его положению в дереве.
##	_fC_NodeCfg/builder-функции ниже переписаны под эту форму
##2026-08-17 - mzs7 - _fC_BuildScreen_Ch теперь строит экран через
##	Class_UI.fC_Mass_Creating_Node_CrTr (её и чинили только что в Class_UI.gd),
##	а не через fC_SyncNodesWithConfig_Ch(..., {}, конфиг) — для самой первой
##	сборки это ровно то, для чего мас-крейт и предназначена. fC_SyncNodesWithConfig_Ch
##	по-прежнему используется в fC_SwitchOrientation_Ch (там нужен именно diff
##	старого/нового конфига) — обе функции теперь ждут один и тот же вложенный формат
##2026-08-18 - mzs7 - каждый из 5 блоков верхнего уровня (CheckListPanel/
##	CompanyIconRect/InfoBoxPanel/ModalOverlay/TopButtonsHBox) в
##	_fC_BuildScreenConfig_Cr теперь помечен "v__Ebeveyn" = собственное имя блока —
##	восстановленная логическая метка "принадлежности" (см. changelog Class_UI.gd,
##	не путь в дереве сцены, отдельный механизм). Её подхватывают все вложенные
##	ноды блока через A_BelongTo_A — можно будет проводить массовые операции по
##	блоку целиком (Class_UI.fxC_MassDelitNode/fC_Node_changes_key), не трогая остальные.
##	_fC_CountNodeConfigs_I обновлена — обходит любую обёртку-Dictionary одинаково,
##	а не только буквально "__Children_D" (тот же принцип, что и в Class_UI.gd)
##2026-08-19 - mzs7 - _fC_BuildScreenConfig_Cr переписана на ОДИН вложенный
##	словарь-литерал (по образцу vXD_BottomPanel_D/vXD_TopPanel_D в home.gd) —
##	весь экран виден целиком в одном месте, без прыжков по функциям. Удалены
##	_fC_BuildCheckListPanelNode_Cr/_fC_BuildCheckListVBoxNode_Cr/
##	_fC_BuildCheckRowNode_Cr/_fC_BuildCompanyIconNode_Cr/_fC_BuildInfoBoxNode_Cr/
##	_fC_BuildModalNode_Cr/_fC_BuildTopButtonsNode_Cr — их тела перенесены прямо
##	внутрь литерала. Строки чек-листа остались отдельным циклом ДО литерала
##	(vLD_CheckRows_D) — их состав данные (vLA_CheckOrder_A), а не фиксированная
##	структура, дублировать вручную 5 одинаковых по форме строк было бы хуже для
##	понимания, а не лучше
##2026-08-19 - mzs7 - ⚠️ НАЙДЕНА И ПОЧИНЕНА ОШИБКА в _fC_ToggleModal_Ch:
##	предполагалось, что "вторая" кнопка (не открывавшая окно) сама по себе
##	некликабельна, т.к. ModalOverlay её перекрывает — неверно, TopButtonsHBox
##	создаётся ПОСЛЕ ModalOverlay и рисуется поверх него, обе кнопки всегда
##	получали клики. Добавлена _fC_SetOtherTopButtonBlocked_Ch — на время
##	открытого окна выставляет второй кнопке mouse_filter = IGNORE (клик проходит
##	насквозь), а не Button.disabled (та ещё и перекрашивает кнопку в серый —
##	по условиям задачи кнопка должна оставаться визуально обычной)
##2026-08-19 - mzs7 - ⚠️ ЭТА ЖЕ ОШИБКА ПОЧИНЕНА ПОЛНОСТЬЮ: предыдущая правка
##	(mouse_filter = IGNORE) решала только кликабельность — обе кнопки всё равно
##	лежали ПОСЛЕ ModalOverlay в _fC_BuildScreenConfig_Cr и потому НИКОГДА не
##	затемнялись оверлеем (в отличие от остального экрана, который затемняется
##	правильно). Правильное решение: TopButtonsHBox теперь лежит ДО ModalOverlay
##	(обе кнопки по умолчанию под оверлеем — и не кликабельны, и затемняются, как
##	весь остальной экран), а кнопка-источник поднимается НАД оверлеем точечно
##	через z_index (_fC_SetActiveTopButtonRaised_Ch, заменила
##	_fC_SetOtherTopButtonBlocked_Ch — mouse_filter больше не трогаем)
##2026-08-19 - mzs7 - добавлено содержимое окна настроек: слева в
##	ModalWindowPanel — боковой вертикальный список категорий (SettingsSidebarVBox:
##	"Экран"/"Звук"/"Язык", пока просто текстовые Label — иконок-текстур для
##	категорий нет, см. changelog 2026-08-14 про принцип заглушек), виден только
##	когда окно открыто ИЗ Settings (переключается в _fC_ToggleModal_Ch). Panel
##	перестроена на HBox (ModalContentHBox: сайдбар + ModalWindowLabel) вместо
##	свободных anchor/offset — Godot сам скрывает invisible-детей контейнера из
##	раскладки, поэтому у Support (сайдбар скрыт) ModalWindowLabel просто занимает
##	всю ширину, без ручного пересчёта offset'ов под два разных случая
##2026-08-19 - mzs7 - ⚠️ ПОЛНАЯ ПЕРЕРАБОТКА окна Поддержка/Настройки по новому
##	ТЗ из 8 пунктов. Что изменилось:
##	1) Окно теперь настоящее — заголовок (X-закрытие СЛЕВА-сверху,
##	   название СПРАВА-сверху, см. ModalHeaderHBox) + тело (ModalBodyHBox);
##	2) закрытие теперь ещё и по клику на затемнённую область — ModalOverlay
##	   получил сигнал gui_input (_fC_OnOverlayGuiInput_Ch); клик по самой
##	   Panel до оверлея не доходит (Control сам останавливает событие на
##	   первом, кто его обработал), поэтому это безопасно;
##	3) категории настроек (Язык/Экран/Звук) переехали НАПРАВО
##	   (SettingsCategoriesVBox, было — слева, SettingsSidebarVBox) и стали
##	   настоящими toggle-кнопками с общим ButtonGroup (_vGC_CategoryButtonGroup) —
##	   Godot сам гарантирует, что нажата только одна, и сам её визуально
##	   выделяет (готовым theme, без своей раскраски). Контент под каждую
##	   категорию — свой VBox внутри ModalContentArea, видимость переключает
##	   _fC_UpdateModalContentVisibility_Ch;
##	4) Язык — LanguageSearchLineEdit + LanguageListVBox: список языков
##	   строится ДИНАМИЧЕСКИ (Button.new() в коде, не через Class_UI/конфиг —
##	   это единственное осознанное отступление от конфиг-словаря во всём
##	   экране, т.к. список зависит от текста поиска и не может быть частью
##	   статического конфига), фильтруется по алфавиту введённого текста через
##	   Class_Localization.fC_FilterLocalesByInputScript_A (кириллица/латиница/
##	   CJK — см. её changelog). Выбор языка сохраняется в Settings.json и сразу
##	   переключает TranslationServer.set_locale — весь переводимый текст на
##	   экране (все "text": "КЛЮЧ" из Localization.csv) перерисовывается сам,
##	   без ручного обновления нод (auto_translate_mode, см. changelog
##	   Class_Localization.gd);
##	5) Экран — ScreenWidthLineEdit/ScreenHeightLineEdit (текст, Enter —
##	   text_submitted) + 3 кнопки готовых пресетов + ScreenFullscreenButton
##	   (toggle). И ручной ввод, и пресет, и fullscreen реально двигают окно
##	   ОС (get_window().size/.mode) И сохраняются в Settings.json;
##	6) Звук — два HSlider (min 0, max 100, step 1 — точность до 1%, см.
##	   п.7 ТЗ) на Музыку/Интерфейс + Label с текущим числом. Пока только
##	   сохраняют значение в Settings.json (реальных аудио-шин в проекте
##	   пока нет — подключение звука не входит в эту задачу);
##	7) ВЕСЬ текст этого окна (заголовки, категории, названия языков,
##	   плейсхолдеры, подписи) — теперь ключи из res://Save/Localization.csv
##	   (например "SETTINGS_TITLE", "CATEGORY_LANGUAGE"), а не русский текст
##	   напрямую — грузится через новый Class_Localization (см. отдельный
##	   changelog в Class_Localization.gd). ⚠️ Согласовано с пользователем:
##	   локализация сейчас касается ТОЛЬКО этого окна (Поддержка/Настройки),
##	   остальной экран (чек-лист, InfoBox, фатальные ошибки) НЕ переведён —
##	   осознанно, чтобы не трогать уже рабочий код без необходимости;
##	8) настройки теперь реально загружаются/сохраняются (_fC_LoadSettings_Cr/
##	   _fC_SaveSettings_Ch, через Class_Json, файл res://Save/Settings.json,
##	   ранее содержавший просто null). Если Locale ещё не сохранён — при
##	   первом запуске берётся язык системы (Class_Localization.fC_DetectSystemLocale_S).
##	   Сохранённые ScreenWidth/ScreenHeight/Fullscreen применяются к окну ОС
##	   уже в _ready(), до сборки экрана — при следующем запуске игра
##	   открывается с тем размером/режимом, что выбрал пользователь.
##	   Удалены: SettingsSidebarVBox/ModalContentHBox (старый сайдбар слева) —
##	   заменены на ModalContentArea+SettingsCategoriesVBox справа;
##	   _fC_SetOtherTopButtonBlocked_Ch уже был удалён раньше, не путать
##2026-08-19 - mzs7 - доработка окна Поддержка/Настройки по 5 пунктам обратной связи:
##	1) "поменяй местами право и лево" + непрозрачное окно 80% экрана: заголовок
##	   теперь название СЛЕВА / крестик закрытия СПРАВА (было наоборот), тело —
##	   категории (Язык/Экран/Звук) СЛЕВА / контент СПРАВА (было наоборот).
##	   ModalWindowPanel больше не фиксированные пиксельные offset — anchor
##	   0.1..0.9 по обеим осям (= 80% экрана, само пересчитывается под любое
##	   разрешение) + непрозрачный theme_override_styles/panel через новый
##	   StyleBoxFlat (_fC_ModalCardStyle_Cr, alpha=1 — раньше Panel красилась
##	   стандартной темой, местами полупрозрачной);
##	2) "экран не меняется в размерах" — в _fC_ApplyScreenSize_Ch между сменой
##	   Window.mode (выход из fullscreen/maximized) и следующим же изменением
##	   .size добавлен await get_tree().process_frame — известная особенность
##	   Godot, когда смена режима окна обрабатывается ОС асинхронно и .size,
##	   выставленный в том же кадре, тихо игнорируется. Плюс поля ширины/высоты
##	   теперь применяются и по потере фокуса (focus_exited), не только по
##	   Enter (text_submitted) — раньше клик мимо поля без Enter выглядел как
##	   "не работает". Плюс strip_edges() перед is_valid_int() (случайный
##	   пробел раньше делал поле "нерабочим"). ⚠️ Если после этого всё ещё не
##	   меняется — см. подробный комментарий у самой _fC_ApplyScreenSize_Ch про
##	   вероятную причину: "Embed Game" в редакторе Godot 4.3+;
##	3) push_error → лог + окно с ошибками, без падения программы. Затронуто:
##	   Class_Localization.fxG_LoadLocalization_Ch (была push_error 3 раза,
##	   теперь ВОЗВРАЩАЕТ String с описанием — "" при успехе; вызывающая
##	   сторона решает, что делать). Новая _fC_ReportError_Ch(компонент, текст)
##	   логирует через Class_Logger (если поток уже есть) И показывает текст в
##	   InfoBoxLabel — используется в _ready() для ошибки локализации (после
##	   того, как и лог, и экран точно готовы — до этого момента их физически
##	   нет). ⚠️ Не тронуто намеренно: единственный push_error в самом
##	   Start_Program.gd — в _fC_FatalStartupError_Ch (Логгер/Интерфейс) — это
##	   осознанный контролируемый останов (OS.alert+quit), не случайный сбой, и
##	   он уже и логирует, и показывает окно ОС;
##	4) print → лог, добавлено логирование на развилках. В Class_UI.gd —
##	   дочинены 2 "забытых" print() в fC_Creating_Node_CrTr (см. её же
##	   changelog отдельно, файл Class_UI.gd). В Start_Program.gd print() не
##	   было вообще — добавлено логирование через Class_Logger.fC_Adding_Buffer_Cr
##	   (гейтовано по vLS_StreamId != "", как и весь остальной проект) на
##	   основных развилках интерактивных функций: _ready (Logger/Interface
##	   passed), fC_SwitchOrientation_Ch, _fC_OpenModal_Ch/_fC_CloseModal_Ch,
##	   _fC_OnSettingsCategoryToggled_Ch, _fC_RebuildLanguageList_Ch,
##	   _fC_OnLanguageOptionPressed_Ch, _fC_On{ScreenSize,ScreenPreset,
##	   Fullscreen,SoundSlider}*_Ch. ⚠️ Статический словарь-литерал экрана
##	   (_fC_BuildScreenConfig_Cr) логированием НЕ обвешан — это данные, а не
##	   ветвления, и такой лог только замусорил бы файл лога при каждой сборке;
##	5) создание окна + "материал" (стиль фона) вынесены в отдельные
##	   переиспользуемые функции: _fC_ModalCardStyle_Cr() -> StyleBoxFlat и
##	   _fC_BuildModalChrome_Cr(vLD_BodyChildren_D) -> Dictionary (вся рамка:
##	   Panel+VBox+заголовок с крестиком/названием+обёртка тела). Раньше вся
##	   структура ModalWindowPanel лежала прямо внутри _fC_BuildScreenConfig_Cr
##	   одним куском — теперь при добавлении будущих модальных окон (не только
##	   Поддержка/Настройки) можно переиспользовать ту же рамку, передав только
##	   содержимое тела, а не копировать разметку заголовка заново
##2026-08-19 - mzs7 - доработка по ещё 2 пунктам обратной связи (шрифт + размытие фона):
##	1) "шрифт должен быть стандартизирован и зависеть от размеров окна, он был
##	   размыт". Добавлен const vGD_BaseFontSizes_D — 5 именованных уровней
##	   (Small/Body/Subtitle/Title/Large) вместо разбросанных магических чисел
##	   (14/16/18/20/22 без системы). Новая _fC_FontSize_I(Уровень) домножает
##	   базовый размер (посчитан для design-канвы 540x900) на масштаб РЕАЛЬНОГО
##	   текущего окна (min по ширине/высоте — не даёт тексту вылезти при "нестандартном"
##	   соотношении сторон) и округляет до целого — дробный итоговый размер
##	   шрифта был одной из реальных причин размытости. ВСЕ theme_override_font_sizes/
##	   font_size по всему экрану (чек-лист, окно информации, кнопки Поддержка/
##	   Настройки, всё окно настроек, включая ранее "голые" без явного размера —
##	   категории, поля ввода, ползунки звука) переведены на _fC_FontSize_I,
##	   включая динамически создаваемые кнопки списка языков
##	   (_fC_RebuildLanguageList_Ch — add_theme_font_size_override, они не через
##	   конфиг). Новые _fC_CollectFontSizeUpdates_Cr + _fC_RescaleFonts_Ch дают
##	   ЖИВОЙ перерасчёт: get_window().size_changed (подключено в _ready())
##	   вызывает _fC_RescaleFonts_Ch, которая строит свежий конфиг ТОЛЬКО чтобы
##	   получить актуальные числа размера шрифта под новый размер окна и
##	   точечно обновляет существующие ноды через уже готовый
##	   Class_UI.fxC_UpdateExistingNodes_Ch — полной пересборки экрана не требует.
##	   ⚠️ Если текст ВСЁ РАВНО останется размытым после этой правки — скорее
##	   всего дело не в коде экрана, а в project.godot: window/stretch/mode=
##	   "canvas_items" + window/stretch/scale=0.5 — это масштабирует УЖЕ
##	   отрисованную картинку целиком (растровое увеличение), а не сам код
##	   Start_Program.gd; трогать project.godot в эту задачу не входило
##	   (project-wide настройка, влияет на все сцены, не только на эту) —
##	   если нужно, это отдельное решение, которое стоит явно обсудить отдельно;
##	2) "часть что затемняет задний фон — добавить размытия". Новый шейдер
##	   res://Shaders/ModalOverlayBlur.gdshader (canvas_item, box-blur 5x5 по
##	   SCREEN_TEXTURE + смешивание с тем же полупрозрачным затемнением, что
##	   было раньше) + _fC_ModalOverlayBlurMaterial_Cr(), подставляется в
##	   "material" у ModalOverlay. Если файл шейдера почему-то не на диске/не
##	   грузится — material остаётся null, ColorRect тихо откатывается к
##	   плоскому затемнению без размытия (как было раньше), а не падает
##2026-08-19 - mzs7 - удалена _fC_SetActiveTopButtonRaised_Ch (обратная связь:
##	"не размывается кнопка настроек — исправить"). Раньше кнопка, открывшая
##	окно, точечно поднималась над ModalOverlay через z_index, чтобы по ней же
##	можно было кликнуть повторно и закрыть окно — из-за этого именно она не
##	затемнялась и не размывалась вместе с остальным экраном. Теперь, когда
##	есть ещё 2 способа закрыть (крестик, клик по затемнённому/размытому фону —
##	оба добавлены в этой же серии правок), поднимать кнопку больше не нужно:
##	убраны оба вызова (_fC_OpenModal_Ch/_fC_CloseModal_Ch) и сама функция.
##	Обе кнопки (Support и Settings) теперь ВСЕГДА одинаково затемняются и
##	размываются вместе с остальным экраном, пока окно открыто. Единственное
##	следствие: повторный клик по ТОЙ ЖЕ кнопке, что открыла окно, больше НЕ
##	закрывает его (она теперь тоже позади оверлея и не получает клики) —
##	закрытие только через крестик или клик по фону
##2026-08-19 - mzs7 - ⚠️ ИСПРАВЛЕНА ошибка размытия ("сетка из точек" на
##	шрифтах вместо плавного размытия) в Shaders/ModalOverlayBlur.gdshader —
##	подробности самой ошибки и решения см. в changelog самого файла шейдера
##	(коротко: сэмплы SCREEN_TEXTURE брались с шагом в blur_amount ЦЕЛЫХ
##	пикселей, между ними были дыры — классический алиасинг на высокочастотном
##	содержимом вроде букв; теперь сэмплы идут подряд, шаг всегда 1 пиксель, а
##	blur_amount — это радиус в пикселях). _fC_ModalOverlayBlurMaterial_Cr
##	обновлена под новый смысл blur_amount (3.0 вместо 5.0 — теперь это радиус,
##	не множитель шага)
##2026-08-19 - mzs7 - ⚠️ БОЛЬШАЯ ДОРАБОТКА: отложенное применение размера экрана
##	+ окна-уведомления + % вместо пикселей. По пунктам:
##	1) РАЗМЕР ЭКРАНА БОЛЬШЕ НЕ ПРИМЕНЯЕТСЯ СРАЗУ. Раньше
##	   _fC_OnScreenSizeChanged_Ch/_fC_OnScreenPresetPressed_Ch сразу звали
##	   _fC_ApplyScreenSize_Ch (реальный get_window().size). Теперь они зовут
##	   новую _fC_QueueScreenSizeChange_Ch — она только копит "хочу применить"
##	   (vLB_ScreenSizeDirty_Bv/vLI_PendingScreenWidth_I/Height_I), реальный
##	   get_window().size трогается ТОЛЬКО при закрытии настроек (см. п.3);
##	2) окно-ИНФО (ScreenInfoPopupOverlay) — показывается при КАЖДОМ изменении
##	   ширины/высоты/пресета (если не отключено галочкой), текст "применится
##	   после закрытия" + кнопка "Понял" (по центру снизу) + галочка "больше не
##	   показывать" (снизу справа, НАД кнопкой — порядок в VBox сверху вниз);
##	3) окно-ПОДТВЕРЖДЕНИЕ (ScreenApplyConfirmOverlay) — при попытке ЗАКРЫТЬ
##	   настройки (крестик/клик по фону — оба идут через _fC_CloseModal_Ch),
##	   если размер менялся (vLB_ScreenSizeDirty_Bv), закрытие теперь
##	   ПРЕРЫВАЕТСЯ и показывается это окно вместо немедленного закрытия (если
##	   само окно не отключено галочкой — тогда применяется молча и закрытие
##	   продолжается как раньше). Кнопки "Применить"/"Отмена" внутри отдельной
##	   "арены" (ScreenApplyConfirmArena, обычный Control, не контейнер — чтобы
##	   "Применить" можно было двигать вручную). ⚠️ "Применить" — с
##	   антидребезгом: клик быстрее чем за 0.5 сек после предыдущего клика по
##	   ней же не засчитывается, кнопка "убегает" на случайную позицию внутри
##	   арены (_fC_DodgeApplyButton_Ch) — защита от поспешного/случайного
##	   двойного клика. "Отмена" — откатывает поля ввода к реально сохранённому
##	   размеру и закрывает без применения;
##	4) новая 4-я категория настроек "Подтверждения" (CategoryConfirmationsButton/
##	   ConfirmationsContentVBox) — по одной строке на каждое из двух окон выше,
##	   с цветным переключателем (зелёная = показывается, красная = скрыто,
##	   переиспользует vGD_StatusColors_D["passed"/"failed"]) — управляет теми же
##	   Settings.json-ключами (ShowScreenChangeInfoPopup/ShowScreenApplyConfirmPopup),
##	   что и галочки "больше не показывать" на самих окнах — синхронизированы
##	   в обе стороны (_fC_UpdateConfirmToggleVisual_Ch);
##	5) оба новых окна используют общий переиспользуемый builder
##	   _fC_BuildConfirmOverlay_Cr(префикс, содержимое) — тот же принцип, что и
##	   _fC_BuildModalChrome_Cr у большого модального окна (оверлей+блюр+
##	   непрозрачная карточка), но без заголовка/крестика — закрываются только
##	   своими кнопками. Оба добавлены в vLD_Screen_D через merge() ПОСЛЕ
##	   основного литерала — специально, чтобы оказаться позже ModalOverlay по
##	   порядку ключей = рисоваться ПОВЕРХ него (тот же принцип z-порядка, что
##	   и у TopButtonsHBox/ModalOverlay);
##	6) новые ключи локализации добавлены в Save/Localization.csv (заглушки в
##	   том же формате "Слово_XX", что и весь остальной файл) —
##	   CATEGORY_CONFIRMATIONS, SCREEN_CHANGE_INFO, DONT_SHOW_AGAIN, OK_UNDERSTOOD,
##	   SCREEN_APPLY_CONFIRM_TEXT, CANCEL, APPLY, CONFIRM_SCREEN_INFO_LABEL,
##	   CONFIRM_SCREEN_APPLY_LABEL, CONFIRM_TOGGLE_ON, CONFIRM_TOGGLE_OFF;
##	7) ⚠️ КООРДИНАТЫ ВСЕГО ЭКРАНА ПЕРЕВЕДЕНЫ С "anchor+offset(px)" НА ЧИСТЫЙ %
##	   (обратная связь: "изменим логику интерфейса с координат на %, чтобы
##	   убедиться в работе параметров, изменяющих размер деталей") —
##	   CheckListPanel/CompanyIconRect/TopButtonsHBox/InfoBoxPanel (обе
##	   ориентации) теперь используют anchor_* в долях экрана и offset_*=0
##	   везде, без единого пикселя. Пересчёт — по старому anchor-центру ±
##	   offset_px / размер дизайн-холста (540 ширина / 900 высота,
##	   vSV2_Scene_size_V2i), подробности и формула — прямо в
##	   _fC_BuildScreenConfig_Cr рядом с новыми числами. ⚠️ НЕ переведены
##	   внутренние отступы/padding (offset_* внутри _fC_BuildModalChrome_Cr,
##	   theme_override_constants/separation везде) — это зазоры между соседними
##	   элементами внутри уже динамической панели, а не позиция на экране;
##	   перевод их в % от размера самой панели выглядел бы то исчезающе малым,
##	   то огромным на разных размерах окна — осознанное исключение, см.
##	   комментарий в самой _fC_BuildScreenConfig_Cr;
##	8) ⚠️ Про "экран не менялся, проблема была в размерах окна" — по словам
##	   пользователя причина была в режиме работы окна в настройках проекта
##	   (Project Settings редактора, не в этом файле) — код _fC_ApplyScreenSize_Ch
##	   не менялся, работа отложенного применения (п.1-3) строится поверх него
##	   как есть
##2026-08-19 - mzs7 - доработка по 5 пунктам обратной связи после проверки:
##	1) кнопка "Применить" в окне подтверждения изначально стояла НЕ на одном
##	   уровне с "Отмена" (anchor_top 0.0 вместо 0.5) — теперь тот же
##	   вертикальный якорь/офсет, что и у "Отмена" (обе по центру арены);
##	2) пресеты размера экрана переименованы с "разрешений" (1280x720 и т.п.)
##	   на короткие PRESET_1K/2K/3K + добавлен ВТОРОЙ ряд таких же пресетов
##	   для ВЕРТИКАЛЬНОГО экрана (720x1280/1080x1920/1440x2560) — у каждого
##	   ряда своя подпись (SCREEN_PRESETS_HORIZONTAL/VERTICAL), иначе
##	   одинаковые "1K/2K/3K" в двух рядах не понять, какой ряд к чему. Перед
##	   полями свободного ввода ширины/высоты теперь ПОСТОЯННЫЕ Label
##	   (ScreenWidthLabel/ScreenHeightLabel, ключи SCREEN_WIDTH/HEIGHT те же,
##	   что и у placeholder) — не пропадают при вводе, в отличие от placeholder;
##	3) в категории "Подтверждения" — 2 новых настраиваемых ползунка вместо
##	   раньше жёстко зашитых чисел: AutoClickProtectionSeconds (0.5 сек по
##	   умолчанию, "защита от автоматического пропуска" — сколько секунд после
##	   клика по "Применить" следующий клик считается слишком быстрым) и
##	   AutoClickDodgeLimit (1 по умолчанию, "количество телепортов" — сколько
##	   раз подряд кнопка успеет "убежать" за один показ окна, дальше клики
##	   обычные). Читаются в _fC_OnScreenApplyButtonPressed_Ch вместо старых
##	   жёстких 0.5/безлимита. Счётчик "побегов" _vLI_ApplyButtonDodgeCount_I
##	   сбрасывается при каждом новом показе окна (_fC_ShowScreenApplyConfirm_Ch);
##	4) ⚠️ вкладка "Подтверждения" ПЕРЕСТРОЕНА в двухуровневую: слева ВТОРОЙ
##	   столбик (ConfirmSourceVBox, тот же паттерн, что и SettingsCategoriesVBox
##	   слева от всего окна настроек) переключает "Настройки"/"Обучение"
##	   (ConfirmSourceSettingsButton/ConfirmSourceTrainingButton,
##	   _fC_OnConfirmSourceToggled_Ch/_fC_UpdateConfirmSourceVisibility_Ch).
##	   "Настройки" — то, что уже было (плюс новые ползунки из п.3), "Обучение" —
##	   осознанно ПУСТОЙ раздел (ConfirmSourceTrainingVBox без детей — раздела
##	   "Обучение" в проекте пока не существует). Внутри "Настроек" — 2
##	   визуальных раздела с заголовком покрупнее (_fC_FontSize_I("Subtitle"),
##	   left-aligned): "Автоматическое нажатие человеком" (ползунки п.3, САМЫЙ
##	   ВЕРХНИЙ — без линии-разделителя над собой) и "Окна поддержки" (старые 2
##	   переключателя — линия-разделитель ConfirmWindowsSeparator (HSeparator)
##	   НАД заголовком, т.к. это не первый раздел);
##	5) ⚠️ полноэкранный режим ТЕПЕРЬ ИДЁТ ЧЕРЕЗ ТОТ ЖЕ отложенный механизм
##	   "применить/отмена", что и размер (была жалоба "полноэкранный режим не
##	   работает через подтверждения" — раньше ScreenFullscreenButton была
##	   ЕДИНСТВЕННОЙ в категории "Экран", которая применяла get_window().mode
##	   СРАЗУ, в обход всего механизма). Новый vLB_PendingFullscreen_Bv рядом с
##	   vLI_PendingScreenWidth_I/Height_I — свой "хочу применить"-флаг, не
##	   зависящий от размера (выбор size сбрасывает pending fullscreen в false,
##	   выбор fullscreen НЕ трогает pending width/height — они пригодятся,
##	   когда пользователь потом выключит fullscreen). Базовые pending-значения
##	   выставляет _fC_OpenModal_Ch при каждом открытии Настроек — из реально
##	   сохранённых vXD_Settings_D. _fC_ApplyScreenSize_Ch получила 3-й параметр
##	   vLB_Fullscreen_Bv (по умолчанию false — старые вызовы без него не
##	   ломаются). Кнопка переехала — была отдельно внизу категории "Экран",
##	   теперь она ПЕРВАЯ среди пресетов (сразу перед PRESET_1K в
##	   ScreenPresetsHBox, п.5 ТЗ: "переместим к шаблонам в самом начале")
##2026-08-20 - mzs7 - доработка по обратной связи (частично, остальное —
##	отдельными заходами):
##	3) ⚠️ окно-информирование "настройки применятся после закрытия" (и окно
##	   "применить/отмена") визуально ЗАКРЫВАЛО собой окно настроек — потому что
##	   %sOverlay в _fC_BuildConfirmOverlay_Cr был ОДНОВРЕМЕННО и
##	   click-блокером НА ВЕСЬ ЭКРАН, и размытым фоном (нёс материал блюра).
##	   Раз он на весь экран — он полностью закрывал Settings-панель позади,
##	   выглядело как "настройки закрылись", а не "поверх появилось окошко".
##	   Исправлено: теперь 3 слоя вместо 2 — %sOverlay остался на весь экран,
##	   но стал ПОЛНОСТЬЮ ПРОЗРАЧНЫМ (Color(0,0,0,0), без материала) и служит
##	   только click-блокером (mouse_filter=STOP), новый %sBackdrop —
##	   отдельный ColorRect, размером ПОД панель (anchor 0.10..0.90 x
##	   0.28..0.72, чуть больше %sPanel), несёт материал блюра
##	   (mouse_filter=IGNORE, клики уже съедены родителем), и только ПОД ним
##	   лежит %sPanel — та же карточка, что и раньше. Итог: своё размытие есть
##	   только вокруг самого попапа, Settings-панель под ним видна не
##	   размытой/затемнённой, клики по ней всё ещё блокируются;
##	4) а) Label "Окна поддержки" (CONFIRM_SECTION_WINDOWS) переименован в
##	   "Окна подтверждения и информирования" — старое название ошибочно
##	   ссылалось на никак не связанную функцию Support (кнопка "Поддержка"),
##	   хотя раздел на самом деле про попапы информирования/подтверждения
##	   изменения экрана; б) 2 поля раздела "Автоматическое нажатие человеком"
##	   (защита от автоклика/лимит телепортов) были HSlider — по новой
##	   обратной связи ("лучше свободный ввод") переведены на LineEdit, тот же
##	   паттерн валидации text_submitted+focus_exited, что и у
##	   ScreenWidth/HeightLineEdit (см. _fC_OnAutoClickProtectionSubmitted_Ch/
##	   _fC_OnAutoClickDodgeLimitSubmitted_Ch — заменили старые
##	   _fC_On...Changed_Ch, привязанные к value_changed);
##	⚠️ пункт 2 (баг сворачивания окна колесом мыши) — на момент ЭТОЙ записи не
##	   найдено ни одного упоминания wheel/scroll/minimize в этом файле,
##	   похоже на поведение ОС/редактора, а не код. ⚠️ ОКАЗАЛОСЬ НЕВЕРНО — см.
##	   отдельную запись ниже, реальная причина найдена и починена. Пункт 6
##	   обратной связи оборвался на полуслове ("Для избежаннием") — нужен
##	   полный текст. Пункты 1 и 5 — см. следующую запись
##2026-08-20 - mzs7 - доработка по обратной связи, продолжение:
##	1) ⚠️ пресеты размера экрана ПОЛНОСТЬЮ ПЕРЕСОБРАНЫ по таблице соотношений
##	   сторон (VGA..8K UHD, 15 разрешений), которую прислал пользователь —
##	   старая ad-hoc схема PRESET_1K/2K/3K удалена целиком. Кнопки сгруппированы
##	   по aspect ratio (4:3/5:4/16:10/16:9/21:9/~17:9 — 6 групп), у каждой
##	   группы своя подпись-заголовок (просто текст соотношения, "4:3" и т.п.,
##	   БЕЗ ключа локализации — цифры/двоеточие не нуждаются в переводе) и свой
##	   ряд кнопок (ScreenPresetGroup{ratio}HBox), имя кнопки — название из
##	   таблицы (VGA/SVGA/XGA/.../8K, через новые CSV-ключи PRESET_VGA..
##	   PRESET_8KUHD, тоже без реального перевода — акронимы одинаковы на всех
##	   языках). Горизонтальный блок (ScreenPresetsHorizontalLabel) использует
##	   "Разрешение (Горизонтальное)" из таблицы, вертикальный
##	   (ScreenPresetsVerticalLabel) — те же 6 групп ещё раз, но с "Портрет"-
##	   значениями (W/H поменяны местами) и перевёрнутыми подписями групп
##	   (3:4/4:5/10:16/9:16/9:21/~9:17) — ИТОГО 30 кнопок-пресетов вместо
##	   старых 6. ScreenFullscreenButton выехал из общего ряда пресетов в
##	   СВОЙ отдельный ScreenFullscreenRow НАД всеми пресетами (было — первой
##	   кнопкой в общем ряду, п.1 ТЗ: "кнопку полноэкранный стоит поставить
##	   выше всех отдельно");
##	5) ⚠️ три связанные правки против "текст вылезает за пределы окна":
##	   а) новая настройка "Размер шрифта" (категория "Экран" →
##	   ScreenFontScaleGroupVBox/ScreenFontScaleLineEdit, свободный ввод,
##	   0.5..3.0, по умолчанию 1.0) — сохраняется в Settings.json как
##	   FontScale, читается в _fC_FontSize_I как ДОПОЛНИТЕЛЬНЫЙ множитель ПОВЕРХ
##	   уже существующего масштаба по размеру окна (тот, что домножает базовые
##	   14/16/18/20/22 из vGD_BaseFontSizes_D — см. changelog 2026-08-19 "шрифт
##	   должен зависеть от размеров окна"). Применяется СРАЗУ по Enter/потере
##	   фокуса поля (_fC_OnFontScaleSubmitted_Ch дёргает уже существующий
##	   _fC_RescaleFonts_Ch) — НЕ идёт через отложенный apply/cancel механизм,
##	   т.к. в список настроек, требующих подтверждения, размер шрифта не
##	   входит (см. changelog 2026-08-19 "список настроек... только Экран
##	   (ширина/высота/пресеты)"); б) всем Label с потенциально длинным текстом
##	   (подписи полей, заголовки разделов, тексты попапов информирования/
##	   подтверждения и т.п. — 19 штук, кроме коротких/фиксированной ширины
##	   вроде "4:3" или "100%") добавлен "autowrap_mode": 3
##	   (AUTOWRAP_WORD_SMART) — при переполнении строка переносится вместо
##	   того, чтобы вылезать за границы контейнера; в) ⚠️ ряды "подпись слева —
##	   поле/ползунок/переключатель справа" (HBoxContainer) ПЕРЕСТРОЕНЫ в
##	   "подпись сверху — управление снизу" (VBoxContainer), тот же паттерн,
##	   что уже был у ScreenWidthGroupVBox/ScreenHeightGroupVBox: категория
##	   "Подтверждения" — ConfirmAutoClickProtectionRow/DodgeLimitRow/
##	   ScreenInfoRow/ScreenApplyRow; категория "Звук" —
##	   SoundMusicHBox/SoundInterfaceHBox переименованы в *VBox, ползунок+число
##	   уехали в собственный вложенный *ControlHBox под подписью. Не трогались
##	   (осознанно): пары чекбокс+подпись (иконка/текст рядом — стандартная
##	   идиома, тянуть чекбокс под текст выглядело бы страннее, чем оставить),
##	   структурные ряды типа шапки модального окна/чек-листа запуска — это не
##	   "подпись+поле настройки", а другой тип верстки
##2026-08-20 - mzs7 - ⚠️ п.2 НАЙДЕН И ПОЧИНЕН: "окно закрывается/сворачивается
##	при вращении колесика мыши где угодно". Подсказка пользователя ("колесико
##	попадает в триггер на закрытие окна") оказалась точной — в
##	_fC_OnOverlayGuiInput_Ch (обработчик gui_input на ModalOverlay, закрывает
##	Настройки/Поддержку по клику мимо окна) проверялось ТОЛЬКО
##	InputEventMouseButton.pressed, без проверки button_index. В Godot прокрутка
##	колёсика — это ТОЖЕ InputEventMouseButton (button_index
##	MOUSE_BUTTON_WHEEL_UP/DOWN/LEFT/RIGHT), с pressed=true на каждый тик —
##	поэтому любой тик колёсика над прозрачным (на весь экран) ModalOverlay
##	читался как "клик мимо окна" и закрывал модалку. Раньше не нашёл эту
##	причину, т.к. искал буквальные "wheel"/"scroll"/"minimize" в коде — их
##	там действительно нет, баг был в СЕМАНТИКЕ InputEventMouseButton, а не в
##	отдельной обработке колёсика. Исправлено — теперь закрытие срабатывает
##	только на MOUSE_BUTTON_LEFT/RIGHT/MIDDLE, колёсико явно игнорируется
##2026-08-20 - mzs7 - ⚠️ БАГ НАЙДЕН И ПОЧИНЕН: "надпись Настройки стала
##	вертикальной, из-за чего сломался UI" — побочный эффект предыдущей записи
##	про autowrap_mode (см. changelog 2026-08-20 "п.5"). Причина и фикс — см.
##	комментарий прямо над ModalHeaderHBox в _fC_BuildModalChrome_Cr (коротко:
##	ModalWindowLabel получил autowrap_mode, но не size_flags_horizontal=3, а
##	соседний ModalHeaderSpacer забирал себе весь EXPAND-запас — Label
##	ужимался до минимума и с autowrap заворачивался по одной букве. Спейсер
##	убран, EXPAND перевешен на сам Label). ⚠️ Localization.csv: заглушки
##	вида "Настройки_RU"/"Настройки_EN" заменены на настоящие переводы en/de/
##	fr/es (обратная связь "переведи на выбор языка весь интерфейс"), из
##	русского столбца убрана приписка "_RU" — подробности см. changelog в
##	Class_Localization.gd. Ни один из старых суффиксов нигде в коде не
##	проверялся — чисто косметика, замена безопасна
##2026-08-20 - mzs7 - доработка по 2 мелочам:
##	1) ⚠️ размер шрифта переехал из категории "Экран" в СВОЮ, отдельную
##	   категорию "Шрифт" — обратная связь: "перенеси размер текста в низ, а не
##	   после размеров окна". Раньше ScreenFontScaleGroupVBox лежал ВНУТРИ
##	   ScreenContentVBox сразу после полей ширины/высоты; теперь это отдельный
##	   FontContentVBox, 5-я категория в списке слева (CategoryFontButton,
##	   специально ПОСЛЕДНЯЯ — "в низ"), переключается тем же
##	   vLS_ActiveSettingsCategory_S/_fC_UpdateModalContentVisibility_Ch, что и
##	   остальные. Сама категория "Экран" переименована в CSV на "Размер окна"
##	   (было "Экран") — раз в ней теперь строго только размер/полноэкранный
##	   режим/пресеты, а не вообще всё про экран;
##	2) ⚠️ и колонка категорий слева (SettingsCategoriesVBox), и основной блок
##	   контента справа (ModalContentArea) обёрнуты в ScrollContainer
##	   (SettingsCategoriesScroll/ModalContentScroll) — обратная связь: "сделай
##	   возможность прокрутки вниз через колёсико мыши категорий слева и
##	   основного экрана настроек". ScrollContainer в Godot обрабатывает
##	   прокрутку колёсиком САМ, без единой строчки кода — ничего похожего на
##	   _fC_OnOverlayGuiInput_Ch писать не пришлось. Единственная тонкость:
##	   обёрнутый ребёнок (сама VBox с категориями / сама ModalContentArea) НЕ
##	   должен иметь size_flags_vertical=3 — иначе Godot сжимает его под
##	   видимую высоту ScrollContainer'а, и скроллить нечего (контент
##	   обрежется вместо того, чтобы дать прокрутку) — эти флаги убраны у обоих.
##	   Видимость: у SettingsCategoriesVBox раньше скрывался/показывался САМ
##	   VBox под источник "Support"/"Settings" (_fC_OpenModal_Ch) — теперь
##	   скрывается ОБЁРТКА SettingsCategoriesScroll целиком, иначе пустой
##	   ScrollContainer с невидимым содержимым оставлял бы пустую полосу под
##	   окном Поддержки. У ModalContentArea обёртки такой видимости не было и
##	   не нужно — сам ModalContentArea всегда видим, видимость переключают
##	   только его *ContentVBox-дети
##2026-08-20 - mzs7 - доработка по 3 пунктам обратной связи:
##	1) ⚠️ CategoryFontButton перенесена со ПОСЛЕДНЕГО места в списке категорий
##	   на ВТОРОЕ, сразу после CategoryScreenButton (было: Язык/Экран/Звук/
##	   Подтверждения/Шрифт, стало: Язык/Размер окна/Шрифт/Звук/Подтверждения) —
##	   обратная связь "шрифт должен быть в экране следующим разделом после
##	   габаритов окна". ⚠️ Здесь возможна двоякая трактовка: понято как
##	   "порядок кнопок-категорий слева", а НЕ как "вернуть шрифт внутрь
##	   контента категории Экран" (для второго варианта нужен был бы отдельный
##	   откат прошлой правки про разделение Экран/Шрифт) — если это не то, что
##	   имелось в виду, поправьте;
##	2) ⚠️ все 12 групп пресетов разрешений по соотношению сторон (4:3/5:4/
##	   16:10/16:9/21:9/~17:9, горизонтальные + вертикальные) стали
##	   сворачиваемыми — заголовок группы (раньше просто Label с текстом "4:3"
##	   и т.п.) теперь кнопка-переключатель (toggle_mode=true,
##	   button_pressed=false — свёрнуто по умолчанию), а сам HBox с кнопками
##	   разрешений внутри группы скрыт (visible=false) и появляется только по
##	   нажатию на заголовок своей группы — новый обработчик
##	   _fC_OnPresetGroupToggled_Ch(нажато, ключ_HBox_группы), привязан через
##	   .bind() к каждому из 12 заголовков отдельно (это НЕ ButtonGroup — группы
##	   разворачиваются независимо, можно открыть сколько угодно одновременно).
##	   Обратная связь: "варианты разрешения для соотношений надо сделать
##	   появляющимися после нажатия на разрешения, а сами разрешения сделать
##	   кнопками" — сами разрешения кнопками были уже раньше (см. запись
##	   2026-08-19 про 15 разрешений/6 групп), в этот раз кнопками стали
##	   ЗАГОЛОВКИ групп, а ряд разрешений под ними — сворачиваемым;
##	3) ⚠️ БАГ НАЙДЕН И ПОЧИНЕН (предположительно, без визуальной проверки):
##	   "окна которые должны предупреждать об изменениях делают окно настроек
##	   прозрачным по высоте" — _fC_BuildConfirmOverlay_Cr собирает 3 слоя (см.
##	   запись 2026-08-19 выше): %sOverlay на ВЕСЬ экран (только click-блокер,
##	   был Color(0,0,0,0) — полностью прозрачный) и %sBackdrop с размытием,
##	   но %sBackdrop занимает только среднюю полосу по высоте (anchor_top=0.28
##	   .. anchor_bottom=0.72 = 44% высоты экрана). Раз %sOverlay ничего не
##	   красит, а %sBackdrop красит только среднюю полосу — верхние и нижние
##	   ~28% высоты экрана (там, где видна остальная часть окна настроек) не
##	   затемняются ВООБЩЕ, только средняя полоса вокруг самого попапа — отсюда,
##	   видимо, и ощущение "окно настроек прозрачное по высоте" (не то чтобы
##	   прозрачное, а частично незатемнённое). Исправление: %sOverlay теперь
##	   красит ВЕСЬ экран лёгким затемнением Color(0,0,0,0.25) (без размытия,
##	   заметно слабее, чем 0.55 у %sBackdrop/%sPanel-фона) — верх/низ теперь
##	   тоже затемняются, просто слабее, чем полоса вокруг самого попапа, "дыр"
##	   больше нет. Если это не то, что имелось в виду под "прозрачным по
##	   высоте" — нужен скриншот, чтобы разобраться точнее
##2026-08-20 - mzs7 - уточнение обратной связи по 3 пунктам из предыдущей записи:
##	   1) ⚠️ ОТКАЧЕНО: категория "Шрифт" (CategoryFontButton/FontContentVBox)
##	   удалена, ScreenFontScaleGroupVBox вернулся ВНУТРЬ категории "Экран",
##	   сразу после полей ширины/высоты (ровно туда, где лежал до самого
##	   первого разделения) — точная формулировка обратной связи: "именно
##	   вернуть в категорию экран". Категорий снова 4: Язык/Экран/Звук/
##	   Подтверждения (было 5 с "Шрифтом");
##	   2) ⚠️ УТОЧНЕНА ПРИЧИНА "прозрачности по высоте" — обратная связь: "пропадала
##	   часть окна настроек, которая была на одной высоте с окном
##	   предупреждения". Это НЕ про недостающее затемнение по краям (та правка
##	   из предыдущей записи остаётся — она просто убирала контраст между
##	   "дырой" и затемнённой полосой), а про САМУ полосу под %sBackdrop:
##	   %sBackdrop — это ColorRect с материалом РАЗМЫТИЯ ЭКРАНА (box-blur
##	   7x7=49 сэмплов, ModalOverlayBlur.gdshader) + затемнением tint alpha=0.55
##	   — тот же самый материал, что и у большого ModalOverlay, который
##	   размывает/затемняет ВЕСЬ стартовый экран позади настроек (там это
##	   уместно). Здесь же он ложится на маленькую полосу ПРЯМО ПОВЕРХ
##	   настоящих полей настроек (Panel карточки с текстом/кнопками) — размытие
##	   7x7 на мелком тексте UI фактически размазывает его до однородного
##	   серого пятна, а 55% чёрного поверх делает пятно ещё и тёмным — итог
##	   выглядит не как "слегка затемнённый фон", а как дыра/пустота на месте
##	   реального контента, отсюда и "пропадала часть окна". Исправление:
##	   новая отдельная _fC_ConfirmBackdropBlurMaterial_Cr() — специально для
##	   %sBackdrop (НЕ для большого ModalOverlay, у него материал прежний) —
##	   radius=1.0 (3x3=9 сэмплов вместо 49) и tint alpha=0.30 (вместо 0.55).
##	   Полоса всё ещё заметно выделена (эффект "фокуса" на попапе сохраняется),
##	   но текст под ней остаётся узнаваемым, а не превращается в пустое пятно;
##	   3) добавлено закрытие по Esc (_unhandled_input, новый обработчик) —
##	   обратная связь: "сворачивание окна на esc, если был выбор — в сторону
##	   нет". Приоритет сверху вниз: если открыто окно "Применить/Отмена"
##	   (ScreenApplyConfirmOverlay) — Esc жмёт "Отмена" (тот же
##	   _fC_OnScreenApplyCancelPressed_Ch, что и по кнопке — откат несохранённых
##	   полей + закрытие настроек, то есть буквально "выбор в сторону нет");
##	   иначе если открыто инфо-окно (ScreenInfoPopupOverlay, у него нет
##	   выбора Да/Нет, только "Понял") — Esc жмёт "Понял"
##	   (_fC_OnScreenInfoOkPressed_Ch); иначе если открыто само окно Настройки/
##	   Поддержка (vLB_ModalOpen_Bv) — Esc равнозначен крестику/клику мимо окна
##	   (_fC_CloseModal_Ch, там же по цепочке откроется окно "Применить/Отмена",
##	   если размер экрана менялся — это ожидаемо, тот же путь, что и у
##	   крестика). set_input_as_handled() после срабатывания, чтобы Esc не
##	   обработался ещё и где-то ещё в том же кадре
##2026-08-22 - mzs7 - доработка по 3 пунктам обратной связи (со скриншотом):
##	   1) ⚠️ БАГ НАЙДЕН И ПОЧИНЕН ТОЧНЕЕ: скриншот показал, что сквозь
##	   %sBackdrop (окно "Применить/Отмена", и то же самое — у инфо-окна)
##	   виден чек-лист ЗАПУСКА ("Запуск"/"Соединение" и т.п. с зелёными
##	   галочками) — элемент стартового экрана, который в принципе не
##	   должен быть виден сквозь непрозрачную карточку настроек. Значит
##	   дело не в силе размытия/затемнения (правка из прошлой записи это
##	   не лечила), а в том, ЧТО читает SCREEN_TEXTURE — похоже на
##	   известное ограничение рендерера Compatibility (project.godot:
##	   gl_compatibility) с экранными шейдерами в многослойных сценах.
##	   Не полагаясь на догадки без возможности визуально проверить —
##	   %sBackdrop ВООБЩЕ перестал читать экран: material убран,
##	   _fC_ConfirmBackdropBlurMaterial_Cr() удалена, остался только
##	   плоский цвет (alpha=0.45, без размытия) — гарантированно без
##	   просвечивания чего-либо постороннего, ценой самого эффекта
##	   "стеклянного" размытия у этих двух маленьких попапов (у большого
##	   ModalOverlay размытие не трогал — он такого бага не показывал);
##	   2) категория "Экран" разбита на 2 подписанных раздела ВНУТРИ себя
##	   (тот же паттерн заголовок+HSeparator, что и в "Подтверждениях" —
##	   см. ConfirmWindowsSeparator/CONFIRM_SECTION_*): "Размер экрана"
##	   (SCREEN_SECTION_SIZE, раздел 1 — без линии сверху, ширина/высота/
##	   fullscreen/все пресеты) и "Шрифт" (SCREEN_SECTION_FONT, раздел 2 —
##	   с линией сверху, ScreenFontScaleGroupVBox теперь физически в
##	   самом конце категории). Обратная связь: "раздели в категории
##	   экран шрифт и экран по месту в окне, как разделили окна
##	   подтверждения/информирования и автоматическое нажатие" — важно:
##	   это НЕ то же самое, что отдельная категория/кнопка слева (та
##	   версия был откачена в предыдущей записи) — здесь Font и остальной
##	   Экран остаются ОДНОЙ категорией, просто визуально размечены
##	   заголовками внутри одного и того же окна;
##	   3) ⚠️ БАГ НАЙДЕН И ПОЧИНЕН: "автоматическое нажатие не работает" —
##	   см. подробный changelog у _vLF_ApplyButtonUnlockMsec_F (была
##	   _vLF_ApplyButtonLastClickMsec_F) чуть выше: защита сравнивала
##	   время с ПРОШЛОГО клика, а не с показа окна, поэтому самый первый
##	   клик (в т.ч. мгновенный) всегда проходил — защита реально ни разу
##	   не срабатывала на типичном сценарии. Плюс добавлен видимый
##	   счётчик (ScreenApplyConfirmCountdownHBox, тикает через
##	   ScreenApplyConfirmCountdownTimer раз в 0.1с) — обратная связь:
##	   "добавь счётчик у кнопки подтвердить, который будет исчезать по
##	   истечении и позволять нажать на кнопку" — счётчик стоит НАД
##	   ареной (не двигается вместе с кнопкой при "побеге"), показывает
##	   остаток защиты в секундах и исчезает сам, когда время истекло
##2026-08-22 - mzs7 - новая категория "Логи и реплеи" (CategoryLoggerButton/
##	   LoggerContentVBox, последняя в списке слева) — обратная связь: "добавляем
##	   настройки Логера, а именно Логи Программы и Реплеи как два раздела в
##	   одной категории с настройками Максимальное количество/время/объём
##	   памяти логов/реплеев, с окном ввода справа и галочкой (квадрат
##	   красно/зелёный) слева, которая включает настройку". По пунктам:
##	   1) структура — тот же паттерн "заголовок раздела + HSeparator между
##	   разделами", что и у "Экран" (Размер экрана/Шрифт) и "Подтверждения"
##	   (Автоматическое нажатие/Окна подтверждения) — раздел 1 "Логи
##	   программы" (LOGGER_SECTION_LOGS, без линии сверху) и раздел 2
##	   "Реплеи" (LOGGER_SECTION_REPLAYS, с линией) внутри ОДНОЙ категории —
##	   именно так, как просили, а не как отдельные категории/кнопки слева;
##	   2) в каждом разделе — по 3 одинаковые по структуре строки (количество/
##	   время в днях/объём памяти в МБ), в каждой — подпись сверху (тот же
##	   принцип "элементы управления ниже текста", что и везде на этом
##	   экране), под ней HBox из галочки-квадрата СЛЕВА (LineEdit СПРАВА —
##	   в точности как просили). Галочка — маленькая (28x28) toggle-кнопка
##	   без текста, кроме символа ✓/✗ (переиспользует vGD_StatusSymbols_D),
##	   перекрашивается в зелёный/красный через modulate (переиспользует ту
##	   же vGD_StatusColors_D, что и чек-лист/переключатели "Подтверждений" —
##	   "квадрат красно/зелёный" из обратной связи);
##	   3) все 6 строк — ОДНА пара generic-обработчиков через .bind() (см.
##	   _fC_OnLoggerLimitToggled_Ch/_fC_OnLoggerLimitSubmitted_Ch), а не 12
##	   отдельных функций — тот же принцип, что и у _fC_OnConfirmationToggle_Ch/
##	   _fC_OnSoundSliderChanged_Ch. Список из 6 строк (Enabled-ключ/числовой
##	   ключ/ключ ноды-галочки/ключ ноды-поля) вынесен в общий
##	   const vGD_LoggerRows_A — используется в _fC_ApplySettingsToUI_Ch
##	   (синк настроек -> поля при открытии окна), обработчикам сам список не
##	   нужен, они уже параметризованы прямо в конфиге узлов;
##	   4) 12 новых ключей в Settings.json (6 *Enabled bool + 6 числовых —
##	   LogMaxCount(Enabled)/LogMaxAgeDays(Enabled)/LogMaxSizeMB(Enabled) и
##	   те же 3 для Replay*), дефолты: 100/30/50 для логов, 20/7/200 для
##	   реплеев, все Enabled=true. ⚠️ ВАЖНО: это ТОЛЬКО настройки и их UI —
##	   реальное применение лимитов (удаление старых логов/реплеев по
##	   количеству/возрасту/объёму) этот экран не делает и Class_Logger.gd не
##	   трогает — значения только сохраняются в Settings.json, ждут своей
##	   системы ротации логов/реплеев (отдельная задача, не запрашивалась)
##2026-08-22 - mzs7 - обратная связь: "добавь при изменении параметров
##	   информационное окно, что изменения будут применены только после
##	   перезапуска" — про категорию "Логи и реплеи" (её лимиты Class_Logger.gd
##	   перечитывает из Settings.json только один раз, при СЛЕДУЮЩЕМ старте
##	   программы, см. её changelog от того же дня — "сделай так, чтобы логи
##	   брали настройки из Settings"). Третье всплывающее окно поверх настроек,
##	   ТОТ ЖЕ паттерн, что и ScreenInfoPopup/ScreenApplyConfirm
##	   (_fC_BuildConfirmOverlay_Cr, новый префикс "LoggerInfoPopup") — Label
##	   (LOGGER_RESTART_INFO) + галочка "больше не показывать"
##	   (LoggerInfoPopupDontShowCheck, сохраняется как ShowLoggerRestartInfoPopup)
##	   + кнопка "Понял" (LoggerInfoPopupOkButton). Показывается
##	   (_fC_ShowLoggerInfoPopup_Ch) из ОБОИХ обработчиков категории — и
##	   галочки (_fC_OnLoggerLimitToggled_Ch), и поля ввода
##	   (_fC_OnLoggerLimitSubmitted_Ch, только при реально валидном/сохранённом
##	   значении, не при откате невалидного ввода) — на любое из 6 полей любого
##	   из 2 разделов (Логи/Реплеи). В категорию "Подтверждения" добавлена
##	   третья строка-переключатель (ConfirmLoggerInfoRow/ConfirmLoggerInfoToggle,
##	   в тот же раздел CONFIRM_SECTION_WINDOWS, что и Screen-строки) — тот же
##	   generic _fC_OnConfirmationToggle_Ch, ничего нового не потребовалось.
##	   Esc закрывает это окно так же, как ScreenInfoPopupOverlay (см.
##	   _unhandled_input). Заодно убран случайный повторный вызов
##	   _fC_SaveSettings_Ch (дважды подряд, без эффекта, но лишний) в
##	   _fC_OnLoggerLimitSubmitted_Ch

var Class_Logger = load("res://Godot_Template/Class_Logger.gd").new()
var Class_UI = load("res://Godot_Template/Class_UI.gd").new()
var Class_Json = load("res://Godot_Template/Class_Json.gd").new()
var Class_Localization = load("res://Godot_Template/Class_Localization.gd").new()

## Id потока, зарегистрированного для этой сцены — нужен, чтобы корректно
## завершить его перед переходом на следующий экран
var vLS_StreamId: String = ""

## Размер "логического" холста для UI-шаблонов (см. ShablonUI.md) — не размер
## реального окна, только для хранения в __Data_D наравне с остальными UI-конфигами проекта
var vSV2_Scene_size_V2i := Vector2(540, 900)

## Реестр всех созданных нод экрана: {уникальное_имя: {Node, ...}} — заполняется/
## обновляется через Class_UI.fC_SyncNodesWithConfig_Ch, напрямую не трогаем
var vSD_AllNodes_D := {}

## Конфиг экрана, который сейчас реально применён — нужен как "старый" конфиг
## для fC_SyncNodesWithConfig_Ch при следующей пересборке (см. fC_SwitchOrientation_Ch)
var vXD_CurrentScreenConfig_D := {}

## Текущая ориентация экрана. "Vertical" по умолчанию — совпадает с реальным
## окном проекта (project.godot: 540x900). "Horizontal" — второй вариант раскладки
var vLS_Orientation_S: String = "Vertical"

## Порядок проверок чек-листа и их подписи
var vLA_CheckOrder_A := ["Logger", "Interface", "Server", "Update", "ThirdPartyFiles"]
var vLD_CheckLabels_D := {
	"Logger": "Запуск логгера",
	"Interface": "Запуск интерфейса",
	"Server": "Соединение с сервером",
	"Update": "Актуальное обновление",
	"ThirdPartyFiles": "Проверка на сторонние файлы",
}

## Текущий статус каждой проверки — "waiting" / "passed" / "failed"
var vLD_CheckStatuses_D := {
	"Logger": "waiting",
	"Interface": "waiting",
	"Server": "waiting",
	"Update": "waiting",
	"ThirdPartyFiles": "waiting",
}

## Символ и цвет на каждый статус — черточка/многоточие/галочка/крестик.
## "checking" — новый 4-й статус: проверка уже идёт, результат ещё не известен
## (в отличие от "waiting" — ещё даже не начали)
const vGD_StatusSymbols_D := {
	"waiting": "–",
	"checking": "…",
	"passed": "✓",
	"failed": "✗",
}
const vGD_StatusColors_D := {
	"waiting": Color(0.7, 0.7, 0.7, 1),
	"checking": Color(0.95, 0.75, 0.2, 1),
	"passed": Color(0.3, 0.85, 0.3, 1),
	"failed": Color(0.9, 0.25, 0.25, 1),
}

## 2026-08-22: категория "Логи и реплеи" — список из 6 строк (одна на каждый
## лимит: количество/время/объём памяти × логи/реплеи), каждая — [Enabled-ключ
## в vXD_Settings_D, числовой ключ в vXD_Settings_D, ключ ноды-галочки, ключ
## ноды-поля ввода]. Общий источник правды для _fC_ApplySettingsToUI_Ch (синк
## настроек -> UI при открытии окна) — сами обработчики toggled/text_submitted
## этот список не используют, они уже параметризованы через .bind() в конфиге
## узлов (см. _fC_OnLoggerLimitToggled_Ch/_fC_OnLoggerLimitSubmitted_Ch)
const vGD_LoggerRows_A := [
	["LogMaxCountEnabled", "LogMaxCount", "LoggerLogMaxCountCheck", "LoggerLogMaxCountLineEdit"],
	["LogMaxAgeEnabled", "LogMaxAgeDays", "LoggerLogMaxAgeCheck", "LoggerLogMaxAgeLineEdit"],
	["LogMaxSizeEnabled", "LogMaxSizeMB", "LoggerLogMaxSizeCheck", "LoggerLogMaxSizeLineEdit"],
	["ReplayMaxCountEnabled", "ReplayMaxCount", "LoggerReplayMaxCountCheck", "LoggerReplayMaxCountLineEdit"],
	["ReplayMaxAgeEnabled", "ReplayMaxAgeDays", "LoggerReplayMaxAgeCheck", "LoggerReplayMaxAgeLineEdit"],
	["ReplayMaxSizeEnabled", "ReplayMaxSizeMB", "LoggerReplayMaxSizeCheck", "LoggerReplayMaxSizeLineEdit"],
]

## Стандартизированные размеры шрифта (2026-08-19, обратная связь: "шрифт
## должен быть стандартизирован и зависеть от размеров окна") — 5 именованных
## уровней вместо разбросанных по всему файлу магических чисел (было и 14, и
## 16, и 18, и 20, и 22 без единой системы). Значения ниже — БАЗОВЫЕ, для
## design-канвы vSV2_Scene_size_V2i (540x900); реальный размер, который
## реально ставится в theme_override_font_sizes/font_size, всегда идёт через
## _fC_FontSize_I(Уровень) — она домножает базу на масштаб текущего окна
const vGD_BaseFontSizes_D := {
	"Small": 14,    # пресеты размера экрана
	"Body": 16,     # обычный текст (крестик закрытия, поля ввода, кнопки категорий)
	"Subtitle": 18, # окно информации, иконки-кнопки Поддержка/Настройки
	"Title": 20,    # строки чек-листа
	"Large": 22,    # заголовок модального окна, кнопка "Войти"
}

## Путь к шейдеру размытия фона под модальным окном (см. _fC_ModalOverlayBlurMaterial_Cr)
const FileModalBlurShader := "res://Shaders/ModalOverlayBlur.gdshader"

## Открыто ли сейчас модальное окно (Поддержка/Настройки) и кем именно
var vLB_ModalOpen_Bv := false
var vLS_ModalSource_S := ""

## Какая категория настроек сейчас активна ("Language"/"Screen"/"Sound"/
## "Confirmations"/"Logger" — 2026-08-22: добавлена "Logger", см. её changelog
## у CategoryLoggerButton; "Font" была ненадолго отдельной категорией, затем
## откачена обратно внутрь "Screen" по уточнённой обратной связи, см.
## changelog у ScreenFontScaleGroupVBox) — управляет видимостью
## соответствующего *ContentVBox внутри ModalContentArea (см.
## _fC_UpdateModalContentVisibility_Ch).
## "Language" по умолчанию — под неё же изначально нажата CategoryLanguageButton
## (button_pressed=true в конфиге)
var vLS_ActiveSettingsCategory_S := "Language"

## 2026-08-19: какой ВТОРОЙ столбик сейчас активен ВНУТРИ категории
## "Подтверждения" — "Settings" (то, что уже было) / "Training" (заглушка,
## пока пусто) — см. ConfirmSourceVBox в _fC_BuildScreenConfig_Cr и
## _fC_UpdateConfirmSourceVisibility_Ch. "Settings" по умолчанию — под неё
## изначально нажата ConfirmSourceSettingsButton (button_pressed=true в конфиге)
var vLS_ActiveConfirmSource_S := "Settings"

## Путь к файлу настроек и сам загруженный словарь настроек (см.
## _fC_LoadSettings_Cr/_fC_SaveSettings_Ch) — {Locale, ScreenWidth, ScreenHeight,
## Fullscreen, MusicVolume, InterfaceVolume}
var FileSettings := "res://Save/Settings.json"
var vXD_Settings_D := {}

## 2026-08-23: путь к файлу списка аккаунтов (см. vLD_AccountsList_D выше и
## _fC_LoadAccountsList_Cr/_fC_SaveAccountsList_Ch) — тот же принцип, что и
## FileSettings, отдельная константа, чтобы путь не дублировался
var FileAccountsList := "res://Save/AccountsList.json"

## Общие ButtonGroup для взаимоисключающих кнопок: категории настроек
## (toggle-кнопки Язык/Экран/Звук/Подтверждения — CategoryLanguageButton и т.п.,
## задаются в _fC_BuildScreenConfig_Cr) и варианты языка (кнопки создаются
## ДИНАМИЧЕСКИ в _fC_RebuildLanguageList_Ch, не через конфиг — поэтому свой
## group нужен и им)
var _vGC_CategoryButtonGroup := ButtonGroup.new()
var _vGC_LanguageButtonGroup := ButtonGroup.new()
## 2026-08-19: ButtonGroup ВТОРОГО столбика внутри категории "Подтверждения"
## (Настройки/Обучение — ConfirmSourceSettingsButton/ConfirmSourceTrainingButton)
var _vGC_ConfirmSourceButtonGroup := ButtonGroup.new()

## 2026-08-19: отложенное применение размера экрана (окно "Применить/Отмена"
## при закрытии настроек, см. changelog) — пока пользователь двигает
## ширину/высоту/пресеты/fullscreen, реальный get_window() НЕ трогаем, только
## копим сюда "хочу применить" и взводим флаг; настоящее применение —
## _fC_CommitScreenSizeChange_Ch (по кнопке "Применить" или сразу при закрытии,
## если сам диалог отключён в Подтверждениях). Базовые значения выставляет
## _fC_OpenModal_Ch при каждом открытии Настроек — из реально сохранённых
## vXD_Settings_D, чтобы "тронуть только ширину" не портило pending fullscreen
## и наоборот. ⚠️ 2026-08-19: vLB_PendingFullscreen_Bv добавлен отдельно —
## раньше fullscreen применялся МИМО этого механизма сразу же (обратная связь
## "полноэкранный режим не работает через подтверждения" — теперь работает
## через тот же путь, что и размер, см. _fC_OnFullscreenToggled_Ch)
var vLB_ScreenSizeDirty_Bv := false
var vLI_PendingScreenWidth_I := 0
var vLI_PendingScreenHeight_I := 0
var vLB_PendingFullscreen_Bv := false

## 2026-08-19: антидребезг кнопки "Применить" в окне подтверждения — задумано
## как защита от "автоматического" (слишком быстрого, нечеловеческого) клика:
## пока не прошло настроенное "время защиты" (по умолчанию 0.5 сек, см.
## AutoClickProtectionSeconds в Settings.json / категория "Подтверждения"),
## клик не засчитывается как применение, а кнопка "убегает" на случайное
## место внутри своей зоны (см. _fC_OnScreenApplyButtonPressed_Ch/
## _fC_DodgeApplyButton_Ch). Убегает не бесконечно — AutoClickDodgeLimit (по
## умолчанию 1) ограничивает число "побегов" за один показ окна, дальше
## клики проходят как обычно (см. _vLI_ApplyButtonDodgeCount_I), чтобы
## пользователь гарантированно мог когда-нибудь применить настройки
## ⚠️ 2026-08-22 — БАГ НАЙДЕН И ПОЧИНЕН ("автоматическое нажатие не
## работает"): раньше _vLF_ApplyButtonLastClickMsec_F ("время ПРЕДЫДУЩЕГО
## клика") при показе окна сбрасывался в -1000.0 (глубоко в прошлом) — а
## защита сравнивала "сколько времени прошло с прошлого клика". У самого
## ПЕРВОГО клика "время с прошлого клика" получалось огромным (now - (-1000)),
## условие "слишком быстро" никогда не выполнялось — защита срабатывала
## только на быстрый ПОВТОРНЫЙ клик по уже однажды нажатой кнопке, а не на
## сам факт мгновенного нажатия сразу после появления окна — то есть от
## настоящего "нажал не глядя, сразу как открылось" не защищала ВООБЩЕ.
## Исправлено: теперь _vLF_ApplyButtonUnlockMsec_F — это АБСОЛЮТНОЕ время
## (устанавливается при ПОКАЗЕ окна = сейчас + AutoClickProtectionSeconds), а
## не "время прошлого клика" — защита действует, пока текущее время меньше
## этого дедлайна, независимо от того, был ли клик до этого вообще. Плюс —
## видимый счётчик (ScreenApplyConfirmCountdownHBox/_fC_OnApplyCountdownTick_Ch)
## показывает, сколько осталось, и исчезает, когда защита снята
var _vLF_ApplyButtonUnlockMsec_F := 0.0
var _vLI_ApplyButtonDodgeCount_I := 0

## 2026-08-23: окно выбора аккаунта — показывается при доходе чек-листа до
## проверки "Server" (см. _fC_ShowAccountPicker_Ch, вызывается из _ready()
## после успешной проверки "Interface"). Чтобы на одном устройстве можно было
## запустить два экземпляра программы и в одном выбрать реальный аккаунт
## (клиент), а в другом — "Server" (чтобы этот экземпляр поднимал у себя
## сервер вместо подключения к чужому). ACCOUNT_TECHNICAL_SERVER_VALUE — не
## настоящий аккаунт, а техническая метка для этого случая, поэтому его
## нельзя использовать как ник нового аккаунта (см. валидацию в
## _fC_OnAccountAddSavePressed_Ch)
const ACCOUNT_TECHNICAL_SERVER_VALUE := "Server"

## 2026-08-23 (правка): список аккаунтов теперь не хардкожен, а читается из
## res://Save/AccountsList.json (см. FileAccountsList/_fC_LoadAccountsList_Cr)
## — файл с реальными паролями сознательно НЕ шифруется и хранится плоским
## текстом (тот же уровень "для локальной проверки", что и остальной код на
## этом этапе, без TLS) — ключ словаря — ник, значение — {"ID","Password"}
var vLD_AccountsList_D := {}

## Что выбрал пользователь в окне выбора аккаунта — "" пока не выбрано,
## иначе один из ключей vLD_AccountsList_D или ACCOUNT_TECHNICAL_SERVER_VALUE
var vLS_SelectedAccount_S := ""

## 2026-08-23: реальное подключение к серверу (см. _fC_RunServerCheck_Ch/
## _fC_StartClientConnection_Ch/_process ниже) — заменяет прежнюю заглушку,
## которая сразу считала проверку "Server" пройденной. Фаза 1 (см.
## План проекта/Class_Internet.md → «После установления соединения»): реальное
## соединение+авторизация работают (Class_InternetClient/Server).
## 2026-08-23 (фаза 2): "Update"/"ThirdPartyFiles" тоже теперь настоящие
## проверки — см. _fC_OnServerConnected_Ch/_fC_OnClientAppOpReceived_Ch ниже и
## AppOpReceived_S (см. changelog Class_Internet.gd) — раньше обе сразу
## отмечались пройденными вместе с "Server", без реальной проверки
var Class_InternetClient = load("res://Godot_Template/Class_InternetClient.gd").new()

## Тот же адрес/порт, что слушает сервер в test.gd (SERVER_PORT_I) — для
## локальной проверки клиент+сервер на одном устройстве двумя экземплярами
## программы (см. changelog "Как запустить два экземпляра" в
## План проекта/Сцена Start_Program.md)
const SERVER_ADDRESS_S := "ws://127.0.0.1:9050"

## Сколько секунд ждём успешного подключения/входа, прежде чем предложить
## автономный режим (см. _fC_OfferAutonomousMode_Ch) — Class_InternetClient
## сам по себе пытается переподключаться бесконечно (см. fC_Reconnect_Ch), эта
## переменная не про "сдаться совсем", а именно про "когда предложить
## пользователю не ждать дальше". ⚠️ 2026-08-23 (ответ на вопрос "сколько раз
## мы пытаемся подключиться?"): сейчас РОВНО одна попытка (один вызов
## fxG_EstablishingConnection_CrTr в _fC_StartClientConnection_Ch) — это
## значение просто ограничивает, сколько ждать РЕЗУЛЬТАТА этой попытки, прежде
## чем показать кнопку "Продолжить без сервера"; сам клиент при этом не
## переподключается повторно, пока не откроется сокет хотя бы раз (fC_Reconnect_Ch
## вызывается только ПОСЛЕ разрыва уже открытого соединения, см. changelog
## Class_InternetClient.gd _process) — если разрешить "лишние" попытки при
## неудачном connect_to_url, менять здесь и/или в _fC_StartClientConnection_Ch
const SERVER_CONNECT_TIMEOUT_SECONDS_F := 8.0

var _vLB_ClientConnectionStarted_Bv := false
var _vLB_ServerCheckResolved_Bv := false
var _vLB_CredentialsSent_Bv := false
var _vLB_AutonomousModeOffered_Bv := false
var _vLF_ServerConnectAttemptStartedAtMsec_F := 0.0

## 2026-08-24 (по просьбе пользователя "добавим... регистрацию для входа"):
## true — текущая попытка подключения начата через "Зарегистрироваться" (см.
## _fC_OnAccountAddRegisterPressed_Ch), а не через выбор существующего
## аккаунта. Пока true, AUTHENTICATING-ветка _process шлёт Op "Register"
## вместо обычного AuthRequest (см. _fC_SendRegistrationRequest_Ch) — сама
## Class_InternetClient ничего не знает про регистрацию, различие полностью
## на стороне Start_Program. Сбрасывается в false в _fC_HandleRegisterResult_Ch
## (и успехом, и провалом — при успехе следующий кадр уже логинится обычным
## путём под тем же логином/паролем, при провале попытка просто прекращается)
var _vLB_PendingRegistration_Bv := false
var _vLB_RegistrationSent_Bv := false

## true — пользователь осознанно продолжил без сервера (см.
## _fC_OnAutonomousModePressed_Ch). 2026-08-23 (обратная связь "если
## подключаемся без сети — то все крестики"): "Server"/"Update"/
## "ThirdPartyFiles" в автономном режиме отмечаются "failed" (крестик), а не
## "passed" — честнее отражает "не проверено", а не "проверено и всё ок". Раз
## они больше не "passed", _fC_UpdateOverallProgress_Ch делает для них
## исключение именно по этому флагу (см. ниже), иначе чек-лист завис бы навсегда
var _vLB_AutonomousMode_Bv := false

## Версия ЭТОГО клиента — сверяется с сервером через Op "CheckUpdate" (см.
## _fC_SendUpdateCheckRequest_Ch). Пока просто константа в коде (см. пункт
## "вынести захардкоженные значения в настройки" в План проекта/Обзор проекта.md
## — сюда это тоже относится, но отдельным заходом); у test.gd — своя,
## "серверная" константа SERVER_LATEST_VERSION_S, с которой сверяется эта
const APP_VERSION_S := "0.1.0.0"


#Функционал:
#	Точка входа программы. Жёсткая последовательность с остановкой на первом
#	же провале — см. changelog про 2026-08-17 (нельзя увидеть интерфейс без
#	"галочки" логгера, нельзя пользоваться экраном без "галочки" интерфейса)
#Форматы данных:
#	Входные: нет
#	Выходные: нет
#Принцип работы:
#	1. Добавляет Class_UI в дерево сцены — без этого не работает get_tree()
#		внутри fC_Creating_Node_CrTr/fxC_SceneSwitching
#	2. Запускает логгер и ЧЕСТНО проверяет результат ДО того, как строить
#		хоть что-то на экране. Провал — fC_FatalStartupError_Ch и выход,
#		дальше _ready() не идёт (интерфейса без рабочего логгера не будет)
#	3. Строит экран (это и есть проверка "Interface") и ЧЕСТНО проверяет,
#		что все ноды из конфига реально создались. Провал — та же
#		fC_FatalStartupError_Ch, экран не считается готовым к использованию
#	4. Только если оба шага прошли — статусы "Logger"/"Interface" в чек-листе
#		становятся "✓", остальные 3 проверки остаются "Ожидает проверки" —
#		их подключение (сетевой код) — следующий этап
func _ready() -> void:
	add_child(Class_UI)

	# Локализация и настройки — до сборки экрана, чтобы окно Поддержка/Настройки
	# сразу собиралось с правильным языком и чтобы сохранённый размер/режим окна
	# применился ДО того, как пользователь вообще увидит экран (см. changelog
	# 2026-08-19 про полную переработку окна настроек). Ошибку локализации
	# (если есть) сохраняем и показываем/логируем ПОЗЖЕ — ни лога (нет ещё
	# vLS_StreamId), ни экрана (InfoBoxLabel ещё не создан) на этом шаге нет
	var vLS_LocalizationError_S: String = Class_Localization.fxG_LoadLocalization_Ch()
	vXD_Settings_D = _fC_LoadSettings_Cr()
	# 2026-08-23: список аккаунтов грузим ДО _fC_BuildScreen_Ch() ниже — экран
	# выбора аккаунта собирается из vLD_AccountsList_D уже на первой сборке
	# (см. цикл в _fC_BuildScreenConfig_Cr)
	vLD_AccountsList_D = _fC_LoadAccountsList_Cr()
	Class_Localization.fC_SetLocale_Ch(vXD_Settings_D.get("Locale", "en"))
	if vXD_Settings_D.get("Fullscreen", false):
		get_window().mode = Window.MODE_FULLSCREEN
	else:
		get_window().mode = Window.MODE_WINDOWED
		get_window().size = Vector2i(
			int(vXD_Settings_D.get("ScreenWidth", vSV2_Scene_size_V2i.x)),
			int(vXD_Settings_D.get("ScreenHeight", vSV2_Scene_size_V2i.y))
		)

	Class_Logger.fxG_StartingLogger_CrTr()
	vLS_StreamId = Class_Logger.fC_RegisterStream_Cr("StartProgram")

	# Шаг 1: логгер — интерфейс ещё не существует, честную проверку делаем "вслепую"
	var vLB_LoggerOk: bool = Class_Logger.FileLogger != "" and FileAccess.file_exists(Class_Logger.FileLogger)
	if not vLB_LoggerOk:
		_fC_FatalStartupError_Ch("Логгер", "Не удалось запустить логгер — файл лога не создан.")
		return
	vLD_CheckStatuses_D["Logger"] = "passed"
	Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_ready: Logger check passed", [], null)

	# Шаг 2: интерфейс — сама попытка построить экран и есть эта проверка.
	# Статус "checking" выставляем ДО сборки, чтобы строка чек-листа сразу
	# появилась с "…", а не мелькнула "waiting" на один кадр
	vLD_CheckStatuses_D["Interface"] = "checking"
	_fC_BuildScreen_Ch()

	if not _fC_VerifyInterfaceBuilt_Bv():
		_fC_FatalStartupError_Ch("Интерфейс", "Не все элементы экрана удалось создать.")
		return
	fC_SetCheckStatus_Ch("Interface", "passed")
	Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_ready: Interface check passed", [], null)

	# 2026-08-23: проверка "Server" начинается с выбора аккаунта — см.
	# _fC_ShowAccountPicker_Ch. Update/ThirdPartyFiles по-прежнему не
	# подключены — следующий этап, как и раньше
	fC_SetCheckStatus_Ch("Server", "checking")
	_fC_ShowAccountPicker_Ch()

	# Живой перерасчёт размеров шрифта под окно (2026-08-19, "шрифт... должен
	# зависеть от размеров окна") — срабатывает и на ручное перетаскивание
	# края окна пользователем, и на наши fullscreen/пресеты в Настройках
	get_window().size_changed.connect(_fC_RescaleFonts_Ch)

	# Теперь и лог, и экран (InfoBoxLabel) точно готовы — можно безопасно
	# сообщить об ошибке локализации, если она была (п.3 ТЗ 2026-08-19: не
	# push_error, а окно с ошибками + лог, программа при этом не прерывается —
	# без переводов экран настроек просто покажет сырые ключи вместо текста)
	if vLS_LocalizationError_S != "":
		_fC_ReportError_Ch("Локализация", vLS_LocalizationError_S)


#Функционал:
#	2026-08-23: опрашивает состояние Class_InternetClient раз в кадр, пока
#	идёт (или ждём начала) подключение к серверу — сама библиотека
#	(Class_InternetClient) ничего не сигналит наружу, только меняет
#	vGE_State/ConnectionState, поэтому Start_Program следит за ним сама.
#	Ничего не делает, пока подключение ещё не запускалось
#	(_vLB_ClientConnectionStarted_Bv) или уже разрешилось
#	(_vLB_ServerCheckResolved_Bv — успешным подключением или автономным
#	режимом, см. _fC_OnServerConnected_Ch/_fC_OnAutonomousModePressed_Ch)
func _process(_vLF_Delta_F: float) -> void:
	if not _vLB_ClientConnectionStarted_Bv or _vLB_ServerCheckResolved_Bv:
		return

	_fC_UpdateConnectingTimerLabel_Ch()

	match Class_InternetClient.vGE_State:
		Class_InternetClient.ConnectionState.AUTHENTICATING:
			# 2026-08-24: попытка, начатая через "Зарегистрироваться" — сначала
			# Register, обычный вход (_fC_SendStoredCredentials_Ch) начнётся уже
			# на следующем кадре после успеха, см. _fC_HandleRegisterResult_Ch
			if _vLB_PendingRegistration_Bv:
				_fC_SendRegistrationRequest_Ch()
			else:
				_fC_SendStoredCredentials_Ch()
		Class_InternetClient.ConnectionState.CONNECTED:
			_fC_OnServerConnected_Ch()
		_:
			if not _vLB_AutonomousModeOffered_Bv and Time.get_ticks_msec() - _vLF_ServerConnectAttemptStartedAtMsec_F > SERVER_CONNECT_TIMEOUT_SECONDS_F * 1000.0:
				_fC_OfferAutonomousMode_Ch()


#Функционал:
#	2026-08-24 (обратная связь "когда подключаемся к серверу, в окошке снизу
#	должен появляться таймер, чтобы человек понимал, что подключение
#	работает"): раз в кадр, пока идёт попытка подключения (см. вызов из
#	_process выше — под той же охраной, что и вся остальная обвязка
#	подключения), обновляет текст InfoBoxConnectingLabel количеством
#	прошедших секунд от начала ЭТОЙ попытки (_vLF_ServerConnectAttemptStartedAtMsec_F
#	— выставляется в _fC_StartClientConnection_Ch, общая и для первой
#	попытки, и для повтора через InfoBoxRetryButton)
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_UpdateConnectingTimerLabel_Ch() -> void:
	if not vSD_AllNodes_D.has("InfoBoxConnectingLabel"):
		return
	var vLI_ElapsedSeconds_I: int = int((Time.get_ticks_msec() - _vLF_ServerConnectAttemptStartedAtMsec_F) / 1000.0)
	vSD_AllNodes_D["InfoBoxConnectingLabel"]["Node"].text = "Подключение к серверу… %d сек" % vLI_ElapsedSeconds_I


#Функционал:
#	2026-08-20 — обратная связь: "добавь сворачивания окна на esc, если там
#	был выбор — он выбирается в сторону нет". Обрабатывает Esc
#	(is_action_pressed("ui_cancel") — стандартное действие Godot, по
#	умолчанию завязано на клавишу Escape независимо от раскладки клавиатуры)
#	и закрывает САМЫЙ ВЕРХНИЙ из открытых сейчас окон/попапов, по приоритету
#	сверху вниз (см. подробности в changelog у _ready() чуть выше):
#	1) ScreenApplyConfirmOverlay (окно "Применить/Отмена") — Esc = "Отмена",
#	   то есть буквально "выбор в сторону нет";
#	2) ScreenInfoPopupOverlay (окно-инфо, у него нет выбора Да/Нет) — Esc
#	   просто закрывает его, как и кнопка "Понял";
#	2.5) LoggerInfoPopupOverlay (2026-08-22, тот же принцип, что и у п.2 —
#	   окно-инфо "изменения применятся после перезапуска", без выбора Да/Нет);
#	3) иначе — если открыто само окно Настройки/Поддержка — Esc закрывает
#	   его тем же путём, что и крестик/клик мимо (если при этом есть
#	   несохранённый размер экрана — по цепочке откроется ScreenApplyConfirmOverlay,
#	   как и при обычном закрытии).
#	set_input_as_handled() после срабатывания — чтобы один Esc не сработал
#	сразу на нескольких уровнях за один кадр
#Форматы данных:
#	Входные:
#		vL_Event: InputEvent — событие от движка (_unhandled_input получает
#			ввод, который не был перехвачен ни одним Control'ом раньше)
func _unhandled_input(vL_Event: InputEvent) -> void:
	if not vL_Event.is_action_pressed("ui_cancel"):
		return
	if vSD_AllNodes_D.has("ScreenApplyConfirmOverlay") and vSD_AllNodes_D["ScreenApplyConfirmOverlay"]["Node"].visible:
		_fC_OnScreenApplyCancelPressed_Ch()
		get_viewport().set_input_as_handled()
		return
	if vSD_AllNodes_D.has("ScreenInfoPopupOverlay") and vSD_AllNodes_D["ScreenInfoPopupOverlay"]["Node"].visible:
		_fC_OnScreenInfoOkPressed_Ch()
		get_viewport().set_input_as_handled()
		return
	# 2026-08-22: третье info-окно (LoggerInfoPopupOverlay, "изменения применятся
	# после перезапуска") — та же логика, что и у ScreenInfoPopupOverlay выше:
	# у него нет выбора Да/Нет, Esc просто закрывает его, как и кнопка "Понял"
	if vSD_AllNodes_D.has("LoggerInfoPopupOverlay") and vSD_AllNodes_D["LoggerInfoPopupOverlay"]["Node"].visible:
		_fC_OnLoggerInfoOkPressed_Ch()
		get_viewport().set_input_as_handled()
		return
	if vLB_ModalOpen_Bv:
		_fC_CloseModal_Ch()
		get_viewport().set_input_as_handled()


#Функционал:
#	Строит конфиг словаря для узла-конфига (__Data_D/__Inspector_D/__Node_D,
#	опционально __Children_D) — сокращает повторяющийся код при описании экрана.
#	Путь до родителя и имя ноды НЕ указываются — их выставляет сам Class_UI
#	(fC_Mass_Creating_Node_CrTr/fC_SyncNodesWithConfig_Ch) по ключу словаря и
#	его месту в дереве (см. changelog)
#Форматы данных:
#	Входные:
#		vLS_Type_S: String — тип ноды Godot (ClassDB)
#		vLD_Inspector_D: Dictionary — свойства ноды (без "name" — его выставит Class_UI)
#		vLD_Signals_D: Dictionary — {имя_сигнала: Callable}, по умолчанию нет
#		vLD_Children_D: Dictionary — {ИмяРебёнка: конфиг_ребёнка (в этом же формате)},
#			по умолчанию нет — попадает в конфиг ноды под ключом __Children_D
#	Выходные:
#		Dictionary — готовый вложенный конфиг ноды для Class_UI
func _fC_NodeCfg(vLS_Type_S: String, vLD_Inspector_D: Dictionary, vLD_Signals_D: Dictionary = {}, vLD_Children_D: Dictionary = {}) -> Dictionary:
	var vLD_Result_D := {
		"__Data_D": {
			"__type_S": vLS_Type_S,
			"__Scene_size_V2i": vSV2_Scene_size_V2i,
		},
		"__Inspector_D": vLD_Inspector_D.duplicate(),
		"__Node_D": vLD_Signals_D,
	}
	if not vLD_Children_D.is_empty():
		vLD_Result_D["__Children_D"] = vLD_Children_D
	return vLD_Result_D


#Функционал:
#	Сливает базовые Inspector-свойства с доп. свойствами (например, якорями под
#	конкретную ориентацию), не изменяя переданные словари
func _fC_MergeInspector_Cr(vLD_Base_D: Dictionary, vLD_Extra_D: Dictionary) -> Dictionary:
	var vLD_Result_D: Dictionary = vLD_Base_D.duplicate()
	vLD_Result_D.merge(vLD_Extra_D)
	return vLD_Result_D


#Функционал:
#	Загружает настройки из res://Save/Settings.json и достраивает недостающие
#	ключи значениями по умолчанию — файл сейчас может быть пустым/содержать
#	null (Class_Json.fxG_LoadJson_Cr в этом случае честно вернёт {}, см. её
#	changelog), а после первого сохранения — только часть ключей, если формат
#	настроек расширится позже
#Форматы данных:
#	Входные: нет
#	Выходные:
#		Dictionary — {Locale, ScreenWidth, ScreenHeight, Fullscreen,
#			MusicVolume, InterfaceVolume}, все ключи гарантированно заполнены
#Принцип работы:
#	Locale, если не сохранён (или сохранён пустой строкой) — берём язык
#	системы через Class_Localization.fC_DetectSystemLocale_S (см. п.5 ТЗ:
#	"если ничего не указано в настройках — получать язык системы")
func _fC_LoadSettings_Cr() -> Dictionary:
	var vLD_Loaded_D: Dictionary = Class_Json.fxG_LoadJson_Cr(FileSettings)

	if not vLD_Loaded_D.has("Locale") or vLD_Loaded_D["Locale"] == "":
		vLD_Loaded_D["Locale"] = Class_Localization.fC_DetectSystemLocale_S()
	if not vLD_Loaded_D.has("ScreenWidth"):
		vLD_Loaded_D["ScreenWidth"] = int(vSV2_Scene_size_V2i.x)
	if not vLD_Loaded_D.has("ScreenHeight"):
		vLD_Loaded_D["ScreenHeight"] = int(vSV2_Scene_size_V2i.y)
	if not vLD_Loaded_D.has("Fullscreen"):
		vLD_Loaded_D["Fullscreen"] = false
	if not vLD_Loaded_D.has("MusicVolume"):
		vLD_Loaded_D["MusicVolume"] = 100
	if not vLD_Loaded_D.has("InterfaceVolume"):
		vLD_Loaded_D["InterfaceVolume"] = 100
	# 2026-08-19: окна-подтверждения при изменении размера экрана (см. changelog
	# про "окно применить/отмена") — по умолчанию ОБА показываются (true),
	# пользователь может отключить каждое отдельно через категорию "Подтверждения"
	if not vLD_Loaded_D.has("ShowScreenChangeInfoPopup"):
		vLD_Loaded_D["ShowScreenChangeInfoPopup"] = true
	if not vLD_Loaded_D.has("ShowScreenApplyConfirmPopup"):
		vLD_Loaded_D["ShowScreenApplyConfirmPopup"] = true
	# 2026-08-19: "защита от автоматического нажатия человеком" — категория
	# "Подтверждения" → "Автоматическое нажатие человеком" (ползунки), см.
	# _fC_OnScreenApplyButtonPressed_Ch. AutoClickProtectionSeconds — сколько
	# секунд после клика по "Применить" следующий клик считается "слишком
	# быстрым"; AutoClickDodgeLimit — сколько раз подряд кнопка успеет
	# "убежать" за один показ окна, прежде чем клики станут обычными
	if not vLD_Loaded_D.has("AutoClickProtectionSeconds"):
		vLD_Loaded_D["AutoClickProtectionSeconds"] = 0.5
	if not vLD_Loaded_D.has("AutoClickDodgeLimit"):
		vLD_Loaded_D["AutoClickDodgeLimit"] = 1
	# 2026-08-20: глобальный множитель размера шрифта — категория "Экран" →
	# ScreenFontScaleLineEdit (п.5 ТЗ: "давай в настройке экрана добавим размер
	# шрифта, сохраняем в настройки и влияет на всё"). 1.0 = обычный размер
	# (см. _fC_FontSize_I — домножается ПОВЕРХ уже существующего масштаба по
	# размеру окна, то есть это ДОПОЛНИТЕЛЬНЫЙ множитель, а не замена)
	if not vLD_Loaded_D.has("FontScale"):
		vLD_Loaded_D["FontScale"] = 1.0

	# 2026-08-22: категория "Логи и реплеи" — лимиты на хранение логов
	# программы и реплеев (6 лимитов: количество/время/объём памяти — на
	# каждый и Enabled-флаг для галочки-квадрата слева, и само число для поля
	# ввода справа). ⚠️ Здесь заводятся ТОЛЬКО настройки и их UI — реальную
	# ротацию/удаление старых логов и реплеев по этим лимитам делает (или
	# будет делать) сам Class_Logger/будущая система реплеев, этот экран их
	# не трогает и не читает, просто хранит значения (тот же принцип, что и
	# у AutoClickProtectionSeconds/AutoClickDodgeLimit — вычитывает их уже
	# _fC_OnScreenApplyButtonPressed_Ch, а не этот файл)
	if not vLD_Loaded_D.has("LogMaxCountEnabled"):
		vLD_Loaded_D["LogMaxCountEnabled"] = true
	if not vLD_Loaded_D.has("LogMaxCount"):
		vLD_Loaded_D["LogMaxCount"] = 100
	if not vLD_Loaded_D.has("LogMaxAgeEnabled"):
		vLD_Loaded_D["LogMaxAgeEnabled"] = true
	if not vLD_Loaded_D.has("LogMaxAgeDays"):
		vLD_Loaded_D["LogMaxAgeDays"] = 30
	if not vLD_Loaded_D.has("LogMaxSizeEnabled"):
		vLD_Loaded_D["LogMaxSizeEnabled"] = true
	if not vLD_Loaded_D.has("LogMaxSizeMB"):
		vLD_Loaded_D["LogMaxSizeMB"] = 50
	if not vLD_Loaded_D.has("ReplayMaxCountEnabled"):
		vLD_Loaded_D["ReplayMaxCountEnabled"] = true
	if not vLD_Loaded_D.has("ReplayMaxCount"):
		vLD_Loaded_D["ReplayMaxCount"] = 20
	if not vLD_Loaded_D.has("ReplayMaxAgeEnabled"):
		vLD_Loaded_D["ReplayMaxAgeEnabled"] = true
	if not vLD_Loaded_D.has("ReplayMaxAgeDays"):
		vLD_Loaded_D["ReplayMaxAgeDays"] = 7
	if not vLD_Loaded_D.has("ReplayMaxSizeEnabled"):
		vLD_Loaded_D["ReplayMaxSizeEnabled"] = true
	if not vLD_Loaded_D.has("ReplayMaxSizeMB"):
		vLD_Loaded_D["ReplayMaxSizeMB"] = 200
	# 2026-08-22: окно-инфо "изменения применятся после перезапуска" для
	# категории "Логи и реплеи" — по умолчанию показывается (true), тот же
	# принцип отключаемого уведомления, что и у ShowScreenChangeInfoPopup выше
	if not vLD_Loaded_D.has("ShowLoggerRestartInfoPopup"):
		vLD_Loaded_D["ShowLoggerRestartInfoPopup"] = true

	return vLD_Loaded_D


#Функционал:
#	Сохраняет текущий словарь настроек в res://Save/Settings.json — тонкая
#	обёртка над Class_Json.fxG_SaveJson_Ch, чтобы путь к файлу не дублировался
#	по всем местам, где что-то в настройках меняется
func _fC_SaveSettings_Ch(vLD_Settings_D: Dictionary) -> void:
	Class_Json.fxG_SaveJson_Ch(FileSettings, vLD_Settings_D)


#Функционал:
#	2026-08-23: читает список аккаунтов из res://Save/AccountsList.json (тот
#	же Class_Json, что и у настроек). Если файла ещё нет (первый запуск —
#	Class_Json.fxG_LoadJson_Cr вернёт {} и сама запишет push_error, это
#	ожидаемо и не является настоящей ошибкой) — заводит один аккаунт-пример
#	"Пользователь1" и СРАЗУ сохраняет его на диск через
#	_fC_SaveAccountsList_Ch, чтобы после первого запуска файл реально
#	появился (в отличие от настроек, этот файл — не "текущий черновик,
#	сохраняемый по кнопке Применить", а единственный источник правды для
#	окна выбора аккаунта, поэтому дефолт пишем на диск сразу же)
#	⚠️ Пароль в файле хранится обычным текстом, без хеширования — тот же
#	уровень "для локальной проверки на этом этапе", что и у остального сетевого
#	кода без TLS (см. План проекта/Class_Internet.md). Это ОТДЕЛЬНО от
#	PasswordHash на сервере (Class_InternetServer.FileUsers_S) — этот файл
#	описывает УЖЕ существующие клиентские аккаунты для окна выбора на старте,
#	а не серверную базу пользователей
#Форматы данных:
#	Входные: нет
#	Выходные:
#		Dictionary — {Ник: {"ID": String, "Password": String}, ...}
func _fC_LoadAccountsList_Cr() -> Dictionary:
	var vLD_Loaded_D: Dictionary = Class_Json.fxG_LoadJson_Cr(FileAccountsList)

	if vLD_Loaded_D.is_empty():
		vLD_Loaded_D = {
			"Пользователь1": {"ID": "user1", "Password": "12345"},
		}
		_fC_SaveAccountsList_Ch(vLD_Loaded_D)

	return vLD_Loaded_D


#Функционал:
#	Сохраняет текущий словарь аккаунтов в res://Save/AccountsList.json —
#	тонкая обёртка над Class_Json.fxG_SaveJson_Ch, тот же принцип, что и у
#	_fC_SaveSettings_Ch
func _fC_SaveAccountsList_Ch(vLD_Accounts_D: Dictionary) -> void:
	Class_Json.fxG_SaveJson_Ch(FileAccountsList, vLD_Accounts_D)


#Функционал:
#	Считает реальный пиксельный размер шрифта под указанный стандартный
#	уровень (vGD_BaseFontSizes_D) — база задана для design-канвы
#	vSV2_Scene_size_V2i (540x900), реальный размер домножается на масштаб
#	ТЕКУЩЕГО окна ОС (2026-08-19, обратная связь: "шрифт должен... зависеть
#	от размеров окна"). round() — не только чтобы размер был целым числом
#	пикселей "для красоты": дробный итоговый размер шрифта — одна из реальных
#	причин размытости текста (см. changelog про "он был не постоянно размыт")
#Форматы данных:
#	Входные:
#		vLS_Tier_S: String — ключ из vGD_BaseFontSizes_D ("Small".."Large")
#	Выходные:
#		int — реальный размер в пикселях, не меньше 8 (страховка от нечитаемо
#			мелкого текста при экстремально маленьком окне)
#Принцип работы:
#	Масштаб — min(ширина_окна/540, высота_окна/900): именно min, а не среднее
#	и не отдельно по X/Y — иначе при сильно "нестандартном" соотношении сторон
#	окна (например, очень широком, но невысоком) текст мог бы вылезти за
#	границы своих контейнеров по высоте. 2026-08-20: ПОВЕРХ этого масштаба
#	домножается ещё и пользовательский vXD_Settings_D["FontScale"] (по
#	умолчанию 1.0, категория "Экран" → ScreenFontScaleLineEdit, п.5 ТЗ) — два
#	независимых множителя: один подстраивается под размер окна автоматически,
#	второй пользователь крутит вручную поверх него
func _fC_FontSize_I(vLS_Tier_S: String) -> int:
	var vLI_Base_I: int = vGD_BaseFontSizes_D.get(vLS_Tier_S, 16)
	var vLV2_WindowSize_V2: Vector2 = Vector2(get_window().size)
	var vLF_ScaleW_F: float = vLV2_WindowSize_V2.x / vSV2_Scene_size_V2i.x
	var vLF_ScaleH_F: float = vLV2_WindowSize_V2.y / vSV2_Scene_size_V2i.y
	var vLF_Scale_F: float = min(vLF_ScaleW_F, vLF_ScaleH_F)
	var vLF_UserFontScale_F: float = vXD_Settings_D.get("FontScale", 1.0)
	return max(8, int(round(vLI_Base_I * vLF_Scale_F * vLF_UserFontScale_F)))


#Функционал:
#	Собирает {ИмяНоды: {"theme_override_font_sizes/font_size": N}} по ВСЕМУ
#	дереву конфига — используется для перерасчёта размеров шрифта при смене
#	размера окна БЕЗ полной пересборки экрана (см. _fC_RescaleFonts_Ch).
#	Переиспользует тот же принцип обхода, что и _fC_CountNodeConfigs_I —
#	спускается в любую Dictionary, которая сама не нода, независимо от имени
#Форматы данных:
#	Входные:
#		vLD_Tree_D: Dictionary — дерево конфига (или его часть)
#	Выходные:
#		Dictionary — плоский {ИмяНоды: {свойство: значение}}, формат, который
#			ожидает Class_UI.fxC_UpdateExistingNodes_Ch
func _fC_CollectFontSizeUpdates_Cr(vLD_Tree_D: Dictionary) -> Dictionary:
	var vLD_Updates_D := {}
	for elLS_Key in vLD_Tree_D:
		if elLS_Key == "__Data_D" or elLS_Key == "__Inspector_D" or elLS_Key == "__Node_D" or elLS_Key == "v__Ebeveyn":
			continue
		var vL_Value = vLD_Tree_D[elLS_Key]
		if typeof(vL_Value) != TYPE_DICTIONARY:
			continue
		if Class_UI._fC_IsNodeConfig(vL_Value):
			var vLD_Inspector_D: Dictionary = vL_Value.get("__Inspector_D", {})
			if vLD_Inspector_D.has("theme_override_font_sizes/font_size"):
				vLD_Updates_D[elLS_Key] = {"theme_override_font_sizes/font_size": vLD_Inspector_D["theme_override_font_sizes/font_size"]}
		vLD_Updates_D.merge(_fC_CollectFontSizeUpdates_Cr(vL_Value))
	return vLD_Updates_D


#Функционал:
#	Живой перерасчёт размеров шрифта под текущий размер окна — вызывается на
#	get_window().size_changed (подключено в _ready()), то есть срабатывает и
#	на ручное перетаскивание края окна, и на наши собственные
#	_fC_ApplyScreenSize_Ch/_fC_OnFullscreenToggled_Ch. НЕ пересобирает экран
#	целиком (ноды/раскладка через anchor и так масштабонезависимы) — строит
#	СВЕЖИЙ конфиг только чтобы получить актуальные числа размеров шрифта
#	(_fC_FontSize_I внутри него уже посчитает их под новый get_window().size)
#	и точечно обновляет через уже существующий Class_UI.fxC_UpdateExistingNodes_Ch
func _fC_RescaleFonts_Ch() -> void:
	var vXD_FreshConfig_D: Dictionary = _fC_BuildScreenConfig_Cr(vLS_Orientation_S)
	var vLD_FontUpdates_D: Dictionary = _fC_CollectFontSizeUpdates_Cr(vXD_FreshConfig_D)
	Class_UI.fxC_UpdateExistingNodes_Ch(vSD_AllNodes_D, vLD_FontUpdates_D)

	# Список языков создаётся НАПРЯМУЮ (не через конфиг, см. changelog
	# _fC_RebuildLanguageList_Ch), поэтому _fC_CollectFontSizeUpdates_Cr его не
	# видит — перестраиваем отдельно, с тем же текстом поиска, что уже введён
	if vSD_AllNodes_D.has("LanguageSearchLineEdit"):
		_fC_RebuildLanguageList_Ch(vSD_AllNodes_D["LanguageSearchLineEdit"]["Node"].text)

	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_RescaleFonts_Ch", [vLD_FontUpdates_D.size()], null)


#Функционал:
#	Материал размытия для ModalOverlay (2026-08-19: "часть что затемняет
#	задний фон — не плохо было бы добавить размытия"). Читает
#	res://Shaders/ModalOverlayBlur.gdshader (шейдер SCREEN_TEXTURE + box-blur,
#	см. сам файл шейдера) — если файл ещё не записан на диск/не грузится,
#	возвращает null, а не падает: ColorRect просто останется однотонным
#	затемнением без размытия, как было раньше (тихая деградация, а не сбой)
#	⚠️ 2026-08-19: blur_amount теперь РАДИУС в пикселях (сэмплы подряд, без
#	дыр) — раньше это был множитель шага между всего 5 сэмплами, из-за чего
#	между сэмплами были дыры и на шрифтах получалась "сетка из точек" (см.
#	changelog самого шейдера). 3.0 подобрано как заметное, но не чрезмерно
#	дорогое размытие (7x7 = 49 сэмплов на пиксель) для статичного экрана
#Форматы данных:
#	Входные: нет
#	Выходные:
#		ShaderMaterial — готов для "material" у ModalOverlay; или null при
#			любой проблеме с загрузкой шейдера
func _fC_ModalOverlayBlurMaterial_Cr() -> ShaderMaterial:
	if not ResourceLoader.exists(FileModalBlurShader):
		return null
	var vL_Shader: Shader = load(FileModalBlurShader)
	if vL_Shader == null:
		return null
	var vL_Material := ShaderMaterial.new()
	vL_Material.shader = vL_Shader
	vL_Material.set_shader_parameter("tint_color", Color(0, 0, 0, 0.55))
	vL_Material.set_shader_parameter("blur_amount", 3.0)
	return vL_Material


#	2026-08-22 - mzs7 - ⚠️ ОТКАЗ ОТ РАЗМЫТИЯ у %sBackdrop (была
#	_fC_ConfirmBackdropBlurMaterial_Cr — своя, более лёгкая версия материала
#	блюра, radius=1.0/alpha=0.30 вместо 3.0/0.55 у большого ModalOverlay,
#	функция удалена). Причина: обратная связь показала на скриншоте, что даже
#	после ослабления та же проблема осталась — сквозь %sBackdrop (окно
#	"Применить/Отмена", как и инфо-окно) виден чек-лист запуска ("Запуск",
#	"Соединение" и т.п. с зелёными галочками) — элемент СТАРТОВОГО экрана,
#	который в принципе не должен быть виден: он должен быть полностью скрыт
#	позади непрозрачной карточки настроек (ModalWindowPanel, alpha=1) и
#	затемнённого/размытого ModalOverlay ЕЩЁ ДО того, как поверх открылся
#	маленький попап. Раз материал самого попапа ПОКАЗЫВАЕТ то, что находится
#	ДВУМЯ слоями глубже (стартовый экран, а не карточку настроек прямо под
#	собой) — дело не в силе размытия/затемнения (это лечится только по
#	касательной), а в том, ЧТО читает SCREEN_TEXTURE в момент отрисовки.
#	Вероятная причина — известное ограничение рендерера Compatibility
#	(project.godot: renderer/rendering_method="gl_compatibility"): экранные
#	шейдеры (hint_screen_texture) в этом рендерере не гарантируют "то, что
#	нарисовано ДО меня В ЭТОМ ЖЕ кадре" так же надёжно, как в Forward+/Mobile
#	— используется бэкбафер откуда-то из предыдущего состояния кадра, и в
#	многослойных сценах (стартовый экран → ModalOverlay → карточка настроек →
#	ещё один попап поверх) он может "видеть" не самый верхний уже
#	нарисованный слой, а более ранний. Чинить сам рендерер/шейдер вслепую (без
#	возможности визуально проверить результат) слишком рискованно — вместо
#	этого %sBackdrop больше ВООБЩЕ не читает экран: material убран, остаётся
#	только собственный плоский цвет (color, alpha=0.45, БЕЗ размытия) —
#	просто закрашенный прямоугольник поверх нужной полосы, гарантированно
#	без любых сюрпризов с тем, что через него "просвечивает". Внешне это
#	чуть менее эффектно (нет стеклянного размытия), зато со 100% надёжностью
#	не покажет ничего постороннего. Большой ModalOverlay (за пределами этого
#	метода) размытие сохраняет — сам по себе он такого бага не показывал, и
#	трогать его без явного запроса не стал ("менять можно только с
#	разрешения") — но если там тоже когда-нибудь "просветит" что-то чужое,
#	причина будет та же самая, и лечится так же (снять material)


#Функционал:
#	"Материал" карточки модального окна (Поддержка/Настройки) — сплошной
#	непрозрачный фон со скруглением. Вынесен в отдельную функцию (п.5 ТЗ
#	2026-08-19: "вынесем создание окна и изменение материала в отдельную
#	функцию, так как будем пользоваться этим часто") — раньше Panel красилась
#	стандартной темой Godot (местами полупрозрачной), из-за чего окно выглядело
#	просвечивающим; теперь стиль один переиспользуемый StyleBoxFlat
#Форматы данных:
#	Входные: нет
#	Выходные:
#		StyleBoxFlat — непрозрачный (alpha=1) фон, готов для
#			"theme_override_styles/panel" у Panel-подобных нод
func _fC_ModalCardStyle_Cr() -> StyleBoxFlat:
	var vL_Style := StyleBoxFlat.new()
	vL_Style.bg_color = Color(0.12, 0.12, 0.15, 1.0) # alpha=1 — гарантированно непрозрачный, не зависит от темы
	vL_Style.set_corner_radius_all(12)
	vL_Style.set_content_margin_all(0)
	return vL_Style


#Функционал:
#	Строит переиспользуемую "рамку" модального окна — саму карточку
#	(ModalWindowPanel, непрозрачная, 80% экрана — п.1 ТЗ 2026-08-19) с
#	заголовком (название СЛЕВА, крестик закрытия СПРАВА — см. changelog про
#	"поменяй местами право и лево") и телом, куда подставляется переданное
#	содержимое. Вынесена отдельно (п.5 ТЗ), чтобы будущие модальные окна
#	(не только Поддержка/Настройки) могли переиспользовать ту же рамку, не
#	копируя разметку заголовка/стиль карточки заново
#Форматы данных:
#	Входные:
#		vLD_BodyChildren_D: Dictionary — {ИмяРебёнка: конфиг_ребёнка (формат
#			_fC_NodeCfg)} — содержимое тела окна, порядок ключей = порядок
#			слева направо внутри ModalBodyHBox
#	Выходные:
#		Dictionary — {"ModalWindowPanel": конфиг_ноды} — один верхнеуровневый
#			ключ, готовый как __Children_D-значение для ModalOverlay
#Принцип работы:
#	Панель занимает 80% экрана через anchor 0.1..0.9 по обеим осям с нулевыми
#	offset — в отличие от фиксированных пиксельных offset'ов (было раньше),
#	такой размер сам пересчитывается под любое разрешение экрана
func _fC_BuildModalChrome_Cr(vLD_BodyChildren_D: Dictionary) -> Dictionary:
	return {
		"ModalWindowPanel": _fC_NodeCfg("Panel", {
			"anchor_left": 0.1, "anchor_top": 0.1,
			"anchor_right": 0.9, "anchor_bottom": 0.9,
			"offset_left": 0, "offset_top": 0, "offset_right": 0, "offset_bottom": 0,
			"theme_override_styles/panel": _fC_ModalCardStyle_Cr(),
		}, {}, {
			"ModalWindowVBox": _fC_NodeCfg("VBoxContainer", {
				"anchor_left": 0.0, "anchor_top": 0.0,
				"anchor_right": 1.0, "anchor_bottom": 1.0,
				"offset_left": 18, "offset_top": 14, "offset_right": -18, "offset_bottom": -14,
				"theme_override_constants/separation": 12,
			}, {}, {
				# Заголовок: название СЛЕВА, крестик закрытия СПРАВА (2026-08-19,
				# п.1 ТЗ — было наоборот, "поменяли местами право и лево")
				# 2026-08-20: ⚠️ БАГ НАЙДЕН И ПОЧИНЕН — "надпись Настройки стала
				# вертикальной, из-за чего сломался UI". Причина: у
				# ModalWindowLabel добавили autowrap_mode (см. changelog 2026-08-20 п.5
				# "перенос текста"), но сам Label остался БЕЗ
				# size_flags_horizontal=3 (EXPAND) — в HBoxContainer весь "лишний"
				# растягиваемый запас забирал соседний ModalHeaderSpacer (у него
				# EXPAND был), а Label ужимался до минимума. У Label С autowrap
				# минимальная ширина — это ширина ОДНОГО символа (а не
				# всего текста, как было без autowrap) — контейнер давал ему
				# именно столько, и "Настройки"/"Поддержка" переносились по
				# букве, столбиком. Исправлено: ModalHeaderSpacer убран целиком,
				# вместо него EXPAND (size_flags_horizontal=3) теперь у самого
				# ModalWindowLabel — он один забирает весь свободный запас
				# (крестик закрытия ModalCloseButton, у которого EXPAND нет,
				# из-за этого сам остаётся прижат к правому краю, как и раньше)
				"ModalHeaderHBox": _fC_NodeCfg("HBoxContainer", {
					"theme_override_constants/separation": 8,
				}, {}, {
					"ModalWindowLabel": _fC_NodeCfg("Label", {
						"autowrap_mode": 3,
						"text": "", # выставляется в _fC_OpenModal_Ch: "SUPPORT_TITLE"/"SETTINGS_TITLE"
						"horizontal_alignment": 0, # LEFT
						"size_flags_horizontal": 3, # EXPAND — см. changelog выше
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Large"),
					}),
					"ModalCloseButton": _fC_NodeCfg("Button", {
						"text": "✕",
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
					}, {
						"pressed": Callable(self, "_fC_CloseModal_Ch"),
					}),
				}),

				"ModalBodyHBox": _fC_NodeCfg("HBoxContainer", {
					"size_flags_vertical": 3,
					"theme_override_constants/separation": 16,
				}, {}, vLD_BodyChildren_D),
			}),
		}),
	}


#Функционал:
#	Строит переиспользуемую "рамку" МАЛЕНЬКОГО всплывающего окна-уведомления
#	(2026-08-19, "окно применить/отмена") поверх УЖЕ открытого окна Настроек.
#	В отличие от _fC_BuildModalChrome_Cr — без заголовка/крестика, эти окна
#	закрываются только своими собственными кнопками (OK / Применить / Отмена)
#	⚠️ 2026-08-19: ПЕРЕДЕЛАНО по обратной связи "не должно закрывать окно
#	настроек, а должно появляться поверх со своим размытием" — раньше сам
#	Overlay был затемнённым/размытым НА ВЕСЬ ЭКРАН (тем же материалом, что и
#	большой ModalOverlay), из-за чего визуально ПОЛНОСТЬЮ перекрывал/прятал
#	карточку настроек позади себя — выглядело так, будто настройки закрылись
#	и открылось совсем другое окно, а не всплыло поверх. Теперь три слоя:
#	1) Overlay — лёгкое затемнение БЕЗ размытия (color alpha=0.25, без
#	   material), нужен и чтобы ловить клики мимо попапа (mouse_filter=STOP,
#	   не пускает их в поля настроек позади), и чтобы у окна настроек не
#	   оставалось "дыр" — см. следующий абзац;
#	2) Backdrop — размытие+затемнение (тот же материал, что у большого
#	   ModalOverlay), но НЕ на весь экран, а только вокруг самой карточки
#	   (чуть больше её размера) — это и есть "своё размытие" из обратной
#	   связи, локальное, а не поверх всего экрана ещё раз;
#	3) Panel — сама непрозрачная карточка (как раньше)
#	⚠️ 2026-08-20: обратная связь "окна которые должны предупреждать об
#	изменениях делает окно настроек прозрачным по высоте окна" — Backdrop
#	накрывает только среднюю полосу по вертикали (anchor_top/bottom
#	0.28..0.72 = 44% высоты экрана), а Overlay БЫЛ полностью прозрачным
#	(alpha=0) на всех остальных 56% (сверху и снизу от карточки) — окно
#	настроек в этих полосах оставалось совсем НЕ затемнено, "дырой" по всей
#	ширине, что и читалось как "прозрачное по высоте". Раньше (alpha=0) это
#	было сознательным решением — не обесцвечивать/не закрывать окно настроек,
#	но оно перестаралось: не давало ВООБЩЕ никакого визуального намёка, что
#	окно временно неактивно. Теперь Overlay красит ВЕСЬ экран лёгким
#	затемнением (0.25, заметно слабее, чем 0.55 у Backdrop/ModalOverlay) БЕЗ
#	размытия — этого достаточно, чтобы не было "дыр" и полос без затемнения,
#	но недостаточно, чтобы снова выглядеть как "окно настроек закрылось"
#	(контент под 0.25 всё ещё хорошо читается, в отличие от старого дизайна)
#Форматы данных:
#	Входные:
#		vLS_Prefix_S: String — уникальный префикс имён нод этого окна (два вызова
#			с разными префиксами — два независимых окна, "ScreenInfoPopup" и
#			"ScreenApplyConfirm", см. _fC_BuildScreenConfig_Cr)
#		vLD_PanelChildren_D: Dictionary — содержимое карточки (формат _fC_NodeCfg)
#	Выходные:
#		Dictionary — {"<Префикс>Overlay": конфиг_ноды} — один верхнеуровневый
#			ключ, готовый для vLD_Screen_D.merge(...)
func _fC_BuildConfirmOverlay_Cr(vLS_Prefix_S: String, vLD_PanelChildren_D: Dictionary) -> Dictionary:
	return {
		("%sOverlay" % vLS_Prefix_S): _fC_NodeCfg("ColorRect", {
			"anchor_left": 0.0, "anchor_top": 0.0, "anchor_right": 1.0, "anchor_bottom": 1.0,
			"offset_left": 0, "offset_top": 0, "offset_right": 0, "offset_bottom": 0,
			"color": Color(0, 0, 0, 0.25), # лёгкое затемнение (без размытия) — устраняет "дыры" сверху/снизу от Backdrop, см. changelog выше
			"mouse_filter": 0, # STOP — не даёт клику дойти до Settings-полей позади
			"visible": false,
		}, {}, {
			("%sBackdrop" % vLS_Prefix_S): _fC_NodeCfg("ColorRect", {
				"anchor_left": 0.10, "anchor_top": 0.28, "anchor_right": 0.90, "anchor_bottom": 0.72,
				"offset_left": 0, "offset_top": 0, "offset_right": 0, "offset_bottom": 0,
				"color": Color(0, 0, 0, 0.45), # плоское затемнение, БЕЗ material/размытия — см. changelog выше про отказ от SCREEN_TEXTURE здесь
				"mouse_filter": 2, # IGNORE — клики уже перехвачены родительским Overlay целиком
			}),
			("%sPanel" % vLS_Prefix_S): _fC_NodeCfg("Panel", {
				"anchor_left": 0.15, "anchor_top": 0.32, "anchor_right": 0.85, "anchor_bottom": 0.68,
				"offset_left": 0, "offset_top": 0, "offset_right": 0, "offset_bottom": 0,
				"theme_override_styles/panel": _fC_ModalCardStyle_Cr(),
			}, {}, vLD_PanelChildren_D),
		}),
	}


#Функционал:
#	Строит полный конфиг экрана (5 основных блоков + 2 всплывающих окна про
#	размер экрана, см. changelog 2026-08-19 "окно применить/отмена") под
#	указанную ориентацию —
#	CheckList/CompanyIcon различаются по расположению, InfoBox/ModalOverlay/
#	TopButtons одинаковы в обеих раскладках (см. постановку задачи). Весь экран
#	собран ЗДЕСЬ, одним вложенным словарём-литералом (см. changelog 2026-08-19),
#	по образцу vXD_BottomPanel_D/vXD_TopPanel_D в home.gd — раньше каждый блок
#	собирался в отдельной _fC_BuildXNode_Cr-функции, теперь всё видно сразу
#	целиком, без прыжков по функциям. Единственное, что честно приходится
#	собирать циклом ДО литерала — строки чек-листа (vLD_CheckRows_D): их состав
#	берётся из данных (vLA_CheckOrder_A), а не фиксирован, дублировать вручную
#	5 одинаковых по форме строк было бы не проще для понимания, а сложнее
func _fC_BuildScreenConfig_Cr(vLS_TargetOrientation_S: String) -> Dictionary:
	var vLD_CheckListAnchors_D: Dictionary
	var vLD_IconAnchors_D: Dictionary

	# 2026-08-19: ⚠️ ВСЕ anchor-блоки ниже переведены с anchor+offset(px) на ЧИСТЫЙ
	# anchor в долях экрана (offset_* везде 0) — обратная связь: "изменим логику
	# интерфейса с координат на %, чтобы убедиться в работе или отсутствии работы
	# параметров, изменяющих размер деталей". Раньше позиция/размер задавались как
	# "anchor по центру + отступ в пикселях ДИЗАЙН-холста" (540x900,
	# vSV2_Scene_size_V2i) — из-за window/stretch/mode="canvas_items" в
	# project.godot это и так масштабировалось вместе со всем холстом (тот же
	# эффект, что и % при равном соотношении сторон), но при "нестандартном"
	# соотношении сторон окна (canvas_items растягивает по ширине через
	# aspect="keep_width", высота может не совпасть 1-в-1) чистый anchor ведёт
	# себя иначе — независимо по каждой оси. Формула пересчёта пикселей старого
	# смещения в anchor: anchor_ось = старый_anchor_центр ± offset_px / размер_холста_по_оси
	# (540 по ширине, 900 по высоте — те же vSV2_Scene_size_V2i). Внутренние
	# отступы/padding (theme_override_constants/separation, offset_* внутри
	# _fC_BuildModalChrome_Cr) НЕ переведены — это не позиция на экране, а
	# фиксированный зазор между соседними элементами, который при переводе в %
	# от размера уже динамической панели выглядел бы абсурдно (то исчезающе
	# малым, то огромным на разных размерах окна) — если это тоже нужно
	# перевести, дай знать отдельно, это осознанное исключение из "ВСЕХ координат"
	if vLS_TargetOrientation_S == "Horizontal":
		# Чек-лист — на средней линии, слева (0.03..0.42 по ширине — уже было %)
		vLD_CheckListAnchors_D = {
			"anchor_left": 0.03, "anchor_right": 0.42,
			"anchor_top": 0.2556, "anchor_bottom": 0.7444, # было 0.5 ± 220/900
			"offset_left": 0, "offset_right": 0,
			"offset_top": 0, "offset_bottom": 0,
		}
		# Иконка компании — на средней линии, справа
		vLD_IconAnchors_D = {
			"anchor_left": 0.7315, "anchor_right": 0.9685, # было 0.85 ± 64/540
			"anchor_top": 0.4289, "anchor_bottom": 0.5711, # было 0.5 ± 64/900
			"offset_left": 0, "offset_right": 0,
			"offset_top": 0, "offset_bottom": 0,
		}
	else: # "Vertical" — по умолчанию, совпадает с реальным окном проекта
		# Чек-лист — на средней линии, по центру (0.1..0.9 по ширине — уже было %)
		vLD_CheckListAnchors_D = {
			"anchor_left": 0.1, "anchor_right": 0.9,
			"anchor_top": 0.2556, "anchor_bottom": 0.7444, # было 0.5 ± 220/900
			"offset_left": 0, "offset_right": 0,
			"offset_top": 0, "offset_bottom": 0,
		}
		# Иконка компании — сверху по центру
		vLD_IconAnchors_D = {
			"anchor_left": 0.3815, "anchor_right": 0.6185, # было 0.5 ± 64/540
			"anchor_top": 0.0489, "anchor_bottom": 0.1911, # было 0.12 ± 64/900
			"offset_left": 0, "offset_right": 0,
			"offset_top": 0, "offset_bottom": 0,
		}

	# Кнопки поддержки/настроек и окно информации — одинаково в обеих ориентациях
	var vLD_TopButtonsAnchors_D := {
		"anchor_left": 0.5926, "anchor_right": 0.9704, # было 1.0 - 220/540 .. 1.0 - 16/540
		"anchor_top": 0.0178, "anchor_bottom": 0.0711, # было 16/900 .. 64/900
		"offset_left": 0, "offset_right": 0,
		"offset_top": 0, "offset_bottom": 0,
	}
	var vLD_InfoBoxAnchors_D := {
		"anchor_left": 0.08, "anchor_right": 0.92,
		"anchor_top": 0.8, "anchor_bottom": 0.9822, # было 1.0 - 180/900 .. 1.0 - 16/900
		"offset_left": 0, "offset_right": 0,
		"offset_top": 0, "offset_bottom": 0,
	}

	# Строки чек-листа — единственная часть экрана, реально управляемая данными
	# (vLA_CheckOrder_A/vLD_CheckLabels_D/vLD_CheckStatuses_D), а не фиксированной
	# структурой; собираем их заранее, чтобы просто подставить готовый словарь ниже
	var vLD_CheckRows_D := {}
	for elLS_CheckKey in vLA_CheckOrder_A:
		var vLS_InitialStatus: String = vLD_CheckStatuses_D[elLS_CheckKey]
		vLD_CheckRows_D["Row_%s" % elLS_CheckKey] = _fC_NodeCfg("HBoxContainer", {
			"theme_override_constants/separation": 12,
		}, {}, {
			"Row_%s_NameLabel" % elLS_CheckKey: _fC_NodeCfg("Label", {
				"autowrap_mode": 3,
				"text": vLD_CheckLabels_D[elLS_CheckKey],
				"size_flags_horizontal": 3,
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Title"),
			}),
			"Row_%s_StatusLabel" % elLS_CheckKey: _fC_NodeCfg("Label", {
				"autowrap_mode": 3,
				"text": vGD_StatusSymbols_D[vLS_InitialStatus],
				"horizontal_alignment": 1,
				"custom_minimum_size": Vector2(36, 0),
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Title"),
				"theme_override_colors/font_color": vGD_StatusColors_D[vLS_InitialStatus],
			}),
		})

	# 2026-08-23 (правка): окно выбора аккаунта переехало ИЗ полноэкранного
	# модального оверлея (затемнение+карточка по центру) ВНУТРЬ InfoBoxPanel —
	# того самого "отведённого окошка под проверками", которое уже существует
	# для статусных сообщений чек-листа (см. InfoBoxPanel/InfoBoxVBox ниже,
	# обратная связь: "окно должно спрашивать под проверками в отведённом
	# окошке"). Кнопки аккаунтов собраны здесь же, до литерала vLD_Screen_D,
	# по тому же принципу, что и vLD_CheckRows_D выше — состав данные, а не
	# фиксированная структура. Список аккаунтов из vLD_AccountsList_D (файл),
	# в конце ВСЕГДА добавлена кнопка "+" (см. _fC_OnAccountAddButtonPressed_Ch)
	var vLD_AccountButtons_D := {}
	for elLS_Account in vLD_AccountsList_D.keys() + [ACCOUNT_TECHNICAL_SERVER_VALUE]:
		vLD_AccountButtons_D["AccountPickerButton_%s" % elLS_Account] = _fC_NodeCfg("Button", {
			"text": elLS_Account,
			"size_flags_horizontal": 3,
			"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
		}, {
			"pressed": Callable(self, "_fC_OnAccountOptionPressed_Ch").bind(elLS_Account),
		})
	vLD_AccountButtons_D["AccountPickerAddButton"] = _fC_NodeCfg("Button", {
		"text": "+",
		"size_flags_horizontal": 3,
		"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
	}, {
		"pressed": Callable(self, "_fC_OnAccountAddButtonPressed_Ch"),
	})

	# Порядок ключей верхнего уровня важен: он же — порядок создания нод, а
	# значит и z-order (последние ключи рисуются поверх предыдущих, и именно они
	# первыми получают клики в этой же области). TopButtonsHBox идёт ДО
	# ModalOverlay специально — оверлей (z_index=0, как и у всех) рисуется
	# поверх ОБЕИХ кнопок и перекрывает клики к ним обеим, как и к остальному
	# экрану, ПОКА окно открыто. ⚠️ 2026-08-19, ещё правка ("не размывается
	# кнопка настроек"): раньше кнопка-источник точечно поднималась над
	# оверлеем через z_index (_fC_SetActiveTopButtonRaised_Ch), из-за чего
	# именно она не затемнялась/не размывалась вместе с остальным экраном —
	# функция удалена, обе кнопки теперь ВСЕГДА одинаково уходят под
	# оверлей, пока он открыт (закрытие — крестик/клик по фону, см.
	# _fC_CloseModal_Ch/_fC_OnOverlayGuiInput_Ch)
	# Каждый ключ верхнего уровня — настоящая нода (без промежуточных
	# групп-обёрток, см. changelog про вложенный формат), дети вложены прямо
	# внутрь через _fC_NodeCfg
	var vLD_Screen_D := {
		"CheckListPanel": _fC_NodeCfg("PanelContainer", vLD_CheckListAnchors_D, {}, {
			"CheckListVBox": _fC_NodeCfg("VBoxContainer", {
				"alignment": 1,
				"theme_override_constants/separation": 10,
			}, {}, vLD_CheckRows_D),
		}),

		"CompanyIconRect": _fC_NodeCfg("TextureRect", _fC_MergeInspector_Cr({
			"texture": load("res://Textures/icon.svg"),
			"expand_mode": 1, # EXPAND_IGNORE_SIZE — размер берётся из anchor/offset, не из текстуры
			"stretch_mode": 5, # STRETCH_KEEP_ASPECT_CENTERED
		}, vLD_IconAnchors_D)),

		"InfoBoxPanel": _fC_NodeCfg("PanelContainer", vLD_InfoBoxAnchors_D, {}, {
			"InfoBoxVBox": _fC_NodeCfg("VBoxContainer", {
				"alignment": 1,
				"theme_override_constants/separation": 8,
			}, {}, {
				"InfoBoxLabel": _fC_NodeCfg("Label", {
					"autowrap_mode": 3,
					"text": "",
					"horizontal_alignment": 1,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
				}),
				# 2026-08-24 (обратная связь "когда подключаемся к серверу, в
				# окошке снизу должен появляться таймер, чтобы человек понимал,
				# что подключение работает"): видна ТОЛЬКО пока идёт попытка
				# подключения — от _fC_StartClientConnection_Ch (первая попытка
				# ИЛИ повтор через InfoBoxRetryButton) до момента, когда попытка
				# разрешилась (успехом — _fC_OnServerConnected_Ch — или
				# истечением SERVER_CONNECT_TIMEOUT_SECONDS_F —
				# _fC_OfferAutonomousMode_Ch, дальше место занимают кнопки).
				# Текст обновляется каждый кадр в _process (см. ниже)
				"InfoBoxConnectingLabel": _fC_NodeCfg("Label", {
					"autowrap_mode": 3,
					"text": "",
					"visible": false,
					"horizontal_alignment": 1,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}),
				# 2026-08-24: текст кнопки "Властвовать" → "Войти" (по просьбе
				# пользователя) — сама кнопка и её поведение не изменились,
				# только подпись (см. _fC_OnProceedPressed_Ch ниже)
				"InfoBoxProceedButton": _fC_NodeCfg("Button", {
					"text": "Войти",
					"visible": false, # появляется только когда все проверки "passed" — см. _fC_UpdateOverallProgress_Ch
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Large"),
				}, {
					"pressed": Callable(self, "_fC_OnProceedPressed_Ch"),
				}),
				# 2026-08-23: кнопка "продолжить без сервера" — появляется при провале
				# ЛЮБОЙ из трёх проверок Server/Update/ThirdPartyFiles (см.
				# _fC_OfferAutonomousMode_Ch/_fC_ShowAutonomousModeButton_Ch) —
				# жмёт непройденные из этих трёх "failed" (крестик, честно — не
				# проверено) вместо бесконечного зависания чек-листа. 2026-08-24
				# (обратная связь "если провалилась ПЕРВАЯ из трёх — предлагаем ЕЩЁ
				# повторить подключение"): для остальных двух (Update/ThirdPartyFiles)
				# повтор подключения ничего бы не исправил (дело не в сети), поэтому
				# InfoBoxRetryButton показывается ТОЛЬКО вместе с этой кнопкой при
				# провале именно "Server" — см. InfoBoxRetryButton ниже
				"InfoBoxAutonomousButton": _fC_NodeCfg("Button", {
					"text": "Продолжить без сервера",
					"visible": false,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}, {
					"pressed": Callable(self, "_fC_OnAutonomousModePressed_Ch"),
				}),
				# 2026-08-24: кнопка "Повторить подключение" — появляется ТОЛЬКО когда
				# не удалось подключение К СЕРВЕРУ (первая из трёх проверок), рядом с
				# InfoBoxAutonomousButton (см. её changelog выше и _fC_OfferAutonomousMode_Ch/
				# _fC_OnRetryConnectionPressed_Ch) — для Update/ThirdPartyFiles эта
				# кнопка не показывается, повтор подключения их не касается
				"InfoBoxRetryButton": _fC_NodeCfg("Button", {
					"text": "Повторить подключение",
					"visible": false,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}, {
					"pressed": Callable(self, "_fC_OnRetryConnectionPressed_Ch"),
				}),
				# 2026-08-23 (правка): окно выбора аккаунта теперь здесь, внутри
				# InfoBoxVBox — тот же "отведённый под проверками" блок, что и у
				# InfoBoxLabel/InfoBoxProceedButton выше, а не отдельный
				# полноэкранный оверлей (обратная связь: "окно должно спрашивать
				# под проверками в отведённом окошке"). "visible": false — скрыт
				# по умолчанию, показывается ТОЛЬКО пока идёт выбор аккаунта
				# (_fC_ShowAccountPicker_Ch), одновременно с этим скрывается
				# InfoBoxLabel, чтобы не накладывались друг на друга в этом
				# небольшом по высоте блоке. ⚠️ В ОТЛИЧИЕ от других "visible:
				# false"-нод в этом файле (у которых пересборка конфига обычно не
				# трогает runtime-видимость) — здесь как раз ТРОГАЕТ: список
				# кнопок внутри лежит ГЛУБЖЕ, чем эта нода, поэтому изменение
				# списка (добавление аккаунта) считается изменением всего этого
				# поддерева и пересоздаёт AccountPickerSection заново из
				# статического "visible": false — см. подробный разбор в
				# changelog _fC_OnAccountAddSavePressed_Ch, там же и исправление
				"AccountPickerSection": _fC_NodeCfg("VBoxContainer", {
					"size_flags_vertical": 3,
					"theme_override_constants/separation": 6,
					"visible": false,
				}, {}, {
					"AccountPickerLabel": _fC_NodeCfg("Label", {
						"autowrap_mode": 3,
						"text": "ACCOUNT_PICKER_TITLE",
						"horizontal_alignment": 1,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
					}),
					# 2026-08-23: отдельная строка под ошибки валидации формы
					# добавления/подтверждение "аккаунт добавлен" — НЕ через общий
					# _fC_ShowInfo_Ch/InfoBoxLabel, тот сейчас скрыт (см.
					# _fC_ShowAccountPicker_Ch), сообщение туда никто бы не увидел.
					# См. _fC_ShowAccountPickerMessage_Ch
					"AccountPickerMessageLabel": _fC_NodeCfg("Label", {
						"autowrap_mode": 3,
						"text": "",
						"horizontal_alignment": 1,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
					}),
					# 2026-08-24 (по просьбе пользователя "предусмотреть вход через
					# сторонние аккаунты — ВК, Google, Яндекс и т.п."): заглушки —
					# архитектура спроектирована (см. План проекта/OAuth сторонние
					# аккаунты.md), но живой OAuth-поток НЕ подключён, для него
					# нужны client_id/client_secret, которые может получить только
					# сам владелец проекта у каждого провайдера (см.
					# _fC_OnOAuthButtonPressed_Ch — честно объясняет это вместо
					# того, чтобы делать вид, что вход работает)
					"AccountPickerOAuthHBox": _fC_NodeCfg("HBoxContainer", {
						"alignment": 1,
						"theme_override_constants/separation": 8,
					}, {}, {
						"AccountPickerOAuthVkButton": _fC_NodeCfg("Button", {
							"text": "VK",
							"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
						}, {
							"pressed": Callable(self, "_fC_OnOAuthButtonPressed_Ch").bind("VK"),
						}),
						"AccountPickerOAuthGoogleButton": _fC_NodeCfg("Button", {
							"text": "Google",
							"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
						}, {
							"pressed": Callable(self, "_fC_OnOAuthButtonPressed_Ch").bind("Google"),
						}),
						"AccountPickerOAuthYandexButton": _fC_NodeCfg("Button", {
							"text": "Яндекс",
							"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
						}, {
							"pressed": Callable(self, "_fC_OnOAuthButtonPressed_Ch").bind("Yandex"),
						}),
					}),
					# ScrollContainer — "аккаунтов может быть много": список кнопок
					# растёт по числу записей в vLD_AccountsList_D, а место под
					# InfoBoxPanel фиксировано (см. vLD_InfoBoxAnchors_D выше) —
					# прокрутка вместо обрезания/вылезания за границы панели
					"AccountPickerScroll": _fC_NodeCfg("ScrollContainer", {
						"size_flags_vertical": 3,
						"horizontal_scroll_mode": 0, # SCROLL_MODE_DISABLED — только вертикальная прокрутка
					}, {}, {
						"AccountPickerButtonsVBox": _fC_NodeCfg("VBoxContainer", {
							"size_flags_horizontal": 3,
							"theme_override_constants/separation": 4,
						}, {}, vLD_AccountButtons_D),
					}),
					# Форма добавления аккаунта — скрыта по умолчанию, показывается
					# ВМЕСТО AccountPickerScroll по нажатию "+" (см.
					# _fC_OnAccountAddButtonPressed_Ch/_fC_OnAccountAddSavePressed_Ch/
					# _fC_OnAccountAddCancelPressed_Ch)
					"AccountPickerAddForm": _fC_NodeCfg("VBoxContainer", {
						"size_flags_vertical": 3,
						"theme_override_constants/separation": 4,
						"visible": false,
					}, {}, {
						"AccountPickerAddNickLineEdit": _fC_NodeCfg("LineEdit", {
							"placeholder_text": "Ник",
							"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
						}),
						"AccountPickerAddIdLineEdit": _fC_NodeCfg("LineEdit", {
							"placeholder_text": "ID",
							"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
						}),
						"AccountPickerAddPasswordLineEdit": _fC_NodeCfg("LineEdit", {
							"placeholder_text": "Пароль",
							"secret": true,
							"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
						}),
						"AccountPickerAddButtonsHBox": _fC_NodeCfg("HBoxContainer", {
							"alignment": 2,
							"theme_override_constants/separation": 8,
						}, {}, {
							"AccountPickerAddCancelButton": _fC_NodeCfg("Button", {
								"text": "CANCEL",
								"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
							}, {
								"pressed": Callable(self, "_fC_OnAccountAddCancelPressed_Ch"),
							}),
							"AccountPickerAddSaveButton": _fC_NodeCfg("Button", {
								"text": "APPLY",
								"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
							}, {
								"pressed": Callable(self, "_fC_OnAccountAddSavePressed_Ch"),
							}),
							# 2026-08-24 (по просьбе пользователя "добавим... регистрацию
							# для входа"): в отличие от APPLY (только локальная запись,
							# сервер узнаёт про этот логин/пароль только при первом
							# реальном входе — и то, если сервер вообще требует
							# авторизацию), эта кнопка сразу создаёт учётную запись НА
							# СЕРВЕРЕ (см. _fC_OnAccountAddRegisterPressed_Ch/
							# Class_InternetServer._fL_OnRegisterRequest_Ch)
							"AccountPickerAddRegisterButton": _fC_NodeCfg("Button", {
								"text": "Зарегистрироваться",
								"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
							}, {
								"pressed": Callable(self, "_fC_OnAccountAddRegisterPressed_Ch"),
							}),
						}),
					}),
				}),
			}),
		}),

		"TopButtonsHBox": _fC_NodeCfg("HBoxContainer", _fC_MergeInspector_Cr({
			"alignment": 2,
			"theme_override_constants/separation": 8,
		}, vLD_TopButtonsAnchors_D), {}, {
			"SupportButton": _fC_NodeCfg("Button", {
				"text": "?",
				"tooltip_text": "Поддержка",
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
			}, {
				"pressed": Callable(self, "_fC_ToggleModal_Ch").bind("Support"),
			}),
			"SettingsButton": _fC_NodeCfg("Button", {
				"text": "⚙",
				"tooltip_text": "Настройки",
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
			}, {
				"pressed": Callable(self, "_fC_ToggleModal_Ch").bind("Settings"),
			}),
		}),

		# Окно поддержки/настроек — общий оверлей + карточка (см. changelog
		# 2026-08-19 про полную переработку по 8-пунктовому ТЗ). ModalOverlay
		# сам ловит клик по прозрачной части через gui_input и закрывает окно
		# (_fC_OnOverlayGuiInput_Ch) — клик по самой Panel до него не доходит.
		# Внутри Panel — вертикально: заголовок (крестик слева, название
		# справа) и тело (контент слева/по центру + категории настроек справа,
		# см. _fC_UpdateModalContentVisibility_Ch/_fC_OnSettingsCategoryToggled_Ch)
		"ModalOverlay": _fC_NodeCfg("ColorRect", {
			"anchor_left": 0.0, "anchor_top": 0.0,
			"anchor_right": 1.0, "anchor_bottom": 1.0,
			"offset_left": 0, "offset_top": 0, "offset_right": 0, "offset_bottom": 0,
			"color": Color(0, 0, 0, 0.55), # запасной вариант — используется, только если material=null (шейдер не загрузился)
			"mouse_filter": 0, # STOP — ловит клики, не пропускает их на то, что ниже
			"visible": false,
			"material": _fC_ModalOverlayBlurMaterial_Cr(), # 2026-08-19: размытие заднего фона, см. её changelog; null = тихая деградация до плоского затемнения выше
		}, {
			"gui_input": Callable(self, "_fC_OnOverlayGuiInput_Ch"),
		}, _fC_BuildModalChrome_Cr({
			# Тело окна — теперь СЛЕВА категории, СПРАВА контент (2026-08-19,
			# п.1 ТЗ: "поменяй местами право и лево" — было наоборот)
			# 2026-08-20: ⚠️ обе колонки завёрнуты в ScrollContainer (п.2 ТЗ —
			# "сделай возможность прокрутки вниз через колёсико мыши категорий
			# слева и основного экрана настроек") — категорий стало 5
			# (добавилась "Шрифт", см. ниже), а у "Экран"/"Подтверждения" контент
			# и так уже мог не влезать по высоте на маленьком окне. ScrollContainer
			# в Godot сам обрабатывает прокрутку колёсиком без доп. кода — важно
			# только НЕ давать растягиваемому по вертикали size_flags_vertical=3
			# ребёнку внутри (иначе Godot сжимает его под видимую высоту и
			# скроллить нечего, содержимое просто обрежется) — поэтому у
			# SettingsCategoriesVBox/ModalContentArea вертикальный размер теперь
			# "естественный" (под контент), а не force-fit
			"SettingsCategoriesScroll": _fC_NodeCfg("ScrollContainer", {
				"custom_minimum_size": Vector2(130, 0),
				"size_flags_horizontal": 0, # SHRINK_BEGIN — не растягивается на весь HBox
				"size_flags_vertical": 3,
				"horizontal_scroll_mode": 0, # DISABLED — список категорий только вертикальный
				"vertical_scroll_mode": 1, # AUTO — полоса прокрутки только когда реально не влезает
				"visible": false, # только для источника "Settings" — переключается в _fC_OpenModal_Ch
			}, {}, {
				"SettingsCategoriesVBox": _fC_NodeCfg("VBoxContainer", {
					"alignment": 1,
					"theme_override_constants/separation": 8,
				}, {}, {
					"CategoryLanguageButton": _fC_NodeCfg("Button", {
						"text": "CATEGORY_LANGUAGE",
						"toggle_mode": true,
						"button_pressed": true, # категория по умолчанию — см. vLS_ActiveSettingsCategory_S
						"button_group": _vGC_CategoryButtonGroup,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
					}, {
						"toggled": Callable(self, "_fC_OnSettingsCategoryToggled_Ch").bind("Language"),
					}),
					"CategoryScreenButton": _fC_NodeCfg("Button", {
						"text": "CATEGORY_SCREEN",
						"toggle_mode": true,
						"button_group": _vGC_CategoryButtonGroup,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
					}, {
						"toggled": Callable(self, "_fC_OnSettingsCategoryToggled_Ch").bind("Screen"),
					}),
					# 2026-08-20: ⚠️ "Шрифт" ОБРАТНО ВНУТРИ категории "Экран" — см.
					# changelog у ScreenFontScaleGroupVBox ниже про откат отдельной
					# категории "Шрифт" (было ненадолго отдельной кнопкой-категорией,
					# уточнение обратной связи: "именно вернуть в категорию экран")
					"CategorySoundButton": _fC_NodeCfg("Button", {
						"text": "CATEGORY_SOUND",
						"toggle_mode": true,
						"button_group": _vGC_CategoryButtonGroup,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
					}, {
						"toggled": Callable(self, "_fC_OnSettingsCategoryToggled_Ch").bind("Sound"),
					}),
					# 2026-08-19: 4-я категория (по счёту создания узлов, не по месту в
					# списке) — список окон-уведомлений (пока только про размер экрана)
					# с переключателем "показывать/не показывать" на каждое (см.
					# changelog про "окно применить/отмена")
					"CategoryConfirmationsButton": _fC_NodeCfg("Button", {
						"text": "CATEGORY_CONFIRMATIONS",
						"toggle_mode": true,
						"button_group": _vGC_CategoryButtonGroup,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
					}, {
						"toggled": Callable(self, "_fC_OnSettingsCategoryToggled_Ch").bind("Confirmations"),
					}),
					# 2026-08-22: новая категория "Логи и реплеи" — обратная связь:
					# "добавляем настройки Логера, а именно Логи Программы и Реплеи
					# как два раздела в одной категории". Последняя в списке — как и
					# у "Подтверждений", отдельного пожелания по месту не было
					"CategoryLoggerButton": _fC_NodeCfg("Button", {
						"text": "CATEGORY_LOGGER",
						"toggle_mode": true,
						"button_group": _vGC_CategoryButtonGroup,
						"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
					}, {
						"toggled": Callable(self, "_fC_OnSettingsCategoryToggled_Ch").bind("Logger"),
					}),
				}),
			}),

			"ModalContentScroll": _fC_NodeCfg("ScrollContainer", {
				"size_flags_horizontal": 3,
				"size_flags_vertical": 3,
				"horizontal_scroll_mode": 0, # DISABLED — контент категорий только вертикальный
				"vertical_scroll_mode": 1, # AUTO
			}, {}, {
			"ModalContentArea": _fC_NodeCfg("VBoxContainer", {
				"size_flags_horizontal": 3,
				"theme_override_constants/separation": 10,
			}, {}, {
							"SupportContentVBox": _fC_NodeCfg("VBoxContainer", {
								"visible": false, # переключается в _fC_UpdateModalContentVisibility_Ch
								"theme_override_constants/separation": 8,
							}, {}, {
								"SupportBodyLabel": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "SUPPORT_BODY",
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
								}),
							}),

							"LanguageContentVBox": _fC_NodeCfg("VBoxContainer", {
								"visible": false,
								"theme_override_constants/separation": 8,
							}, {}, {
								"LanguageSearchLineEdit": _fC_NodeCfg("LineEdit", {
									"placeholder_text": "SEARCH_PLACEHOLDER",
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
								}, {
									"text_changed": Callable(self, "_fC_OnLanguageSearchChanged_Ch"),
								}),
								# Пустой контейнер — список языков строится ДИНАМИЧЕСКИ в
								# _fC_RebuildLanguageList_Ch (не через конфиг, см. changelog)
								"LanguageListVBox": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 4,
								}),
							}),

							# 2026-08-19: доработка по обратной связи —
							# 1) перед полями свободного ввода теперь ПОСТОЯННЫЕ подписи
							#    "Ширина"/"Высота" (не только placeholder, который пропадает
							#    при вводе) — ScreenWidthLineEdit/HeightLineEdit завёрнуты в
							#    свои VBox с Label сверху;
							# 2) пресеты переименованы с "разрешений" (1280x720 и т.п.) на
							#    короткие "1K"/"2K"/"3K" + добавлен ВТОРОЙ ряд таких же
							#    пресетов, но для вертикального экрана (720x1280 и т.п.) —
							#    у обоих рядов своя подпись (Горизонтальный/Вертикальный),
							#    иначе одинаковые "1K/2K/3K" в двух рядах было бы не понять;
							# 3) ScreenFullscreenButton переехал в НАЧАЛО блока пресетов
							#    (был отдельной кнопкой в самом низу) и БОЛЬШЕ НЕ применяется
							#    сразу — теперь идёт через тот же отложенный
							#    "применить/отмена" механизм, что и размер (см. changelog
							#    "полноэкранный режим не работал через подтверждения")
							"ScreenContentVBox": _fC_NodeCfg("VBoxContainer", {
								"visible": false,
								"theme_override_constants/separation": 10,
							}, {}, {
								# 2026-08-22: ⚠️ категория "Экран" разбита на 2 визуальных раздела —
								# тот же паттерн заголовок+линия-разделитель, что и в "Подтверждениях"
								# (см. её changelog про ConfirmWindowsSeparator/CONFIRM_SECTION_*):
								# "Размер экрана" (раздел 1 — самый первый, без линии сверху) и
								# "Шрифт" (раздел 2, ниже пресетов — линия НАД заголовком). Обратная
								# связь: "раздели в категории экран шрифт и экран по месту в окне,
								# как мы разделили окна подтверждения/информирования и автоматическое
								# нажатие" — раньше (см. запись чуть выше про откат категории "Шрифт")
								# ScreenFontScaleGroupVBox просто лежал сразу после ширины/высоты,
								# без какого-либо заголовка/разделения; теперь он физически перенесён
								# в САМЫЙ КОНЕЦ категории (после всех пресетов), под собственным
								# заголовком раздела — то есть Font и "остальной Экран" по-прежнему
								# ОДНА категория (без отдельной кнопки слева), но чётко разделены
								# ВНУТРИ окна, как и просили
								"ScreenSizeSectionTitle": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "SCREEN_SECTION_SIZE",
									"horizontal_alignment": 0, # LEFT
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
								}),
								"ScreenSizeHBox": _fC_NodeCfg("HBoxContainer", {
									"theme_override_constants/separation": 8,
								}, {}, {
									"ScreenWidthGroupVBox": _fC_NodeCfg("VBoxContainer", {
										"size_flags_horizontal": 3,
										"theme_override_constants/separation": 2,
									}, {}, {
										"ScreenWidthLabel": _fC_NodeCfg("Label", {
											"autowrap_mode": 3,
											"text": "SCREEN_WIDTH",
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}),
										"ScreenWidthLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "SCREEN_WIDTH",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											# И Enter (text_submitted), И потеря фокуса (focus_exited) —
											# п.2 ТЗ 2026-08-19: раньше применялось только по Enter,
											# что легко выглядело как "не работает", если просто кликнуть
											# в другое поле. focus_exited сигнала без аргументов — тот же
											# обработчик, просто с пустой bind-строкой вместо текста поля
											"text_submitted": Callable(self, "_fC_OnScreenSizeChanged_Ch"),
											"focus_exited": Callable(self, "_fC_OnScreenSizeChanged_Ch").bind(""),
										}),
									}),
									"ScreenHeightGroupVBox": _fC_NodeCfg("VBoxContainer", {
										"size_flags_horizontal": 3,
										"theme_override_constants/separation": 2,
									}, {}, {
										"ScreenHeightLabel": _fC_NodeCfg("Label", {
											"autowrap_mode": 3,
											"text": "SCREEN_HEIGHT",
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}),
										"ScreenHeightLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "SCREEN_HEIGHT",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnScreenSizeChanged_Ch"),
											"focus_exited": Callable(self, "_fC_OnScreenSizeChanged_Ch").bind(""),
										}),
									}),
								}),
								"ScreenFullscreenRow": _fC_NodeCfg("HBoxContainer", {
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenFullscreenButton": _fC_NodeCfg("Button", {
										"text": "SCREEN_FULLSCREEN",
										"toggle_mode": true,
										"size_flags_horizontal": 3,
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"toggled": Callable(self, "_fC_OnFullscreenToggled_Ch"),
									}),
								}),
								"ScreenPresetsHorizontalLabel": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "SCREEN_PRESETS_HORIZONTAL",
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}),
								"ScreenPresetGroup4_3Title": _fC_NodeCfg("Button", {
									"text": "4:3",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroup4_3HBox"),
								}),
								"ScreenPresetGroup4_3HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_VGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(640, 480),
									}),
									"ScreenPresetSVGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_SVGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(800, 600),
									}),
									"ScreenPresetXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_XGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1024, 768),
									}),
									"ScreenPresetUXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_UXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1600, 1200),
									}),
								}),
								"ScreenPresetGroup5_4Title": _fC_NodeCfg("Button", {
									"text": "5:4",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroup5_4HBox"),
								}),
								"ScreenPresetGroup5_4HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetSXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_SXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1280, 1024),
									}),
								}),
								"ScreenPresetGroup16_10Title": _fC_NodeCfg("Button", {
									"text": "16:10",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroup16_10HBox"),
								}),
								"ScreenPresetGroup16_10HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetWXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_WXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1280, 800),
									}),
									"ScreenPresetWUXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_WUXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1920, 1200),
									}),
								}),
								"ScreenPresetGroup16_9Title": _fC_NodeCfg("Button", {
									"text": "16:9",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroup16_9HBox"),
								}),
								"ScreenPresetGroup16_9HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetHD720Button": _fC_NodeCfg("Button", {
										"text": "PRESET_HD720",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1280, 720),
									}),
									"ScreenPresetFHD1080Button": _fC_NodeCfg("Button", {
										"text": "PRESET_FHD1080",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1920, 1080),
									}),
									"ScreenPresetQHD1440Button": _fC_NodeCfg("Button", {
										"text": "PRESET_QHD1440",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(2560, 1440),
									}),
									"ScreenPresetUHD4K2160Button": _fC_NodeCfg("Button", {
										"text": "PRESET_4KUHD",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(3840, 2160),
									}),
									"ScreenPresetFiveKButton": _fC_NodeCfg("Button", {
										"text": "PRESET_5K",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(5120, 2880),
									}),
									"ScreenPresetUHD8K4320Button": _fC_NodeCfg("Button", {
										"text": "PRESET_8KUHD",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(7680, 4320),
									}),
								}),
								"ScreenPresetGroup21_9Title": _fC_NodeCfg("Button", {
									"text": "21:9",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroup21_9HBox"),
								}),
								"ScreenPresetGroup21_9HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetWQHDButton": _fC_NodeCfg("Button", {
										"text": "PRESET_WQHD",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(3440, 1440),
									}),
								}),
								"ScreenPresetGroup17_9Title": _fC_NodeCfg("Button", {
									"text": "~17:9",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroup17_9HBox"),
								}),
								"ScreenPresetGroup17_9HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetDCI4KButton": _fC_NodeCfg("Button", {
										"text": "PRESET_DCI4K",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(4096, 2160),
									}),
								}),
								"ScreenPresetsVerticalLabel": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "SCREEN_PRESETS_VERTICAL",
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}),
								"ScreenPresetGroupV4_3Title": _fC_NodeCfg("Button", {
									"text": "3:4",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroupV4_3HBox"),
								}),
								"ScreenPresetGroupV4_3HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVVGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_VGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(480, 640),
									}),
									"ScreenPresetVSVGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_SVGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(600, 800),
									}),
									"ScreenPresetVXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_XGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(768, 1024),
									}),
									"ScreenPresetVUXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_UXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1200, 1600),
									}),
								}),
								"ScreenPresetGroupV5_4Title": _fC_NodeCfg("Button", {
									"text": "4:5",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroupV5_4HBox"),
								}),
								"ScreenPresetGroupV5_4HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVSXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_SXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1024, 1280),
									}),
								}),
								"ScreenPresetGroupV16_10Title": _fC_NodeCfg("Button", {
									"text": "10:16",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroupV16_10HBox"),
								}),
								"ScreenPresetGroupV16_10HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVWXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_WXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(800, 1280),
									}),
									"ScreenPresetVWUXGAButton": _fC_NodeCfg("Button", {
										"text": "PRESET_WUXGA",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1200, 1920),
									}),
								}),
								"ScreenPresetGroupV16_9Title": _fC_NodeCfg("Button", {
									"text": "9:16",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroupV16_9HBox"),
								}),
								"ScreenPresetGroupV16_9HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVHD720Button": _fC_NodeCfg("Button", {
										"text": "PRESET_HD720",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(720, 1280),
									}),
									"ScreenPresetVFHD1080Button": _fC_NodeCfg("Button", {
										"text": "PRESET_FHD1080",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1080, 1920),
									}),
									"ScreenPresetVQHD1440Button": _fC_NodeCfg("Button", {
										"text": "PRESET_QHD1440",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1440, 2560),
									}),
									"ScreenPresetVUHD4K2160Button": _fC_NodeCfg("Button", {
										"text": "PRESET_4KUHD",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(2160, 3840),
									}),
									"ScreenPresetVFiveKButton": _fC_NodeCfg("Button", {
										"text": "PRESET_5K",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(2880, 5120),
									}),
									"ScreenPresetVUHD8K4320Button": _fC_NodeCfg("Button", {
										"text": "PRESET_8KUHD",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(4320, 7680),
									}),
								}),
								"ScreenPresetGroupV21_9Title": _fC_NodeCfg("Button", {
									"text": "9:21",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroupV21_9HBox"),
								}),
								"ScreenPresetGroupV21_9HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVWQHDButton": _fC_NodeCfg("Button", {
										"text": "PRESET_WQHD",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(1440, 3440),
									}),
								}),
								"ScreenPresetGroupV17_9Title": _fC_NodeCfg("Button", {
									"text": "~9:17",
									"toggle_mode": true,
									"button_pressed": false, # свёрнуто по умолчанию — см. changelog 2026-08-20
									"size_flags_horizontal": 3,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
								}, {
									"toggled": Callable(self, "_fC_OnPresetGroupToggled_Ch").bind("ScreenPresetGroupV17_9HBox"),
								}),
								"ScreenPresetGroupV17_9HBox": _fC_NodeCfg("HBoxContainer", {
									"visible": false, # свёрнуто по умолчанию, раскрывается кнопкой-заголовком выше
									"theme_override_constants/separation": 6,
								}, {}, {
									"ScreenPresetVDCI4KButton": _fC_NodeCfg("Button", {
										"text": "PRESET_DCI4K",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}, {
										"pressed": Callable(self, "_fC_OnScreenPresetPressed_Ch").bind(2160, 4096),
									}),
								}),
								# Раздел 2 — линия НАД заголовком (тот же паттерн, что и у
								# ConfirmWindowsSeparator в "Подтверждениях") — см. changelog в
								# начале ScreenContentVBox выше про разделение "Экран"/"Шрифт"
								"ScreenFontSeparator": _fC_NodeCfg("HSeparator", {}),
								"ScreenFontSectionTitle": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "SCREEN_SECTION_FONT",
									"horizontal_alignment": 0, # LEFT
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
								}),
								"ScreenFontScaleGroupVBox": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"ScreenFontScaleLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "SCREEN_FONT_SCALE",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
									}),
									"ScreenFontScaleLineEdit": _fC_NodeCfg("LineEdit", {
										"placeholder_text": "1.0",
										"size_flags_horizontal": 3,
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}, {
										"text_submitted": Callable(self, "_fC_OnFontScaleSubmitted_Ch"),
										"focus_exited": Callable(self, "_fC_OnFontScaleSubmitted_Ch").bind(""),
									}),
								}),
							}),

							# 2026-08-19: ⚠️ ПЕРЕСТРОЕНА обратная связь — вкладка "Подтверждения"
							# теперь сама двухуровневая: слева ВТОРОЙ столбик (ConfirmSourceVBox,
							# тот же паттерн, что и SettingsCategoriesVBox слева от всего окна)
							# переключает "Настройки" (то, что уже было) / "Обучение" (заглушка,
							# пока пусто — обучение как раздел ещё не существует). Содержимое
							# "Настроек" разбито на 2 визуальных раздела с заголовком покрупнее
							# и линией-разделителем НАД разделом (кроме самого первого — у него
							# только заголовок, без линии, см. ConfirmWindowsSeparator):
							# "Автоматическое нажатие человеком" (свободный ввод — защита от
							# случайного/поспешного клика на "Применить" и лимит на число
							# "побегов" кнопки, см. _fC_OnScreenApplyButtonPressed_Ch; было
							# ползунками (HSlider), 2026-08-20 переведено на LineEdit) и
							# "Окна подтверждения и информирования" (два переключателя —
							# старое содержимое; переименовано 2026-08-20 — "поддержки" ошибочно
							# ссылалось на отдельную, не связанную функцию Support)
							"ConfirmationsContentVBox": _fC_NodeCfg("HBoxContainer", {
								"visible": false,
								"theme_override_constants/separation": 12,
							}, {}, {
								"ConfirmSourceVBox": _fC_NodeCfg("VBoxContainer", {
									"custom_minimum_size": Vector2(110, 0),
									"size_flags_horizontal": 0, # SHRINK_BEGIN — не растягивается на весь HBox
									"alignment": 1,
									"theme_override_constants/separation": 8,
								}, {}, {
									"ConfirmSourceSettingsButton": _fC_NodeCfg("Button", {
										"text": "CONFIRM_SOURCE_SETTINGS",
										"toggle_mode": true,
										"button_pressed": true,
										"button_group": _vGC_ConfirmSourceButtonGroup,
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}, {
										"toggled": Callable(self, "_fC_OnConfirmSourceToggled_Ch").bind("Settings"),
									}),
									"ConfirmSourceTrainingButton": _fC_NodeCfg("Button", {
										"text": "CONFIRM_SOURCE_TRAINING",
										"toggle_mode": true,
										"button_group": _vGC_ConfirmSourceButtonGroup,
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}, {
										"toggled": Callable(self, "_fC_OnConfirmSourceToggled_Ch").bind("Training"),
									}),
								}),
								"ConfirmSourceContentArea": _fC_NodeCfg("VBoxContainer", {
									"size_flags_horizontal": 3,
									"size_flags_vertical": 3,
									"theme_override_constants/separation": 10,
								}, {}, {
									"ConfirmSourceSettingsVBox": _fC_NodeCfg("VBoxContainer", {
										"visible": true,
										"theme_override_constants/separation": 10,
									}, {}, {
										# Раздел 1 — самый верхний, поэтому БЕЗ линии-разделителя
										# сверху, только заголовок покрупнее
										"ConfirmAutoClickTitle": _fC_NodeCfg("Label", {
											"autowrap_mode": 3,
											"text": "CONFIRM_SECTION_AUTOCLICK",
											"horizontal_alignment": 0, # LEFT
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
										}),
										# 2026-08-20: обратная связь — ползунки неудобны для точного
										# значения, заменены на свободный ввод текста (LineEdit), тот же
										# паттерн валидации, что и у ScreenWidth/HeightLineEdit
										# (text_submitted по Enter + focus_exited при уходе с поля)
										# 2026-08-20: п.5 ТЗ — "все интерактивные части изначально ниже
										# текста": было HBoxContainer (подпись слева/поле справа), стало
										# VBoxContainer (подпись сверху/поле снизу), тот же паттерн, что
										# и у ScreenWidthGroupVBox/ScreenHeightGroupVBox
										"ConfirmAutoClickProtectionRow": _fC_NodeCfg("VBoxContainer", {
											"theme_override_constants/separation": 2,
										}, {}, {
											"ConfirmAutoClickProtectionLabel": _fC_NodeCfg("Label", {
												"autowrap_mode": 3,
												"text": "CONFIRM_PROTECTION_LABEL",
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}),
											"ConfirmAutoClickProtectionLineEdit": _fC_NodeCfg("LineEdit", {
												"placeholder_text": "0.5",
												"custom_minimum_size": Vector2(70, 0),
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}, {
												"text_submitted": Callable(self, "_fC_OnAutoClickProtectionSubmitted_Ch"),
												"focus_exited": Callable(self, "_fC_OnAutoClickProtectionSubmitted_Ch").bind(""),
											}),
										}),
										"ConfirmAutoClickDodgeLimitRow": _fC_NodeCfg("VBoxContainer", {
											"theme_override_constants/separation": 2,
										}, {}, {
											"ConfirmAutoClickDodgeLimitLabel": _fC_NodeCfg("Label", {
												"autowrap_mode": 3,
												"text": "CONFIRM_DODGE_LIMIT_LABEL",
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}),
											"ConfirmAutoClickDodgeLimitLineEdit": _fC_NodeCfg("LineEdit", {
												"placeholder_text": "1",
												"custom_minimum_size": Vector2(70, 0),
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}, {
												"text_submitted": Callable(self, "_fC_OnAutoClickDodgeLimitSubmitted_Ch"),
												"focus_exited": Callable(self, "_fC_OnAutoClickDodgeLimitSubmitted_Ch").bind(""),
											}),
										}),
										# Раздел 2 — линия НАД заголовком (все разделы, кроме самого
										# первого, получают такую линию — п.4 ТЗ 2026-08-19)
										"ConfirmWindowsSeparator": _fC_NodeCfg("HSeparator", {}),
										"ConfirmWindowsTitle": _fC_NodeCfg("Label", {
											"autowrap_mode": 3,
											"text": "CONFIRM_SECTION_WINDOWS",
											"horizontal_alignment": 0,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
										}),
										"ConfirmScreenInfoRow": _fC_NodeCfg("VBoxContainer", {
											"theme_override_constants/separation": 2,
										}, {}, {
											"ConfirmScreenInfoLabel": _fC_NodeCfg("Label", {
												"autowrap_mode": 3,
												"text": "CONFIRM_SCREEN_INFO_LABEL",
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}),
											"ConfirmScreenInfoToggle": _fC_NodeCfg("Button", {
												"text": "CONFIRM_TOGGLE_OFF",
												"toggle_mode": true,
												"button_pressed": false,
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
											}, {
												"toggled": Callable(self, "_fC_OnConfirmationToggle_Ch").bind("ShowScreenChangeInfoPopup", "ConfirmScreenInfoToggle"),
											}),
										}),
										"ConfirmScreenApplyRow": _fC_NodeCfg("VBoxContainer", {
											"theme_override_constants/separation": 2,
										}, {}, {
											"ConfirmScreenApplyLabel": _fC_NodeCfg("Label", {
												"autowrap_mode": 3,
												"text": "CONFIRM_SCREEN_APPLY_LABEL",
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}),
											"ConfirmScreenApplyToggle": _fC_NodeCfg("Button", {
												"text": "CONFIRM_TOGGLE_OFF",
												"toggle_mode": true,
												"button_pressed": false,
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
											}, {
												"toggled": Callable(self, "_fC_OnConfirmationToggle_Ch").bind("ShowScreenApplyConfirmPopup", "ConfirmScreenApplyToggle"),
											}),
										}),
										# 2026-08-22: "добавь при изменении параметров информационное окно,
										# что изменения будут применены только после перезапуска" —
										# про категорию "Логи и реплеи" (LoggerInfoPopupOverlay, см.
										# _fC_ShowLoggerInfoPopup_Ch); тот же generic-переключатель
										# _fC_OnConfirmationToggle_Ch, что и у двух строк выше
										"ConfirmLoggerInfoRow": _fC_NodeCfg("VBoxContainer", {
											"theme_override_constants/separation": 2,
										}, {}, {
											"ConfirmLoggerInfoLabel": _fC_NodeCfg("Label", {
												"autowrap_mode": 3,
												"text": "CONFIRM_LOGGER_INFO_LABEL",
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
											}),
											"ConfirmLoggerInfoToggle": _fC_NodeCfg("Button", {
												"text": "CONFIRM_TOGGLE_OFF",
												"toggle_mode": true,
												"button_pressed": false,
												"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
											}, {
												"toggled": Callable(self, "_fC_OnConfirmationToggle_Ch").bind("ShowLoggerRestartInfoPopup", "ConfirmLoggerInfoToggle"),
											}),
										}),
									}),
									# "Обучение" — осознанно пустой раздел (2026-08-19: "обучения пока
									# будет пустой") — самого раздела "Обучение" в проекте ещё нет
									"ConfirmSourceTrainingVBox": _fC_NodeCfg("VBoxContainer", {
										"visible": false,
									}),
								}),
							}),

							"SoundContentVBox": _fC_NodeCfg("VBoxContainer", {
								"visible": false,
								"theme_override_constants/separation": 12,
							}, {}, {
								# 2026-08-20: п.5 ТЗ — подпись сверху, "управление" (ползунок +
								# число рядом) снизу отдельной строкой; было HBoxContainer с
								# подписью/ползунком/числом в один ряд (переименовано
								# *HBox -> *VBox, ссылок на старое имя нигде не было)
								"SoundMusicVBox": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"SoundMusicLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "SOUND_MUSIC",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"SoundMusicControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"SoundMusicSlider": _fC_NodeCfg("HSlider", {
											"min_value": 0, "max_value": 100, "step": 1,
											"size_flags_horizontal": 3,
										}, {
											"value_changed": Callable(self, "_fC_OnSoundSliderChanged_Ch").bind("MusicVolume", "SoundMusicValueLabel"),
										}),
										"SoundMusicValueLabel": _fC_NodeCfg("Label", {
											"text": "100%",
											"custom_minimum_size": Vector2(44, 0),
											"horizontal_alignment": 2,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}),
									}),
								}),
								"SoundInterfaceVBox": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"SoundInterfaceLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "SOUND_INTERFACE",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"SoundInterfaceControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"SoundInterfaceSlider": _fC_NodeCfg("HSlider", {
											"min_value": 0, "max_value": 100, "step": 1,
											"size_flags_horizontal": 3,
										}, {
											"value_changed": Callable(self, "_fC_OnSoundSliderChanged_Ch").bind("InterfaceVolume", "SoundInterfaceValueLabel"),
										}),
										"SoundInterfaceValueLabel": _fC_NodeCfg("Label", {
											"text": "100%",
											"custom_minimum_size": Vector2(44, 0),
											"horizontal_alignment": 2,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}),
									}),
								}),
							}),

							# 2026-08-22: категория "Логи и реплеи" — обратная связь: "Логи
							# Программы и Реплеи как два раздела в одной категории с
							# настройками Максимальное количество/время/объём памяти, с
							# окном ввода справа и галочкой (квадрат красно/зелёный) слева,
							# которая включает настройку". Тот же паттерн заголовок+
							# HSeparator между разделами, что и у "Экран"/"Подтверждения"
							# (см. их changelog'и), и тот же паттерн "подпись сверху,
							# элементы управления снизу", что и везде на этом экране.
							# Галочка слева — маленькая квадратная toggle-кнопка без текста,
							# просто перекрашивается в зелёный/красный через modulate (та же
							# палитра vGD_StatusColors_D, что и у чек-листа/переключателей
							# "Подтверждений" — см. _fC_UpdateLoggerCheckVisual_Ch), поле
							# ввода справа — обычный LineEdit с тем же паттерном валидации
							# (Enter/focus_exited), что и везде (см. _fC_OnLoggerLimitSubmitted_Ch).
							# ⚠️ Это ТОЛЬКО настройки и их UI — реальное применение лимитов
							# (удаление старых логов/реплеев) эта сцена не делает, см.
							# changelog у _fC_LoadSettings_Cr про эти же ключи
							"LoggerContentVBox": _fC_NodeCfg("VBoxContainer", {
								"visible": false,
								"theme_override_constants/separation": 10,
							}, {}, {
								"LoggerLogsSectionTitle": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "LOGGER_SECTION_LOGS",
									"horizontal_alignment": 0, # LEFT
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
								}),
								"LoggerLogMaxCountRow": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"LoggerLogMaxCountLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "LOGGER_LOG_MAX_COUNT_LABEL",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"LoggerLogMaxCountControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"LoggerLogMaxCountCheck": _fC_NodeCfg("Button", {
											"text": "",
											"toggle_mode": true,
											"button_pressed": true,
											"custom_minimum_size": Vector2(28, 28),
											"size_flags_horizontal": 0, # SHRINK_BEGIN — не растягивается
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}, {
											"toggled": Callable(self, "_fC_OnLoggerLimitToggled_Ch").bind("LogMaxCountEnabled", "LoggerLogMaxCountCheck"),
										}),
										"LoggerLogMaxCountLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "100",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("LogMaxCount", "LoggerLogMaxCountLineEdit"),
											"focus_exited": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("", "LogMaxCount", "LoggerLogMaxCountLineEdit"),
										}),
									}),
								}),
								"LoggerLogMaxAgeRow": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"LoggerLogMaxAgeLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "LOGGER_LOG_MAX_AGE_LABEL",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"LoggerLogMaxAgeControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"LoggerLogMaxAgeCheck": _fC_NodeCfg("Button", {
											"text": "",
											"toggle_mode": true,
											"button_pressed": true,
											"custom_minimum_size": Vector2(28, 28),
											"size_flags_horizontal": 0,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}, {
											"toggled": Callable(self, "_fC_OnLoggerLimitToggled_Ch").bind("LogMaxAgeEnabled", "LoggerLogMaxAgeCheck"),
										}),
										"LoggerLogMaxAgeLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "30",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("LogMaxAgeDays", "LoggerLogMaxAgeLineEdit"),
											"focus_exited": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("", "LogMaxAgeDays", "LoggerLogMaxAgeLineEdit"),
										}),
									}),
								}),
								"LoggerLogMaxSizeRow": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"LoggerLogMaxSizeLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "LOGGER_LOG_MAX_SIZE_LABEL",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"LoggerLogMaxSizeControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"LoggerLogMaxSizeCheck": _fC_NodeCfg("Button", {
											"text": "",
											"toggle_mode": true,
											"button_pressed": true,
											"custom_minimum_size": Vector2(28, 28),
											"size_flags_horizontal": 0,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}, {
											"toggled": Callable(self, "_fC_OnLoggerLimitToggled_Ch").bind("LogMaxSizeEnabled", "LoggerLogMaxSizeCheck"),
										}),
										"LoggerLogMaxSizeLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "50",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("LogMaxSizeMB", "LoggerLogMaxSizeLineEdit"),
											"focus_exited": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("", "LogMaxSizeMB", "LoggerLogMaxSizeLineEdit"),
										}),
									}),
								}),
								# Раздел 2 — линия НАД заголовком (тот же паттерн, что и у
								# ConfirmWindowsSeparator/ScreenFontSeparator)
								"LoggerReplaySeparator": _fC_NodeCfg("HSeparator", {}),
								"LoggerReplaySectionTitle": _fC_NodeCfg("Label", {
									"autowrap_mode": 3,
									"text": "LOGGER_SECTION_REPLAYS",
									"horizontal_alignment": 0,
									"theme_override_font_sizes/font_size": _fC_FontSize_I("Subtitle"),
								}),
								"LoggerReplayMaxCountRow": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"LoggerReplayMaxCountLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "LOGGER_REPLAY_MAX_COUNT_LABEL",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"LoggerReplayMaxCountControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"LoggerReplayMaxCountCheck": _fC_NodeCfg("Button", {
											"text": "",
											"toggle_mode": true,
											"button_pressed": true,
											"custom_minimum_size": Vector2(28, 28),
											"size_flags_horizontal": 0,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}, {
											"toggled": Callable(self, "_fC_OnLoggerLimitToggled_Ch").bind("ReplayMaxCountEnabled", "LoggerReplayMaxCountCheck"),
										}),
										"LoggerReplayMaxCountLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "20",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("ReplayMaxCount", "LoggerReplayMaxCountLineEdit"),
											"focus_exited": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("", "ReplayMaxCount", "LoggerReplayMaxCountLineEdit"),
										}),
									}),
								}),
								"LoggerReplayMaxAgeRow": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"LoggerReplayMaxAgeLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "LOGGER_REPLAY_MAX_AGE_LABEL",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"LoggerReplayMaxAgeControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"LoggerReplayMaxAgeCheck": _fC_NodeCfg("Button", {
											"text": "",
											"toggle_mode": true,
											"button_pressed": true,
											"custom_minimum_size": Vector2(28, 28),
											"size_flags_horizontal": 0,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}, {
											"toggled": Callable(self, "_fC_OnLoggerLimitToggled_Ch").bind("ReplayMaxAgeEnabled", "LoggerReplayMaxAgeCheck"),
										}),
										"LoggerReplayMaxAgeLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "7",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("ReplayMaxAgeDays", "LoggerReplayMaxAgeLineEdit"),
											"focus_exited": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("", "ReplayMaxAgeDays", "LoggerReplayMaxAgeLineEdit"),
										}),
									}),
								}),
								"LoggerReplayMaxSizeRow": _fC_NodeCfg("VBoxContainer", {
									"theme_override_constants/separation": 2,
								}, {}, {
									"LoggerReplayMaxSizeLabel": _fC_NodeCfg("Label", {
										"autowrap_mode": 3,
										"text": "LOGGER_REPLAY_MAX_SIZE_LABEL",
										"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
									}),
									"LoggerReplayMaxSizeControlHBox": _fC_NodeCfg("HBoxContainer", {
										"theme_override_constants/separation": 8,
									}, {}, {
										"LoggerReplayMaxSizeCheck": _fC_NodeCfg("Button", {
											"text": "",
											"toggle_mode": true,
											"button_pressed": true,
											"custom_minimum_size": Vector2(28, 28),
											"size_flags_horizontal": 0,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
										}, {
											"toggled": Callable(self, "_fC_OnLoggerLimitToggled_Ch").bind("ReplayMaxSizeEnabled", "LoggerReplayMaxSizeCheck"),
										}),
										"LoggerReplayMaxSizeLineEdit": _fC_NodeCfg("LineEdit", {
											"placeholder_text": "200",
											"size_flags_horizontal": 3,
											"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
										}, {
											"text_submitted": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("ReplayMaxSizeMB", "LoggerReplayMaxSizeLineEdit"),
											"focus_exited": Callable(self, "_fC_OnLoggerLimitSubmitted_Ch").bind("", "ReplayMaxSizeMB", "LoggerReplayMaxSizeLineEdit"),
										}),
									}),
								}),
							}),
						}),
			}), # закрывает ModalContentScroll (новый уровень вложенности, п.2 ТЗ 2026-08-20)
		})),
	}

	# 2026-08-19: два всплывающих окна про изменение размера экрана (см. changelog
	# "окно применить/отмена") — верхнеуровневые СОСЕДИ ModalOverlay (не внутри
	# него), добавлены через merge() ПОСЛЕ литерала специально, чтобы оказаться
	# ПОСЛЕ ModalOverlay по порядку ключей = рисоваться ПОВЕРХ него (тот же принцип
	# z-порядка "кто позже создан — тот выше", что и у TopButtonsHBox/ModalOverlay
	# выше). Своих собственных категорий на скрытие/показ не имеют — видимость
	# переключают _fC_ShowScreenInfoPopup_Ch/_fC_ShowScreenApplyConfirm_Ch
	vLD_Screen_D.merge(_fC_BuildConfirmOverlay_Cr("ScreenInfoPopup", {
		"ScreenInfoPopupVBox": _fC_NodeCfg("VBoxContainer", {
			"anchor_left": 0.0, "anchor_top": 0.0, "anchor_right": 1.0, "anchor_bottom": 1.0,
			"offset_left": 18, "offset_top": 14, "offset_right": -18, "offset_bottom": -14,
			"theme_override_constants/separation": 10,
		}, {}, {
			"ScreenInfoPopupLabel": _fC_NodeCfg("Label", {
				"autowrap_mode": 3,
				"text": "SCREEN_CHANGE_INFO",
				"size_flags_vertical": 3,
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
			}),
			# Галочка "больше не показывать" — снизу справа, НАД кнопкой (порядок
			# в VBox = порядок сверху вниз, alignment=2/END сдвигает вправо)
			"ScreenInfoPopupCheckHBox": _fC_NodeCfg("HBoxContainer", {
				"alignment": 2,
			}, {}, {
				"ScreenInfoPopupDontShowCheck": _fC_NodeCfg("CheckBox", {
					"text": "DONT_SHOW_AGAIN",
					"button_pressed": false, # всегда сброшена при открытии — разовое действие "выключить сейчас", не отражение текущего состояния
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}, {
					"toggled": Callable(self, "_fC_OnScreenInfoDontShowToggled_Ch"),
				}),
			}),
			"ScreenInfoPopupOkButton": _fC_NodeCfg("Button", {
				"text": "OK_UNDERSTOOD",
				"size_flags_horizontal": 4,
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
			}, {
				"pressed": Callable(self, "_fC_OnScreenInfoOkPressed_Ch"),
			}),
		}),
	}))
	vLD_Screen_D.merge(_fC_BuildConfirmOverlay_Cr("ScreenApplyConfirm", {
		"ScreenApplyConfirmVBox": _fC_NodeCfg("VBoxContainer", {
			"anchor_left": 0.0, "anchor_top": 0.0, "anchor_right": 1.0, "anchor_bottom": 1.0,
			"offset_left": 18, "offset_top": 14, "offset_right": -18, "offset_bottom": -14,
			"theme_override_constants/separation": 10,
		}, {}, {
			"ScreenApplyConfirmLabel": _fC_NodeCfg("Label", {
				"autowrap_mode": 3,
				"text": "SCREEN_APPLY_CONFIRM_TEXT",
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
			}),
			"ScreenApplyConfirmCheckHBox": _fC_NodeCfg("HBoxContainer", {
				"alignment": 2,
			}, {}, {
				"ScreenApplyConfirmDontShowCheck": _fC_NodeCfg("CheckBox", {
					"text": "DONT_SHOW_AGAIN",
					"button_pressed": false,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}, {
					"toggled": Callable(self, "_fC_OnScreenApplyDontShowToggled_Ch"),
				}),
			}),
			# 2026-08-22 — обратная связь "автоматическое нажатие не работает,
			# добавь счётчик у кнопки подтвердить, который будет исчезать по
			# истечении и позволять нажать на кнопку": видимый обратный отсчёт
			# защиты (AutoClickProtectionSeconds) — в фиксированном месте НАД
			# ареной (не двигается вместе с кнопкой при "побеге", в отличие от
			# самой ScreenApplyConfirmArena — см. её changelog). Обновляется
			# по таймеру (ScreenApplyConfirmCountdownTimer, тик каждые 0.1с) —
			# см. _fC_OnApplyCountdownTick_Ch/_fC_UpdateApplyCountdownDisplay_Ch
			"ScreenApplyConfirmCountdownHBox": _fC_NodeCfg("HBoxContainer", {
				"alignment": 1, # CENTER
				"theme_override_constants/separation": 6,
			}, {}, {
				"ScreenApplyConfirmCountdownLabel": _fC_NodeCfg("Label", {
					"text": "SCREEN_APPLY_COUNTDOWN_LABEL",
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}),
				"ScreenApplyConfirmCountdownValueLabel": _fC_NodeCfg("Label", {
					"text": "0.0",
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}),
			}),
			"ScreenApplyConfirmCountdownTimer": _fC_NodeCfg("Timer", {
				"wait_time": 0.1,
				"autostart": false,
				"one_shot": false,
			}, {
				"timeout": Callable(self, "_fC_OnApplyCountdownTick_Ch"),
			}),
			# "Арена" — обычный Control (НЕ контейнер), дети внутри него НЕ
			# перестраиваются автоматически, поэтому кнопку "Применить" можно
			# двигать вручную через .position (см. _fC_DodgeApplyButton_Ch) —
			# именно поэтому она не может лежать в HBox/VBox вместе с "Отмена"
			"ScreenApplyConfirmArena": _fC_NodeCfg("Control", {
				"custom_minimum_size": Vector2(260, 64),
				"size_flags_vertical": 3,
			}, {}, {
				"ScreenApplyConfirmCancelButton": _fC_NodeCfg("Button", {
					"text": "CANCEL",
					"anchor_left": 0.0, "anchor_top": 0.5, "anchor_right": 0.0, "anchor_bottom": 0.5,
					"offset_left": 0, "offset_top": -18, "offset_right": 90, "offset_bottom": 18,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
				}, {
					"pressed": Callable(self, "_fC_OnScreenApplyCancelPressed_Ch"),
				}),
				# 2026-08-19: изначально стояла НЕ на одном уровне с "Отмена" (anchor_top
				# 0.0 вместо 0.5) — обратная связь "кнопка подтверждения не на одном
				# уровне с отменой". Теперь тот же вертикальный якорь/офсет, что и у
				# Cancel (anchor_top/bottom=0.5, offset -18..18) — оба по центру арены
				"ScreenApplyConfirmApplyButton": _fC_NodeCfg("Button", {
					"text": "APPLY",
					"anchor_left": 0.0, "anchor_top": 0.5, "anchor_right": 0.0, "anchor_bottom": 0.5,
					"offset_left": 130, "offset_top": -18, "offset_right": 220, "offset_bottom": 18,
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
				}, {
					"pressed": Callable(self, "_fC_OnScreenApplyButtonPressed_Ch"),
				}),
			}),
		}),
	}))
	# 2026-08-22: третье всплывающее окно, тот же паттерн _fC_BuildConfirmOverlay_Cr,
	# что и два выше — "изменения (категория 'Логи и реплеи') применятся только
	# после перезапуска программы" (обратная связь: "добавь при изменении
	# параметров информационное окно, что изменения будут применены только
	# после перезапуска"). Видимость переключает _fC_ShowLoggerInfoPopup_Ch —
	# вызывается из обработчиков галочек/полей категории "Логи и реплеи"
	vLD_Screen_D.merge(_fC_BuildConfirmOverlay_Cr("LoggerInfoPopup", {
		"LoggerInfoPopupVBox": _fC_NodeCfg("VBoxContainer", {
			"anchor_left": 0.0, "anchor_top": 0.0, "anchor_right": 1.0, "anchor_bottom": 1.0,
			"offset_left": 18, "offset_top": 14, "offset_right": -18, "offset_bottom": -14,
			"theme_override_constants/separation": 10,
		}, {}, {
			"LoggerInfoPopupLabel": _fC_NodeCfg("Label", {
				"autowrap_mode": 3,
				"text": "LOGGER_RESTART_INFO",
				"size_flags_vertical": 3,
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
			}),
			"LoggerInfoPopupCheckHBox": _fC_NodeCfg("HBoxContainer", {
				"alignment": 2,
			}, {}, {
				"LoggerInfoPopupDontShowCheck": _fC_NodeCfg("CheckBox", {
					"text": "DONT_SHOW_AGAIN",
					"button_pressed": false, # всегда сброшена при открытии, тот же принцип, что и у ScreenInfoPopupDontShowCheck
					"theme_override_font_sizes/font_size": _fC_FontSize_I("Small"),
				}, {
					"toggled": Callable(self, "_fC_OnLoggerInfoDontShowToggled_Ch"),
				}),
			}),
			"LoggerInfoPopupOkButton": _fC_NodeCfg("Button", {
				"text": "OK_UNDERSTOOD",
				"size_flags_horizontal": 4,
				"theme_override_font_sizes/font_size": _fC_FontSize_I("Body"),
			}, {
				"pressed": Callable(self, "_fC_OnLoggerInfoOkPressed_Ch"),
			}),
		}),
	}))

	# Каждый блок верхнего уровня помечаем "v__Ebeveyn" = собственное имя блока —
	# логическая метка (см. changelog Class_UI.gd, 2026-08-18), которую подхватят
	# все вложенные ноды блока через A_BelongTo_A. От реального пути в дереве
	# сцены никак не зависит — нужна для будущих массовых операций по блоку целиком
	# (например, Class_UI.fxC_MassDelitNode(vSD_AllNodes_D, ...) по тегу "CheckListPanel")
	for elLS_BlockKey in vLD_Screen_D:
		vLD_Screen_D[elLS_BlockKey]["v__Ebeveyn"] = elLS_BlockKey

	return vLD_Screen_D


#Функционал:
#	Строит конфиг под текущую vLS_Orientation_S и создаёт весь экран одним
#	проходом через Class_UI.fC_Mass_Creating_Node_CrTr — она и предназначена
#	для самой первой сборки дерева нод целиком (см. changelog Class_UI.gd).
#	После сборки — донастройка окна Поддержка/Настройки, которую нельзя
#	описать статическим конфигом: подставить сохранённые значения настроек в
#	поля (_fC_ApplySettingsToUI_Ch), построить список языков под текущий
#	поиск (изначально пустой — весь список) и выставить видимость контента
#	под изначально активную категорию
func _fC_BuildScreen_Ch() -> void:
	vXD_CurrentScreenConfig_D = _fC_BuildScreenConfig_Cr(vLS_Orientation_S)
	Class_UI.fC_Mass_Creating_Node_CrTr(vXD_CurrentScreenConfig_D, vSD_AllNodes_D)
	_fC_ApplySettingsToUI_Ch()
	_fC_RebuildLanguageList_Ch("")
	_fC_UpdateModalContentVisibility_Ch()
	_fC_UpdateConfirmSourceVisibility_Ch()


#Функционал:
#	Переключает ориентацию экрана на лету — пересобирает конфиг под новую
#	ориентацию и передаёт старый/новый конфиг в fC_SyncNodesWithConfig_Ch,
#	который сам решает, что пересоздать/точечно обновить/удалить (см. её
#	changelog в Class_UI.gd). Готовый механизм под будущий переключатель в
#	настройках — сам переключатель не входит в эту задачу
func fC_SwitchOrientation_Ch(vLS_NewOrientation_S: String) -> void:
	if vLS_NewOrientation_S == vLS_Orientation_S:
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "fC_SwitchOrientation_Ch: already this orientation, ignored", [vLS_NewOrientation_S], null)
		return
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "fC_SwitchOrientation_Ch: switching", [vLS_Orientation_S, vLS_NewOrientation_S], null)
	var vXD_NewConfig_D: Dictionary = _fC_BuildScreenConfig_Cr(vLS_NewOrientation_S)
	Class_UI.fC_SyncNodesWithConfig_Ch(vSD_AllNodes_D, vXD_CurrentScreenConfig_D, vXD_NewConfig_D)
	vXD_CurrentScreenConfig_D = vXD_NewConfig_D
	vLS_Orientation_S = vLS_NewOrientation_S


#Функционал:
#	Считает, сколько нод-конфигов (словарей с __Data_D/__Inspector_D/__Node_D)
#	есть в дереве конфига — эталон для честной проверки, что интерфейс собрался
#	полностью. Переиспользует Class_UI._fC_IsNodeConfig, чтобы не дублировать
#	правило "что считается нодой". "__Children_D" больше не единственное
#	признанное имя обёртки (см. changelog Class_UI.gd, 2026-08-18) — здесь та же
#	логика: спускаемся в ЛЮБОЙ Dictionary, который сам не нода, независимо от имени
func _fC_CountNodeConfigs_I(vLD_Tree_D: Dictionary) -> int:
	var vLI_Count := 0
	for elLS_Key in vLD_Tree_D:
		if elLS_Key == "__Data_D" or elLS_Key == "__Inspector_D" or elLS_Key == "__Node_D" or elLS_Key == "v__Ebeveyn":
			continue
		var vL_Value = vLD_Tree_D[elLS_Key]
		if typeof(vL_Value) != TYPE_DICTIONARY:
			continue
		if Class_UI._fC_IsNodeConfig(vL_Value):
			vLI_Count += 1
		vLI_Count += _fC_CountNodeConfigs_I(vL_Value)
	return vLI_Count


#Функционал:
#	Честно проверяет, что интерфейс реально собрался целиком: сверяет число
#	нод, фактически оказавшихся в vSD_AllNodes_D, с числом нод-конфигов в
#	vXD_CurrentScreenConfig_D. Расхождение = где-то внутри fC_Creating_Node_CrTr
#	сработал push_error и вернул {} (несуществующий тип, не найден путь и т.п.)
func _fC_VerifyInterfaceBuilt_Bv() -> bool:
	var vLI_Expected: int = _fC_CountNodeConfigs_I(vXD_CurrentScreenConfig_D)
	var vLI_Actual: int = vSD_AllNodes_D.size()
	if vLI_Actual != vLI_Expected:
		push_warning("⚠️ _fC_VerifyInterfaceBuilt_Bv: создано %d нод из %d ожидаемых" % [vLI_Actual, vLI_Expected])
		return false
	return true


#Функционал:
#	Критическая ошибка на старте (логгер или интерфейс не заработали) —
#	показывает нативное окно ОС (не зависит от нашего Class_UI, который на
#	этом этапе мог и не собраться) и закрывает программу
#Форматы данных:
#	Входные:
#		vLS_Component_S: String — что именно не сработало ("Логгер"/"Интерфейс")
#		vLS_Detail_S: String — подробность для лога и текста окна
func _fC_FatalStartupError_Ch(vLS_Component_S: String, vLS_Detail_S: String) -> void:
	var vLS_Text: String = "Критическая ошибка запуска: %s.\n%s\nПрограмма будет закрыта." % [vLS_Component_S, vLS_Detail_S]
	push_error("⛔ " + vLS_Text)

	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "Fatal startup error: " + vLS_Component_S, [vLS_Detail_S], "push_error")
		Class_Logger.fC_FinishStream_Ch(vLS_StreamId)
		# Форсируем сброс на диск — дальше сразу quit(), обычного автосброса можно не дождаться
		Class_Logger.fC_Saving_Logs_Ch()

	OS.alert(vLS_Text, "Ошибка запуска")
	get_tree().quit()


#Функционал:
#	Некритическая ошибка (программа продолжает работать) — логирует и
#	показывает сообщение в окне информации (InfoBoxLabel), НЕ push_error и НЕ
#	OS.alert/quit (см. changelog 2026-08-19, п.3 ТЗ: "избежать вылета и
#	информировать о проблеме"). В отличие от _fC_FatalStartupError_Ch выше —
#	это для проблем, при которых экран остаётся рабочим (например, не
#	загрузились переводы — тогда просто видны ключи вместо текста, а не крах)
#Форматы данных:
#	Входные:
#		vLS_Component_S: String — что не сработало (например "Локализация")
#		vLS_Detail_S: String — подробность проблемы
func _fC_ReportError_Ch(vLS_Component_S: String, vLS_Detail_S: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "Non-fatal error: " + vLS_Component_S, [vLS_Detail_S], "error")
	_fC_ShowInfo_Ch("%s: %s" % [vLS_Component_S, vLS_Detail_S])


#Функционал:
#	Публичный API для выставления статуса одной проверки чек-листа — им же
#	будет пользоваться будущая реальная логика (сервер/обновление/сторонние файлы)
#Форматы данных:
#	Входные:
#		vLS_Key: String — ключ из vLA_CheckOrder_A ("Logger","Interface",...)
#		vLS_Status: String — "waiting" / "passed" / "failed"
#		vLS_InfoMessage: String — необязательный текст в окно информации (обычно при "failed")
#	Выходные: нет
func fC_SetCheckStatus_Ch(vLS_Key: String, vLS_Status: String, vLS_InfoMessage: String = "") -> void:
	if not vLD_CheckStatuses_D.has(vLS_Key):
		push_warning("⚠️ fC_SetCheckStatus_Ch: неизвестная проверка '%s'" % vLS_Key)
		return
	if not vGD_StatusSymbols_D.has(vLS_Status):
		push_warning("⚠️ fC_SetCheckStatus_Ch: неизвестный статус '%s'" % vLS_Status)
		return

	vLD_CheckStatuses_D[vLS_Key] = vLS_Status

	var vLS_LabelKey: String = "Row_%s_StatusLabel" % vLS_Key
	if vSD_AllNodes_D.has(vLS_LabelKey):
		var vL_Node = vSD_AllNodes_D[vLS_LabelKey]["Node"]
		vL_Node.text = vGD_StatusSymbols_D[vLS_Status]
		vL_Node.set("theme_override_colors/font_color", vGD_StatusColors_D[vLS_Status])
	else:
		push_warning("⚠️ fC_SetCheckStatus_Ch: нода '%s' не найдена в реестре" % vLS_LabelKey)

	if vLS_Status == "failed" and vLS_InfoMessage != "":
		_fC_ShowInfo_Ch(vLS_InfoMessage)

	_fC_UpdateOverallProgress_Ch()


#Функционал:
#	2026-08-23 (правка): открывает окно выбора аккаунта — теперь это не
#	отдельный полноэкранный оверлей, а AccountPickerSection ВНУТРИ
#	InfoBoxPanel/InfoBoxVBox (см. changelog в _fC_BuildScreenConfig_Cr, обратная
#	связь "окно должно спрашивать под проверками в отведённом окошке") —
#	первый шаг проверки "Server" в чек-листе (см. _ready()). Реальные
#	аккаунты (см. vLD_AccountsList_D) + техническое значение "Server" —
#	чтобы на одном устройстве можно было одновременно эмулировать и сервер, и
#	клиента, запустив два экземпляра программы и выбрав в них разное.
#	InfoBoxLabel на время выбора скрывается — в этом небольшом по высоте
#	блоке одновременно и статусный текст, и список аккаунтов не помещаются
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_ShowAccountPicker_Ch() -> void:
	if vSD_AllNodes_D.has("AccountPickerSection"):
		vSD_AllNodes_D["AccountPickerSection"]["Node"].visible = true
	else:
		push_warning("⚠️ _fC_ShowAccountPicker_Ch: нода AccountPickerSection не найдена в реестре")
	if vSD_AllNodes_D.has("InfoBoxLabel"):
		vSD_AllNodes_D["InfoBoxLabel"]["Node"].visible = false
	_fC_ShowAccountPickerMessage_Ch("")


#Функционал:
#	Обработчик нажатия одной из кнопок в окне выбора аккаунта (см.
#	_fC_ShowAccountPicker_Ch, кнопки собраны циклом в _fC_BuildScreenConfig_Cr,
#	каждая привязана через .bind(elLS_Account)) — закрывает AccountPickerSection
#	и возвращает обычный статусный текст (InfoBoxLabel) на место
#Форматы данных:
#	Входные: vLS_Account_S: String — выбранный аккаунт или ACCOUNT_TECHNICAL_SERVER_VALUE
#	Выходные: нет
func _fC_OnAccountOptionPressed_Ch(vLS_Account_S: String) -> void:
	vLS_SelectedAccount_S = vLS_Account_S
	if vSD_AllNodes_D.has("AccountPickerSection"):
		vSD_AllNodes_D["AccountPickerSection"]["Node"].visible = false
	if vSD_AllNodes_D.has("InfoBoxLabel"):
		vSD_AllNodes_D["InfoBoxLabel"]["Node"].visible = true
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "AccountPicker: выбран " + vLS_Account_S, [], null)
	_fC_RunServerCheck_Ch()


#Функционал:
#	2026-08-23 (правка, была заглушка): продолжение проверки "Server" после
#	выбора аккаунта. Роль "Server" — сама сцена Start_Program для неё не
#	место: переключаемся на res://Scenes/test/test.tscn, там (см. её changelog)
#	поднимается Class_InternetServer. Реальный аккаунт — остаётся здесь и
#	запускает настоящее подключение (_fC_StartClientConnection_Ch)
func _fC_RunServerCheck_Ch() -> void:
	if vLS_SelectedAccount_S == ACCOUNT_TECHNICAL_SERVER_VALUE:
		Class_UI.fxC_SceneSwitching("res://Scenes/test/test.tscn")
		return
	_fC_StartClientConnection_Ch()


#Функционал:
#	2026-08-23: запускает настоящее подключение к серверу через
#	Class_InternetClient. add_child ОБЯЗАТЕЛЕН (см. changelog Class_InternetClient.gd
#	— это Node с переопределённым _process, без добавления в дерево сцены
#	опрос сокета просто не будет вызываться), делаем это только один раз
#	(_vLB_ClientConnectionStarted_Bv) — тогда же подключаем AppOpReceived_S
#	(см. changelog Class_Internet.gd) на _fC_OnClientAppOpReceived_Ch — это
#	точка входа для ответов "CheckUpdate"/"CheckFiles" (фаза 2). vGS_UserID_S
#	берём из ID выбранного аккаунта (см. vLD_AccountsList_D) — это то, что
#	сервер увидит как "кто подключился". Дальше опрос состояния подключения —
#	в _process ниже
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_StartClientConnection_Ch() -> void:
	var vLD_Account_D: Dictionary = vLD_AccountsList_D.get(vLS_SelectedAccount_S, {})
	Class_InternetClient.vGS_UserID_S = String(vLD_Account_D.get("ID", vLS_SelectedAccount_S))

	if not _vLB_ClientConnectionStarted_Bv:
		add_child(Class_InternetClient)
		Class_InternetClient.AppOpReceived_S.connect(_fC_OnClientAppOpReceived_Ch)
		_vLB_ClientConnectionStarted_Bv = true

	_vLB_ServerCheckResolved_Bv = false
	_vLB_CredentialsSent_Bv = false
	# 2026-08-24: НЕ сбрасываем _vLB_PendingRegistration_Bv здесь — она
	# выставляется ДО вызова этой функции (см. _fC_OnAccountAddRegisterPressed_Ch)
	# и должна пережить повторный вызов этой же функции через "Повторить
	# подключение" (InfoBoxRetryButton), если регистрация ещё не разрешилась —
	# сбрасывает её только _fC_HandleRegisterResult_Ch
	_vLB_RegistrationSent_Bv = false
	_vLB_AutonomousModeOffered_Bv = false
	_vLF_ServerConnectAttemptStartedAtMsec_F = Time.get_ticks_msec()

	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_StartClientConnection_Ch: подключаемся", [SERVER_ADDRESS_S, Class_InternetClient.vGS_UserID_S], null)

	var vLB_Started_Bv: bool = Class_InternetClient.fxG_EstablishingConnection_CrTr(SERVER_ADDRESS_S)
	if not vLB_Started_Bv:
		fC_SetCheckStatus_Ch("Server", "failed", "Не удалось начать подключение к серверу.")
		_fC_OfferAutonomousMode_Ch()
		return

	# 2026-08-24: показываем таймер только если попытка реально запустилась
	# (см. _fC_UpdateConnectingTimerLabel_Ch — обновляет текст каждый кадр,
	# здесь только видимость + начальный текст "0 сек", чтобы не мигало
	# пустой подписью один кадр до первого _process)
	if vSD_AllNodes_D.has("InfoBoxConnectingLabel"):
		vSD_AllNodes_D["InfoBoxConnectingLabel"]["Node"].visible = true
		vSD_AllNodes_D["InfoBoxConnectingLabel"]["Node"].text = "Подключение к серверу… 0 сек"


#Функционал:
#	2026-08-23: отправляет сохранённые в vLD_AccountsList_D логин/пароль
#	выбранного аккаунта на Challenge (вызывается из _process, когда
#	Class_InternetClient переходит в ConnectionState.AUTHENTICATING — то есть
#	сервер потребовал авторизацию). Один раз за попытку подключения
#	(_vLB_CredentialsSent_Bv) — AUTHENTICATING держится, пока не придёт
#	AuthSuccess/AuthFailed, повторные вызовы не нужны. ⚠️ Не проверялось живьём
#	— по умолчанию у test.gd сервер поднимается с RequireAuth=false, этот путь
#	пока не задействуется в реальном тестировании (см. её changelog)
func _fC_SendStoredCredentials_Ch() -> void:
	if _vLB_CredentialsSent_Bv:
		return
	_vLB_CredentialsSent_Bv = true
	var vLD_Account_D: Dictionary = vLD_AccountsList_D.get(vLS_SelectedAccount_S, {})
	Class_InternetClient.fC_SendCredentials_Ch(String(vLD_Account_D.get("ID", vLS_SelectedAccount_S)), String(vLD_Account_D.get("Password", "")))


#Функционал:
#	2026-08-24 (по просьбе пользователя "добавим... регистрацию для входа"):
#	вызывается из _process ВМЕСТО _fC_SendStoredCredentials_Ch, когда текущая
#	попытка подключения начата через "Зарегистрироваться"
#	(_vLB_PendingRegistration_Bv, см. _fC_OnAccountAddRegisterPressed_Ch) — шлёт
#	Op "Register" с тем же ID/паролем, что только что сохранены в
#	vLD_AccountsList_D. Один раз за попытку (_vLB_RegistrationSent_Bv, тот же
#	приём, что и у _vLB_CredentialsSent_Bv). Ответ ("RegisterResult") придёт в
#	_fC_OnClientAppOpReceived_Ch → _fC_HandleRegisterResult_Ch
func _fC_SendRegistrationRequest_Ch() -> void:
	if _vLB_RegistrationSent_Bv:
		return
	_vLB_RegistrationSent_Bv = true
	var vLD_Account_D: Dictionary = vLD_AccountsList_D.get(vLS_SelectedAccount_S, {})
	Class_InternetClient.fC_RegisterAccount_Ch(String(vLD_Account_D.get("ID", vLS_SelectedAccount_S)), String(vLD_Account_D.get("Password", "")))


#Функционал:
#	2026-08-23: вызывается из _process, когда Class_InternetClient впервые
#	доходит до ConnectionState.CONNECTED — отмечает "Server" пройденным и
#	(2026-08-23, фаза 2) запускает настоящие "Update"/"ThirdPartyFiles" —
#	оба переходят в "checking" (см. vGD_StatusSymbols_D — есть отдельный
#	статус "идёт проверка", отличный от "waiting"/"passed"/"failed") и ждут
#	ответа сервера через _fC_OnClientAppOpReceived_Ch
func _fC_OnServerConnected_Ch() -> void:
	if _vLB_ServerCheckResolved_Bv:
		return
	_vLB_ServerCheckResolved_Bv = true
	if vSD_AllNodes_D.has("InfoBoxAutonomousButton"):
		vSD_AllNodes_D["InfoBoxAutonomousButton"]["Node"].visible = false
	# 2026-08-24 (найден и починен баг "сервер подключился позже, чем должен
	# был — это ломает интерфейс"): раньше здесь пряталась только
	# InfoBoxAutonomousButton, а InfoBoxRetryButton — нет. Если подключение
	# затягивалось дольше SERVER_CONNECT_TIMEOUT_SECONDS_F (обе кнопки уже
	# показаны, см. _fC_OfferAutonomousMode_Ch), а потом ВСЁ ЖЕ проходило само
	# (см. _process — опрашивает состояние каждый кадр, даже после показа
	# кнопок), эта функция корректно прятала "Продолжить без сервера" и вела
	# чек-лист дальше — но "Повторить подключение" оставалась висеть и
	# кликабельной. Нажатие на неё после того, как сервер уже подключён,
	# заново дёргало _fC_StartClientConnection_Ch (см. её changelog про
	# "безопасно вызывать снова") — это откатывало "Server" на "checking" и,
	# при последующем повторном срабатывании этой же функции, сбрасывало
	# "Update"/"ThirdPartyFiles" обратно на "checking" тоже, даже если они уже
	# были "passed" — визуально "меняются галочки на всех трёх". Теперь обе
	# кнопки прячутся здесь разом, как только соединение реально установлено
	if vSD_AllNodes_D.has("InfoBoxRetryButton"):
		vSD_AllNodes_D["InfoBoxRetryButton"]["Node"].visible = false
	# 2026-08-24: таймер "Подключение к серверу…" (см. InfoBoxConnectingLabel)
	# свою роль отыграл — соединение установлено, дальше статус виден по
	# самому чек-листу
	if vSD_AllNodes_D.has("InfoBoxConnectingLabel"):
		vSD_AllNodes_D["InfoBoxConnectingLabel"]["Node"].visible = false
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnServerConnected_Ch: подключено", [], null)
	fC_SetCheckStatus_Ch("Server", "passed")
	fC_SetCheckStatus_Ch("Update", "checking")
	fC_SetCheckStatus_Ch("ThirdPartyFiles", "checking")
	_fC_SendUpdateCheckRequest_Ch()
	_fC_SendFileIntegrityCheckRequest_Ch()


#Функционал:
#	2026-08-23 (фаза 2): отправляет серверу Op "CheckUpdate" со своей версией
#	(APP_VERSION_S) — ответ ("CheckUpdateResult") придёт в
#	_fC_OnClientAppOpReceived_Ch → _fC_HandleUpdateCheckResult_Ch (см.
#	AppOpReceived_S, changelog Class_Internet.gd)
func _fC_SendUpdateCheckRequest_Ch() -> void:
	Class_InternetClient.fC_SendingMessage_Cr("CheckUpdate", {"Version": APP_VERSION_S}, {}, Class_InternetClient.SERVER_RECIPIENT_ID, true)


#Функционал:
#	2026-08-23 (фаза 2): строит отпечатки ВСЕХ проверяемых файлов (см.
#	Class_FileIntegrity.fC_ListCheckableFiles_Cr — "все не индивидуальные",
#	см. ответ на уточняющий вопрос про охват в
#	План проекта/Class_Internet.md) и отправляет Op "CheckFiles" — ответ
#	придёт в _fC_HandleFileIntegrityResult_Ch
func _fC_SendFileIntegrityCheckRequest_Ch() -> void:
	var vLA_Fingerprints_A := []
	for elLS_Path in Class_FileIntegrity.fC_ListCheckableFiles_Cr():
		var vLD_Fingerprint_D := Class_FileIntegrity.fC_BuildFingerprint_Cr(elLS_Path)
		if not vLD_Fingerprint_D.is_empty():
			vLA_Fingerprints_A.append(vLD_Fingerprint_D)
	Class_InternetClient.fC_SendingMessage_Cr("CheckFiles", {"Files": vLA_Fingerprints_A}, {}, Class_InternetClient.SERVER_RECIPIENT_ID, true)


#Функционал:
#	2026-08-23 (фаза 2): точка входа для всех прикладных ответов от сервера
#	(см. AppOpReceived_S, подключён в _fC_StartClientConnection_Ch) —
#	разбирает по Op ИСХОДНОГО запроса (см. её сигнатуру в Class_Internet.gd)
#Форматы данных:
#	Входные: см. Class_Internet.AppOpReceived_S
func _fC_OnClientAppOpReceived_Ch(vLS_Op_S: String, vLD_Envelope_D: Dictionary, _vLF_ReceivedNowAt_F: float, _vLS_ReplyRoutingKey_S: String) -> void:
	match vLS_Op_S:
		"CheckUpdate":
			_fC_HandleUpdateCheckResult_Ch(vLD_Envelope_D)
		"CheckFiles":
			_fC_HandleFileIntegrityResult_Ch(vLD_Envelope_D)
		"Register":
			_fC_HandleRegisterResult_Ch(vLD_Envelope_D)
		_:
			push_warning("⚠️ Start_Program.gd: неизвестная прикладная операция от сервера Op=%s" % vLS_Op_S)


#Функционал:
#	2026-08-23 (фаза 2): разбирает ответ сервера на "CheckUpdate" — при
#	совпадении версий "Update" отмечается пройденным, иначе — крестик с
#	сообщением о доступном обновлении (см. ответ на уточняющий вопрос в
#	План проекта/Class_Internet.md — "появляется несовпадение версии и
#	предложение обновления"). 2026-08-24 (правка): при провале теперь ТОЖЕ
#	предлагается автономный режим (см. _fC_ShowAutonomousModeButton_Ch) —
#	раньше при провале именно этой (или следующей) проверки чек-лист просто
#	зависал без какого-либо выхода
func _fC_HandleUpdateCheckResult_Ch(vLD_ReplyEnvelope_D: Dictionary) -> void:
	var vLD_Messages_D: Dictionary = vLD_ReplyEnvelope_D.get("Messages", {})
	if bool(vLD_Messages_D.get("UpToDate", false)):
		fC_SetCheckStatus_Ch("Update", "passed")
	else:
		var vLS_Latest_S: String = String(vLD_Messages_D.get("LatestVersion", "?"))
		fC_SetCheckStatus_Ch("Update", "failed", "Доступна новая версия %s (у вас %s) — рекомендуем обновиться." % [vLS_Latest_S, APP_VERSION_S])
		_fC_ShowAutonomousModeButton_Ch()


#Функционал:
#	2026-08-23 (фаза 2): разбирает ответ сервера на "CheckFiles" —
#	"ThirdPartyFiles" пройден, только если сервер подтвердил совпадение ПО
#	ВСЕМ присланным файлам (размер + случайный байт) и не нашёл лишних.
#	2026-08-24 (правка): при провале тоже предлагается автономный режим, см.
#	changelog _fC_HandleUpdateCheckResult_Ch выше — тот же принцип.
#	2026-08-24 (ревью интернет-кода): сервер теперь умеет находить ещё и
#	УДАЛЁННЫЕ файлы (см. changelog test.gd::_fC_HandleCheckFilesRequest_Ch —
#	новое поле "Missing" в ответе), сообщение на экране обновлено под это
func _fC_HandleFileIntegrityResult_Ch(vLD_ReplyEnvelope_D: Dictionary) -> void:
	var vLD_Messages_D: Dictionary = vLD_ReplyEnvelope_D.get("Messages", {})
	if bool(vLD_Messages_D.get("Ok", false)):
		fC_SetCheckStatus_Ch("ThirdPartyFiles", "passed")
	else:
		fC_SetCheckStatus_Ch("ThirdPartyFiles", "failed", "Обнаружены изменённые, посторонние или отсутствующие файлы.")
		_fC_ShowAutonomousModeButton_Ch()


#Функционал:
#	2026-08-24 (по просьбе пользователя "добавим... регистрацию для входа"):
#	разбирает ответ сервера на "Register" (см.
#	_fC_SendRegistrationRequest_Ch/Class_InternetServer._fL_OnRegisterRequest_Ch).
#	Успех: только сбрасывает _vLB_PendingRegistration_Bv — Class_InternetClient
#	остаётся в AUTHENTICATING (Register не меняет её состояние, это тот же
#	хендшейк, что и обычный вход), на СЛЕДУЮЩЕМ кадре _process уже пойдёт по
#	обычной ветке (_fC_SendStoredCredentials_Ch) с тем же ID/паролем — то есть
#	сразу логинимся под только что созданным аккаунтом, без дополнительного
#	действия пользователя. Провал: возвращаем окно выбора аккаунта с текстом
#	ошибки сервера (короткий пароль/занятый логин/регистрация выключена на
#	сервере — см. варианты в _fL_OnRegisterRequest_Ch) и НЕ пытаемся логиниться
#	дальше этой попыткой; соединение специально не закрывается — если
#	пользователь не нажмёт форму заново, через SERVER_CONNECT_TIMEOUT_SECONDS_F
#	сработает обычный таймаут (см. _process/_fC_OfferAutonomousMode_Ch)
#Форматы данных:
#	Входные: vLD_ReplyEnvelope_D: Dictionary — см. AppOpReceived_S
#	Выходные: нет
func _fC_HandleRegisterResult_Ch(vLD_ReplyEnvelope_D: Dictionary) -> void:
	var vLD_Messages_D: Dictionary = vLD_ReplyEnvelope_D.get("Messages", {})
	_vLB_PendingRegistration_Bv = false

	if bool(vLD_Messages_D.get("Ok", false)):
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_HandleRegisterResult_Ch: аккаунт создан на сервере, входим", [vLS_SelectedAccount_S], null)
		return

	var vLS_Error_S: String = String(vLD_Messages_D.get("Error", "Не удалось зарегистрироваться."))
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_HandleRegisterResult_Ch: отказ сервера", [vLS_Error_S], null)

	if vSD_AllNodes_D.has("InfoBoxConnectingLabel"):
		vSD_AllNodes_D["InfoBoxConnectingLabel"]["Node"].visible = false
	if vSD_AllNodes_D.has("InfoBoxLabel"):
		vSD_AllNodes_D["InfoBoxLabel"]["Node"].visible = false
	if vSD_AllNodes_D.has("AccountPickerSection"):
		vSD_AllNodes_D["AccountPickerSection"]["Node"].visible = true
	if vSD_AllNodes_D.has("AccountPickerAddForm"):
		vSD_AllNodes_D["AccountPickerAddForm"]["Node"].visible = true
	if vSD_AllNodes_D.has("AccountPickerScroll"):
		vSD_AllNodes_D["AccountPickerScroll"]["Node"].visible = false
	_fC_ShowAccountPickerMessage_Ch(vLS_Error_S)


#Функционал:
#	2026-08-24: показывает InfoBoxAutonomousButton — вынесена отдельно из
#	_fC_OfferAutonomousMode_Ch, потому что теперь её вызывают ТРИ разных места
#	(провал Server/Update/ThirdPartyFiles — любая из трёх последних проверок
#	чек-листа), а не только провал самого подключения
func _fC_ShowAutonomousModeButton_Ch() -> void:
	if vSD_AllNodes_D.has("InfoBoxAutonomousButton"):
		vSD_AllNodes_D["InfoBoxAutonomousButton"]["Node"].visible = true


#Функционал:
#	2026-08-23: подключение затянулось дольше SERVER_CONNECT_TIMEOUT_SECONDS_F
#	— показывает кнопку "Продолжить без сервера" (см. _fC_ShowAutonomousModeButton_Ch
#	выше). Class_InternetClient продолжает пытаться переподключиться сам по
#	себе (fC_Reconnect_Ch) — если получится ПОСЛЕ показа этой кнопки, но ДО
#	того, как пользователь её нажмёт, _process всё равно поймает
#	ConnectionState.CONNECTED и вызовет _fC_OnServerConnected_Ch как обычно
#	(см. её начало — сама скрывает эту кнопку). 2026-08-24 (обратная связь
#	"если провалилось первое из трёх [Server] — предлагаем ЕЩЁ повторить
#	подключение"): теперь рядом появляется ещё и InfoBoxRetryButton — ТОЛЬКО
#	здесь, при провале именно "Server" (см. _fC_OnRetryConnectionPressed_Ch) —
#	Update/ThirdPartyFiles эта кнопка не предлагают, повтор подключения их
#	не касается
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OfferAutonomousMode_Ch() -> void:
	_vLB_AutonomousModeOffered_Bv = true
	fC_SetCheckStatus_Ch("Server", "failed", "Не удалось подключиться к серверу.")
	_fC_ShowAutonomousModeButton_Ch()
	if vSD_AllNodes_D.has("InfoBoxRetryButton"):
		vSD_AllNodes_D["InfoBoxRetryButton"]["Node"].visible = true
	# 2026-08-24: таймер "Подключение к серверу…" (InfoBoxConnectingLabel)
	# прячется здесь же — попытка разрешилась (неудачей), дальше на его месте
	# кнопки "Повторить подключение"/"Продолжить без сервера"
	if vSD_AllNodes_D.has("InfoBoxConnectingLabel"):
		vSD_AllNodes_D["InfoBoxConnectingLabel"]["Node"].visible = false


#Функционал:
#	2026-08-24: нажатие "Повторить подключение" — только для провала "Server"
#	(см. её changelog в _fC_OfferAutonomousMode_Ch выше). Прячет обе кнопки,
#	переводит "Server" обратно в "checking" и запускает новую попытку через
#	уже существующий _fC_StartClientConnection_Ch (она сама сбрасывает флаги
#	_vLB_ServerCheckResolved_Bv/_vLB_CredentialsSent_Bv/_vLB_AutonomousModeOffered_Bv
#	и таймер таймаута — работает как первая попытка, просто вызванная повторно;
#	Class_InternetClient.fxG_EstablishingConnection_CrTr безопасно вызывать
#	снова — заново создаёт WebSocketPeer, старой попытке ничего не мешает).
#	2026-08-24 (доп. защита от бага "сервер подключился позже — ломает
#	интерфейс", см. changelog _fC_OnServerConnected_Ch): сама кнопка теперь
#	прячется, как только соединение реально устанавливается, так что в
#	обычных условиях нажать её ПОСЛЕ подключения уже нельзя — но на случай,
#	если это всё же произойдёт (например, клик и подтверждение подключения
#	случились в один и тот же кадр), добавлена защитная проверка ниже: если
#	Class_InternetClient УЖЕ в состоянии CONNECTED, повторное подключение не
#	запускается вообще — только прячутся кнопки, без сброса проверок
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OnRetryConnectionPressed_Ch() -> void:
	if vSD_AllNodes_D.has("InfoBoxRetryButton"):
		vSD_AllNodes_D["InfoBoxRetryButton"]["Node"].visible = false
	if vSD_AllNodes_D.has("InfoBoxAutonomousButton"):
		vSD_AllNodes_D["InfoBoxAutonomousButton"]["Node"].visible = false
	if Class_InternetClient.vGE_State == Class_InternetClient.ConnectionState.CONNECTED:
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnRetryConnectionPressed_Ch: уже подключены, повтор не нужен", [], null)
		return
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnRetryConnectionPressed_Ch: повторная попытка подключения", [], null)
	fC_SetCheckStatus_Ch("Server", "checking")
	_fC_StartClientConnection_Ch()


#Функционал:
#	2026-08-23: нажатие "Продолжить без сервера" — пользователь осознанно
#	продолжает без реального сервера. 2026-08-23 (правка, обратная связь "если
#	подключаемся без сети — то все крестики"): раньше "Server"/"Update"/
#	"ThirdPartyFiles" отмечались "passed" — визуально неотличимо от реально
#	пройденной проверки, хотя ничего не проверялось. Теперь непройденные из
#	этих трёх — "failed" (крестик), а разрешение продолжать без них —
#	отдельное правило именно для автономного режима в
#	_fC_UpdateOverallProgress_Ch (см. _vLB_AutonomousMode_Bv). 2026-08-24
#	(правка): УЖЕ пройденные из трёх (например "Server", если провалились
#	только "Update"/"ThirdPartyFiles") больше НЕ откатываются на "failed" —
#	раньше эта функция всегда ставила все три "failed" не глядя, что могло
#	перекрасить в крестик уже честно пройденную проверку
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OnAutonomousModePressed_Ch() -> void:
	if vSD_AllNodes_D.has("InfoBoxAutonomousButton"):
		vSD_AllNodes_D["InfoBoxAutonomousButton"]["Node"].visible = false
	if vSD_AllNodes_D.has("InfoBoxRetryButton"):
		vSD_AllNodes_D["InfoBoxRetryButton"]["Node"].visible = false
	_vLB_ServerCheckResolved_Bv = true
	_vLB_AutonomousMode_Bv = true
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnAutonomousModePressed_Ch: продолжаем без сервера", [], null)
	for elLS_Key in AUTONOMOUS_SKIPPED_CHECKS_A:
		if vLD_CheckStatuses_D.get(elLS_Key, "waiting") != "passed":
			fC_SetCheckStatus_Ch(elLS_Key, "failed")
	_fC_ShowInfo_Ch("Работаем в автономном режиме — непройденные проверки пропущены.")


#Функционал:
#	2026-08-23: переключает окно выбора аккаунта из режима "список кнопок" в
#	режим "форма добавления аккаунта" — обработчик всегда присутствующей
#	кнопки "+" (см. цикл в _fC_BuildScreenConfig_Cr). Прямая правка
#	видимости нод (как и у _fC_ShowAccountPicker_Ch выше) — данные ещё не
#	меняются, поэтому пересборка экрана (fC_SyncNodesWithConfig_Ch) здесь не
#	нужна
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OnAccountAddButtonPressed_Ch() -> void:
	if vSD_AllNodes_D.has("AccountPickerScroll"):
		vSD_AllNodes_D["AccountPickerScroll"]["Node"].visible = false
	if vSD_AllNodes_D.has("AccountPickerAddForm"):
		vSD_AllNodes_D["AccountPickerAddForm"]["Node"].visible = true
	_fC_ShowAccountPickerMessage_Ch("")


#Функционал:
#	2026-08-23: обработчик кнопки "Отмена" формы добавления аккаунта —
#	чистит поля и возвращает список кнопок аккаунтов, ничего не сохраняя
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OnAccountAddCancelPressed_Ch() -> void:
	_fC_ClearAccountAddForm_Ch()
	if vSD_AllNodes_D.has("AccountPickerAddForm"):
		vSD_AllNodes_D["AccountPickerAddForm"]["Node"].visible = false
	if vSD_AllNodes_D.has("AccountPickerScroll"):
		vSD_AllNodes_D["AccountPickerScroll"]["Node"].visible = true
	_fC_ShowAccountPickerMessage_Ch("")


#Функционал:
#	2026-08-24: общая валидация формы добавления аккаунта — вынесена из
#	_fC_OnAccountAddSavePressed_Ch, чтобы её могла переиспользовать новая
#	_fC_OnAccountAddRegisterPressed_Ch ("Зарегистрироваться", см. ниже) — те
#	же 3 поля, те же правила (не пусто, ник не зарезервирован, ник ещё не занят)
#Форматы данных:
#	Выходные: Dictionary — {"Ok": bool, "Nick": String, "Id": String, "Password": String, "Error": String}
func _fL_ReadAndValidateAccountAddForm_Cr() -> Dictionary:
	if not (vSD_AllNodes_D.has("AccountPickerAddNickLineEdit") and vSD_AllNodes_D.has("AccountPickerAddIdLineEdit") and vSD_AllNodes_D.has("AccountPickerAddPasswordLineEdit")):
		push_warning("⚠️ _fL_ReadAndValidateAccountAddForm_Cr: поля формы не найдены в реестре")
		return {"Ok": false, "Nick": "", "Id": "", "Password": "", "Error": "Поля формы не найдены."}

	var vLS_Nick_S: String = String(vSD_AllNodes_D["AccountPickerAddNickLineEdit"]["Node"].text).strip_edges()
	var vLS_Id_S: String = String(vSD_AllNodes_D["AccountPickerAddIdLineEdit"]["Node"].text).strip_edges()
	var vLS_Password_S: String = String(vSD_AllNodes_D["AccountPickerAddPasswordLineEdit"]["Node"].text)

	if vLS_Nick_S.is_empty() or vLS_Id_S.is_empty() or vLS_Password_S.is_empty():
		return {"Ok": false, "Nick": vLS_Nick_S, "Id": vLS_Id_S, "Password": vLS_Password_S, "Error": "Заполните ник, ID и пароль."}
	if vLS_Nick_S == ACCOUNT_TECHNICAL_SERVER_VALUE:
		return {"Ok": false, "Nick": vLS_Nick_S, "Id": vLS_Id_S, "Password": vLS_Password_S, "Error": "Имя \"%s\" зарезервировано, выберите другой ник." % ACCOUNT_TECHNICAL_SERVER_VALUE}
	if vLD_AccountsList_D.has(vLS_Nick_S):
		return {"Ok": false, "Nick": vLS_Nick_S, "Id": vLS_Id_S, "Password": vLS_Password_S, "Error": "Аккаунт с ником \"%s\" уже есть." % vLS_Nick_S}

	return {"Ok": true, "Nick": vLS_Nick_S, "Id": vLS_Id_S, "Password": vLS_Password_S, "Error": ""}


#Функционал:
#	2026-08-23: обработчик кнопки "Применить" формы добавления аккаунта —
#	читает Ник/ID/Пароль из полей, проверяет и, если всё в порядке, добавляет
#	аккаунт в vLD_AccountsList_D, сохраняет файл (_fC_SaveAccountsList_Ch) и
#	пересобирает экран (_fC_RefreshScreenAfterDataChange_Ch), чтобы новая
#	кнопка появилась СРАЗУ, без перезапуска программы
#Принцип работы:
#	⚠️ 2026-08-23, правка после переезда окна внутрь InfoBoxPanel: видимость
#	AccountPickerAddForm/AccountPickerScroll/AccountPickerSection выставляем
#	ПОСЛЕ пересборки, а не до. Раньше (когда список кнопок был отдельной
#	веткой без общего родителя с формой) это было не важно, но теперь и
#	список, и форма лежат ВНУТРИ AccountPickerSection — а fC_SyncNodesWithConfig_Ch
#	решает, пересоздавать ли ноду, по тому, отличается ли ЕЁ ПОДДЕРЕВО целиком
#	(старый/новый конфиг сравниваются рекурсивно, см. f__SyncCallback_CreateOrUpdate
#	в Class_UI.gd) — раз список кнопок внутри AccountPickerSection изменился,
#	САМА AccountPickerSection (и всё по цепочке вверх — InfoBoxVBox, InfoBoxPanel)
#	тоже считается "изменившейся" и пересоздаётся ЗАНОВО из статического
#	конфига (там "visible": false у AccountPickerSection) — то есть если
#	выставить видимость ДО пересборки, пересборка её тут же сотрёт, и окно
#	выбора аккаунта попросту исчезнет с экрана после сохранения. Поэтому
#	видимость возвращаем уже ПОСЛЕ, когда все ноды точно на месте
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OnAccountAddSavePressed_Ch() -> void:
	var vLD_Form_D := _fL_ReadAndValidateAccountAddForm_Cr()
	if not bool(vLD_Form_D.get("Ok", false)):
		_fC_ShowAccountPickerMessage_Ch(String(vLD_Form_D.get("Error", "")))
		return
	var vLS_Nick_S: String = vLD_Form_D["Nick"]
	var vLS_Id_S: String = vLD_Form_D["Id"]
	var vLS_Password_S: String = vLD_Form_D["Password"]

	vLD_AccountsList_D[vLS_Nick_S] = {"ID": vLS_Id_S, "Password": vLS_Password_S}
	_fC_SaveAccountsList_Ch(vLD_AccountsList_D)
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "AccountPicker: добавлен аккаунт " + vLS_Nick_S, [], null)

	_fC_ClearAccountAddForm_Ch()
	_fC_RefreshScreenAfterDataChange_Ch()

	# Пересборка выше могла пересоздать AccountPickerSection и всё внутри нею
	# (см. Принцип работы) — возвращаем нужное состояние видимости УЖЕ ПОСЛЕ,
	# работая с (возможно, уже новыми) нодами через vSD_AllNodes_D
	if vSD_AllNodes_D.has("AccountPickerSection"):
		vSD_AllNodes_D["AccountPickerSection"]["Node"].visible = true
	if vSD_AllNodes_D.has("AccountPickerAddForm"):
		vSD_AllNodes_D["AccountPickerAddForm"]["Node"].visible = false
	if vSD_AllNodes_D.has("AccountPickerScroll"):
		vSD_AllNodes_D["AccountPickerScroll"]["Node"].visible = true
	if vSD_AllNodes_D.has("InfoBoxLabel"):
		vSD_AllNodes_D["InfoBoxLabel"]["Node"].visible = false
	_fC_ShowAccountPickerMessage_Ch("Аккаунт \"%s\" добавлен." % vLS_Nick_S)


#Функционал:
#	2026-08-24 (по просьбе пользователя "добавим... регистрацию для входа"):
#	обработчик кнопки "Зарегистрироваться" формы добавления аккаунта. В
#	отличие от "Применить" (только локальная запись — сервер вообще не в
#	курсе этого логина/пароля заранее), эта кнопка сразу создаёт УЧЁТНУЮ
#	ЗАПИСЬ НА СЕРВЕРЕ: сохраняет аккаунт локально (тот же ID/пароль нужен
#	дальше для обычного входа — см. _fC_StartClientConnection_Ch), затем
#	запускает подключение; как только Class_InternetClient дойдёт до
#	AUTHENTICATING, _process (см. её ветку выше) пошлёт Op "Register" вместо
#	обычного AuthRequest — ответ обрабатывает _fC_HandleRegisterResult_Ch.
#	⚠️ Пароль при регистрации идёт на сервер ОДНИМ ПАКЕТОМ В ОТКРЫТОМ ВИДЕ
#	(в отличие от обычного входа — там challenge-response, пароль по сети не
#	передаётся, см. Class_InternetClient.fC_SendCredentials_Ch) — для этого
#	пути обязателен WSS (см. Class_InternetServer.fC_ConfigureTls_Bv), иначе
#	пароль виден по сети как есть (см. changelog Class_InternetClient.fC_RegisterAccount_Ch)
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_OnAccountAddRegisterPressed_Ch() -> void:
	var vLD_Form_D := _fL_ReadAndValidateAccountAddForm_Cr()
	if not bool(vLD_Form_D.get("Ok", false)):
		_fC_ShowAccountPickerMessage_Ch(String(vLD_Form_D.get("Error", "")))
		return
	var vLS_Nick_S: String = vLD_Form_D["Nick"]
	var vLS_Id_S: String = vLD_Form_D["Id"]
	var vLS_Password_S: String = vLD_Form_D["Password"]

	vLD_AccountsList_D[vLS_Nick_S] = {"ID": vLS_Id_S, "Password": vLS_Password_S}
	_fC_SaveAccountsList_Ch(vLD_AccountsList_D)
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "AccountPicker: регистрация нового аккаунта на сервере " + vLS_Nick_S, [], null)

	_fC_ClearAccountAddForm_Ch()
	vLS_SelectedAccount_S = vLS_Nick_S
	_vLB_PendingRegistration_Bv = true

	# Пересборка (см. подробный разбор в _fC_OnAccountAddSavePressed_Ch выше) —
	# новый аккаунт должен появиться в списке кнопок НА СЛУЧАЙ, если
	# регистрация на сервере провалится и мы вернём окно выбора аккаунта назад
	# (см. _fC_HandleRegisterResult_Ch); пересборка попутно возвращает нужные
	# нам умолчания видимости (AccountPickerSection скрыт, InfoBoxLabel
	# виден) — лишние ручные fixup'ы здесь не нужны
	_fC_RefreshScreenAfterDataChange_Ch()

	fC_SetCheckStatus_Ch("Server", "checking")
	_fC_StartClientConnection_Ch()


#Функционал:
#	Очищает 3 поля формы добавления аккаунта — общий хвост для Отмены и
#	успешного Применить (см. _fC_OnAccountAddCancelPressed_Ch/
#	_fC_OnAccountAddSavePressed_Ch)
func _fC_ClearAccountAddForm_Ch() -> void:
	for elLS_Key in ["AccountPickerAddNickLineEdit", "AccountPickerAddIdLineEdit", "AccountPickerAddPasswordLineEdit"]:
		if vSD_AllNodes_D.has(elLS_Key):
			vSD_AllNodes_D[elLS_Key]["Node"].text = ""


#Функционал:
#	2026-08-23: выводит текст в AccountPickerMessageLabel (ошибки валидации
#	формы добавления/подтверждение "аккаунт добавлен") — ОТДЕЛЬНО от общего
#	_fC_ShowInfo_Ch/InfoBoxLabel, потому что InfoBoxLabel скрыт всё время,
#	пока открыто окно выбора аккаунта (см. _fC_ShowAccountPicker_Ch), и
#	сообщение туда никто бы не увидел
#Форматы данных:
#	Входные: vLS_Text_S: String — текст сообщения, "" чтобы очистить
#	Выходные: нет
func _fC_ShowAccountPickerMessage_Ch(vLS_Text_S: String) -> void:
	if vSD_AllNodes_D.has("AccountPickerMessageLabel"):
		vSD_AllNodes_D["AccountPickerMessageLabel"]["Node"].text = vLS_Text_S


#Функционал:
#	2026-08-24: заглушка для входа через сторонние аккаунты (ВК/Google/
#	Яндекс) — по просьбе пользователя ("предусмотреть вход через сторонние
#	аккаунты"). Архитектура спроектирована (см. План проекта/OAuth сторонние
#	аккаунты.md), но живой OAuth-поток НЕ подключён: он требует
#	client_id/client_secret, зарегистрированные РАЗРАБОТЧИКОМ в консоли
#	каждого провайдера (ВК/Google/Яндекс) — это может сделать только сам
#	владелец проекта, заранее в код это не зашить. Кнопка честно объясняет
#	это вместо того, чтобы делать вид, что вход работает
#Форматы данных:
#	Входные: vLS_Provider_S: String — "VK"/"Google"/"Yandex"
#	Выходные: нет
func _fC_OnOAuthButtonPressed_Ch(vLS_Provider_S: String) -> void:
	_fC_ShowAccountPickerMessage_Ch("Вход через %s ещё не подключён — нужны учётные данные приложения от %s (см. План проекта/OAuth сторонние аккаунты.md)." % [vLS_Provider_S, vLS_Provider_S])
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "AccountPicker: нажата заглушка OAuth", [vLS_Provider_S], null)


#Функционал:
#	2026-08-23: пересобирает конфиг экрана под ТЕКУЩУЮ ориентацию и передаёт
#	старый/новый конфиг в Class_UI.fC_SyncNodesWithConfig_Ch — та же самая
#	идея, что и у fC_SwitchOrientation_Ch (см. её changelog), но повод другой:
#	не сменилась ориентация, а изменились данные (vLD_AccountsList_D), из
#	которых строится часть конфига. Вынесена отдельно, чтобы этот приём
#	можно было переиспользовать для любых будущих "живых" изменений данных,
#	а не только для аккаунтов. ⚠️ Не восстанавливает runtime-видимость
#	пересозданных нод сама — это ответственность вызывающего кода (см.
#	changelog _fC_OnAccountAddSavePressed_Ch)
#Форматы данных:
#	Входные: нет
#	Выходные: нет
func _fC_RefreshScreenAfterDataChange_Ch() -> void:
	var vXD_NewConfig_D: Dictionary = _fC_BuildScreenConfig_Cr(vLS_Orientation_S)
	Class_UI.fC_SyncNodesWithConfig_Ch(vSD_AllNodes_D, vXD_CurrentScreenConfig_D, vXD_NewConfig_D)
	vXD_CurrentScreenConfig_D = vXD_NewConfig_D


#Функционал:
#	Выводит текст в окно информации (блок 4)
func _fC_ShowInfo_Ch(vLS_Text: String) -> void:
	if vSD_AllNodes_D.has("InfoBoxLabel"):
		vSD_AllNodes_D["InfoBoxLabel"]["Node"].text = vLS_Text


## Ключи проверок, которые в автономном режиме допустимо оставить "failed" и
## всё равно считать общий прогресс пройденным — см. _fC_UpdateOverallProgress_Ch
## и _fC_OnAutonomousModePressed_Ch (обратная связь "если без сети — все крестики")
const AUTONOMOUS_SKIPPED_CHECKS_A := ["Server", "Update", "ThirdPartyFiles"]

#Функционал:
#	Пересчитывает общий прогресс по всем 5 проверкам: если все "passed" —
#	показывает кнопку "Войти" и сообщение об успехе в окне информации.
#	2026-08-23 (правка): в автономном режиме (_vLB_AutonomousMode_Bv)
#	"Server"/"Update"/"ThirdPartyFiles" намеренно "failed" (крестик, см.
#	_fC_OnAutonomousModePressed_Ch), а не "passed" — для них делается
#	исключение из требования "все passed", иначе чек-лист завис бы навсегда.
#	Сообщение об успехе в этом случае НЕ показывается — его уже показал
#	_fC_OnAutonomousModePressed_Ch, и оно точнее ("проверки пропущены", а не
#	"все проверки пройдены" — это было бы неправдой при крестиках на экране)
func _fC_UpdateOverallProgress_Ch() -> void:
	var vLB_AllPassed := true
	for elLS_Key in vLA_CheckOrder_A:
		if vLD_CheckStatuses_D.get(elLS_Key, "waiting") == "passed":
			continue
		if _vLB_AutonomousMode_Bv and elLS_Key in AUTONOMOUS_SKIPPED_CHECKS_A:
			continue
		vLB_AllPassed = false
		break

	if vLB_AllPassed and vSD_AllNodes_D.has("InfoBoxProceedButton"):
		vSD_AllNodes_D["InfoBoxProceedButton"]["Node"].visible = true
		if not _vLB_AutonomousMode_Bv:
			_fC_ShowInfo_Ch("Все проверки пройдены — можно продолжать.")


#Функционал:
#	Нажатие "Войти" — завершает поток логов сцены и переключается на Home
func _fC_OnProceedPressed_Ch() -> void:
	Class_Logger.fC_FinishStream_Ch(vLS_StreamId)
	Class_UI.fxC_SceneSwitching("res://Scenes/UIScenes/Home/Home.tscn")


#Функционал:
#	Открывает/закрывает общее модальное окно кнопок поддержки/настроек —
#	вызывается только нажатием SupportButton/SettingsButton (та же кнопка
#	закрывает то, что открыла). Закрытие по крестику и по клику на затемнённую
#	область идёт через отдельный _fC_CloseModal_Ch (см. ниже) — общая логика
#	закрытия вынесена туда, чтобы не дублировать её в трёх местах
#Форматы данных:
#	Входные:
#		vLS_Source: String — "Support" или "Settings", кто вызвал переключение
#Принцип работы:
#	⚠️ 2026-08-19, третья правка ("не размывается кнопка настроек"): раньше
#	кнопка-источник поднималась НАД оверлеем через z_index
#	(_fC_SetActiveTopButtonRaised_Ch), чтобы по ней же можно было кликнуть
#	повторно и закрыть окно — но из-за этого именно эта кнопка оставалась
#	не затемнённой и не размытой, пока всё остальное (включая ВТОРУЮ кнопку)
#	уходило под затемнение/размытие ModalOverlay. Теперь, когда закрытие есть
#	ещё и по крестику (_fC_CloseModal_Ch), и по клику мимо окна
#	(_fC_OnOverlayGuiInput_Ch), поднимать кнопку-источник больше не нужно —
#	_fC_SetActiveTopButtonRaised_Ch удалена, ОБЕ кнопки всегда лежат позади
#	ModalOverlay, пока окно открыто, и одинаково затемняются/размываются.
#	Следствие: повторный клик по ТОЙ ЖЕ кнопке, что открыла окно, теперь НЕ
#	закрывает его (клик просто не доходит — перехвачен оверлеем) — закрытие
#	только через крестик или клик по затемнённому/размытому фону
func _fC_ToggleModal_Ch(vLS_Source: String) -> void:
	if not vSD_AllNodes_D.has("ModalOverlay"):
		return

	if vLB_ModalOpen_Bv and vLS_ModalSource_S == vLS_Source:
		_fC_CloseModal_Ch()
	elif not vLB_ModalOpen_Bv:
		_fC_OpenModal_Ch(vLS_Source)
	# else: окно уже открыто ДРУГИМ источником — сюда прийти не должны, т.к.
	# "чужая" кнопка сейчас позади ModalOverlay по z-порядку и не получает клики


#Функционал:
#	Открывает окно от указанного источника: подставляет заголовок (ключ
#	перевода — см. п.8 ТЗ/changelog Class_Localization.gd), показывает
#	категории настроек только у Settings и обновляет видимость содержимого
#	под активную категорию
#Форматы данных:
#	Входные:
#		vLS_Source: String — "Support" или "Settings"
func _fC_OpenModal_Ch(vLS_Source: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OpenModal_Ch", [vLS_Source], null)
	if vSD_AllNodes_D.has("ModalWindowLabel"):
		vSD_AllNodes_D["ModalWindowLabel"]["Node"].text = "SUPPORT_TITLE" if vLS_Source == "Support" else "SETTINGS_TITLE"
	if vSD_AllNodes_D.has("SettingsCategoriesScroll"):
		# Категории (Язык/Экран/Звук/Подтверждения/Шрифт) нужны только у настроек,
		# не у поддержки. ⚠️ 2026-08-20: скрывается теперь ScrollContainer-обёртка
		# (SettingsCategoriesScroll), а не сам SettingsCategoriesVBox — иначе при
		# видимом, но пустом ScrollContainer'е (когда его единственный ребёнок
		# invisible) под окном поддержки осталась бы пустая полоса-заглушка
		vSD_AllNodes_D["SettingsCategoriesScroll"]["Node"].visible = (vLS_Source == "Settings")
	if vLS_Source == "Settings":
		# 2026-08-19: базовая точка отсчёта "хочу применить" — берём РЕАЛЬНО
		# сохранённое (не то, что могло остаться от прошлого незавершённого
		# редактирования), чтобы, например, "тронул только ширину" не утащил
		# за собой случайный pending fullscreen от предыдущего открытия
		vLI_PendingScreenWidth_I = int(vXD_Settings_D.get("ScreenWidth", vSV2_Scene_size_V2i.x))
		vLI_PendingScreenHeight_I = int(vXD_Settings_D.get("ScreenHeight", vSV2_Scene_size_V2i.y))
		vLB_PendingFullscreen_Bv = vXD_Settings_D.get("Fullscreen", false)
		vLB_ScreenSizeDirty_Bv = false
	vSD_AllNodes_D["ModalOverlay"]["Node"].visible = true
	vLB_ModalOpen_Bv = true
	vLS_ModalSource_S = vLS_Source
	_fC_UpdateModalContentVisibility_Ch()


#Функционал:
#	Закрывает окно Поддержка/Настройки — общая точка для двух способов
#	закрытия (крестик — ModalCloseButton.pressed; клик по затемнённой/
#	размытой области — _fC_OnOverlayGuiInput_Ch), чтобы не дублировать сброс
#	состояния дважды
#	⚠️ 2026-08-19: если размер экрана менялся (vLB_ScreenSizeDirty_Bv), но ещё
#	не применён — настройки НЕ закрываются сразу. Вместо этого либо показывается
#	окно "Применить/Отмена" (и функция выходит, реальное закрытие произойдёт
#	ПОВТОРНЫМ вызовом этой же функции из _fC_CommitScreenSizeChange_Ch/
#	_fC_DiscardPendingScreenSizeChange_Ch, когда vLB_ScreenSizeDirty_Bv уже
#	false), либо — если сам диалог отключён в категории "Подтверждения" —
#	размер применяется тут же молча, без диалога, и закрытие продолжается как
#	обычно (см. changelog "окно применить/отмена")
func _fC_CloseModal_Ch() -> void:
	if not vLB_ModalOpen_Bv or not vSD_AllNodes_D.has("ModalOverlay"):
		return

	if vLB_ScreenSizeDirty_Bv:
		if vXD_Settings_D.get("ShowScreenApplyConfirmPopup", true):
			_fC_ShowScreenApplyConfirm_Ch()
			return
		else:
			vLB_ScreenSizeDirty_Bv = false
			_fC_ApplyScreenSize_Ch(vLI_PendingScreenWidth_I, vLI_PendingScreenHeight_I, vLB_PendingFullscreen_Bv)

	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_CloseModal_Ch", [vLS_ModalSource_S], null)
	vSD_AllNodes_D["ModalOverlay"]["Node"].visible = false
	vLB_ModalOpen_Bv = false
	vLS_ModalSource_S = ""


#Функционал:
#	Обработчик gui_input на самом ModalOverlay (п.2 ТЗ: клик по прозрачной
#	затемнённой части тоже закрывает окно). Клик по самой ModalWindowPanel сюда
#	не доходит — Control останавливает обработку события на первом, кто его
#	принял (Panel сама перехватывает клик, дальше он не идёт), поэтому этот
#	обработчик срабатывает только на настоящем клике МИМО окна
#	⚠️ 2026-08-20 — БАГ НАЙДЕН И ПОЧИНЕН: "окно закрывается при вращении
#	колесика мыши где угодно". Причина — InputEventMouseButton в Godot
#	описывает НЕ ТОЛЬКО обычные клики (ЛКМ/ПКМ/средняя), но и прокрутку
#	колёсика (button_index MOUSE_BUTTON_WHEEL_UP/DOWN/LEFT/RIGHT), у которой
#	тоже выставлен pressed=true на каждый "тик". Старая проверка смотрела
#	ТОЛЬКО на .pressed, поэтому любой тик колёсика над прозрачной областью
#	(а она на весь экран, см. ModalOverlay) читался как "клик мимо окна" и
#	закрывал Настройки/Поддержку. Исправлено — теперь дополнительно
#	проверяется button_index: закрытие срабатывает только на настоящих
#	кнопках мыши (ЛКМ/ПКМ/средняя), колёсико игнорируется
#Форматы данных:
#	Входные:
#		vL_Event: InputEvent — событие от gui_input
func _fC_OnOverlayGuiInput_Ch(vL_Event: InputEvent) -> void:
	if not (vL_Event is InputEventMouseButton) or not vL_Event.pressed:
		return
	# Только настоящие кнопки мыши — колёсико (WHEEL_UP/DOWN/LEFT/RIGHT) НЕ
	# должно закрывать окно, см. changelog выше
	var vLA_RealClickButtons_A: Array = [MOUSE_BUTTON_LEFT, MOUSE_BUTTON_RIGHT, MOUSE_BUTTON_MIDDLE]
	if not vLA_RealClickButtons_A.has(vL_Event.button_index):
		return
	_fC_CloseModal_Ch()


#Функционал:
#	Подставляет уже загруженные настройки (vXD_Settings_D) в поля окна
#	Настройки сразу после его сборки — поля ширины/высоты, состояние кнопки
#	fullscreen, положение и подписи ползунков звука. Без этого при каждом
#	открытии окна поля показывали бы значения по умолчанию из конфига, а не
#	то, что реально сохранено/применено к окну ОС
func _fC_ApplySettingsToUI_Ch() -> void:
	if vSD_AllNodes_D.has("ScreenWidthLineEdit"):
		vSD_AllNodes_D["ScreenWidthLineEdit"]["Node"].text = str(int(vXD_Settings_D.get("ScreenWidth", vSV2_Scene_size_V2i.x)))
	if vSD_AllNodes_D.has("ScreenHeightLineEdit"):
		vSD_AllNodes_D["ScreenHeightLineEdit"]["Node"].text = str(int(vXD_Settings_D.get("ScreenHeight", vSV2_Scene_size_V2i.y)))
	if vSD_AllNodes_D.has("ScreenFullscreenButton"):
		vSD_AllNodes_D["ScreenFullscreenButton"]["Node"].button_pressed = vXD_Settings_D.get("Fullscreen", false)
	if vSD_AllNodes_D.has("ScreenFontScaleLineEdit"):
		vSD_AllNodes_D["ScreenFontScaleLineEdit"]["Node"].text = "%.2f" % float(vXD_Settings_D.get("FontScale", 1.0))

	if vSD_AllNodes_D.has("SoundMusicSlider"):
		vSD_AllNodes_D["SoundMusicSlider"]["Node"].value = vXD_Settings_D.get("MusicVolume", 100)
	if vSD_AllNodes_D.has("SoundMusicValueLabel"):
		vSD_AllNodes_D["SoundMusicValueLabel"]["Node"].text = "%d%%" % int(vXD_Settings_D.get("MusicVolume", 100))
	if vSD_AllNodes_D.has("SoundInterfaceSlider"):
		vSD_AllNodes_D["SoundInterfaceSlider"]["Node"].value = vXD_Settings_D.get("InterfaceVolume", 100)
	if vSD_AllNodes_D.has("SoundInterfaceValueLabel"):
		vSD_AllNodes_D["SoundInterfaceValueLabel"]["Node"].text = "%d%%" % int(vXD_Settings_D.get("InterfaceVolume", 100))

	# 2026-08-19: цветные переключатели категории "Подтверждения" — начальный
	# цвет/подпись под реально сохранённое состояние (см. changelog "окно
	# применить/отмена"), тем же способом, что и остальные поля выше
	_fC_UpdateConfirmToggleVisual_Ch("ConfirmScreenInfoToggle", vXD_Settings_D.get("ShowScreenChangeInfoPopup", true))
	_fC_UpdateConfirmToggleVisual_Ch("ConfirmScreenApplyToggle", vXD_Settings_D.get("ShowScreenApplyConfirmPopup", true))
	_fC_UpdateConfirmToggleVisual_Ch("ConfirmLoggerInfoToggle", vXD_Settings_D.get("ShowLoggerRestartInfoPopup", true))

	# 2026-08-19: поля раздела "Автоматическое нажатие человеком"
	# 2026-08-20: были ползунками (HSlider) — по обратной связи переведены на
	# свободный ввод текста (LineEdit), см. changelog у обработчиков ниже
	var vLF_ProtectionSeconds_F: float = vXD_Settings_D.get("AutoClickProtectionSeconds", 0.5)
	if vSD_AllNodes_D.has("ConfirmAutoClickProtectionLineEdit"):
		vSD_AllNodes_D["ConfirmAutoClickProtectionLineEdit"]["Node"].text = "%.1f" % vLF_ProtectionSeconds_F
	var vLI_DodgeLimit_I: int = int(vXD_Settings_D.get("AutoClickDodgeLimit", 1))
	if vSD_AllNodes_D.has("ConfirmAutoClickDodgeLimitLineEdit"):
		vSD_AllNodes_D["ConfirmAutoClickDodgeLimitLineEdit"]["Node"].text = "%d" % vLI_DodgeLimit_I

	# 2026-08-22: категория "Логи и реплеи" — 6 одинаковых по структуре строк
	# (Enabled-ключ, числовой ключ, ключ ноды-галочки, ключ ноды-поля), поэтому
	# циклом вместо 6 копипаст-блоков — см. vGD_LoggerRows_A (общий список,
	# переиспользуется и в _fC_OnLoggerLimitToggled_Ch/_fC_OnLoggerLimitSubmitted_Ch
	# не нужен, они уже параметризованы через .bind(), список нужен только здесь)
	for elLA_Row_A in vGD_LoggerRows_A:
		_fC_UpdateLoggerCheckVisual_Ch(elLA_Row_A[2], vXD_Settings_D.get(elLA_Row_A[0], true))
		if vSD_AllNodes_D.has(elLA_Row_A[3]):
			vSD_AllNodes_D[elLA_Row_A[3]]["Node"].text = "%d" % int(vXD_Settings_D.get(elLA_Row_A[1], 0))


#Функционал:
#	Показывает содержимое, соответствующее источнику окна (Support — только
#	SupportContentVBox) и активной категории настроек (Language/Screen/Sound —
#	только у Settings), прячет остальное. Единая точка обновления видимости —
#	вызывается и при открытии окна, и при переключении категории
func _fC_UpdateModalContentVisibility_Ch() -> void:
	var vLB_IsSupport_Bv: bool = (vLS_ModalSource_S == "Support")

	if vSD_AllNodes_D.has("SupportContentVBox"):
		vSD_AllNodes_D["SupportContentVBox"]["Node"].visible = vLB_IsSupport_Bv
	if vSD_AllNodes_D.has("LanguageContentVBox"):
		vSD_AllNodes_D["LanguageContentVBox"]["Node"].visible = not vLB_IsSupport_Bv and vLS_ActiveSettingsCategory_S == "Language"
	if vSD_AllNodes_D.has("ScreenContentVBox"):
		vSD_AllNodes_D["ScreenContentVBox"]["Node"].visible = not vLB_IsSupport_Bv and vLS_ActiveSettingsCategory_S == "Screen"
	if vSD_AllNodes_D.has("SoundContentVBox"):
		vSD_AllNodes_D["SoundContentVBox"]["Node"].visible = not vLB_IsSupport_Bv and vLS_ActiveSettingsCategory_S == "Sound"
	if vSD_AllNodes_D.has("ConfirmationsContentVBox"):
		vSD_AllNodes_D["ConfirmationsContentVBox"]["Node"].visible = not vLB_IsSupport_Bv and vLS_ActiveSettingsCategory_S == "Confirmations"
	if vSD_AllNodes_D.has("LoggerContentVBox"):
		vSD_AllNodes_D["LoggerContentVBox"]["Node"].visible = not vLB_IsSupport_Bv and vLS_ActiveSettingsCategory_S == "Logger"


#Функционал:
#	Нажатие одной из кнопок-категорий (Язык/Экран/Звук) — они toggle-кнопки в
#	общем ButtonGroup (_vGC_CategoryButtonGroup), поэтому "toggled" приходит
#	дважды при переключении (false у той, что отжалась, true у новой) — нас
#	интересует только true, иначе vLS_ActiveSettingsCategory_S откатится назад
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние кнопки (сигнал toggled)
#		vLS_Category: String — "Language"/"Screen"/"Sound", привязано через .bind()
func _fC_OnSettingsCategoryToggled_Ch(vLB_Pressed: bool, vLS_Category: String) -> void:
	if not vLB_Pressed:
		return
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnSettingsCategoryToggled_Ch", [vLS_Category], null)
	vLS_ActiveSettingsCategory_S = vLS_Category
	_fC_UpdateModalContentVisibility_Ch()


#Функционал:
#	Перестраивает список языков в LanguageListVBox под текущий текст поиска.
#	⚠️ Единственное место экрана, где ноды создаются НАПРЯМУЮ (Button.new()),
#	а не через Class_UI/конфиг-словарь — список зависит от текста поиска и
#	меняется на каждое нажатие клавиши, статическим конфигом это не описать.
#	Эти кнопки НЕ попадают в vSD_AllNodes_D (не часть статического дерева
#	экрана) — это осознанно, см. _fC_CountNodeConfigs_I/_fC_VerifyInterfaceBuilt_Bv,
#	которые считают только то, что в конфиге, и не должны знать про этот список
#Форматы данных:
#	Входные:
#		vLS_Query_S: String — текущий текст в LanguageSearchLineEdit (может быть "")
#Принцип работы:
#	1. Удаляет все текущие дочерние кнопки списка
#	2. Получает отфильтрованный список локалей через
#	   Class_Localization.fC_FilterLocalesByInputScript_A
#	3. На каждую локаль — toggle-кнопка с текстом-ключом перевода (LANG_RU и
#	   т.п.), общий ButtonGroup (_vGC_LanguageButtonGroup), нажатое состояние
#	   выставлено у текущего выбранного языка (vXD_Settings_D["Locale"])
func _fC_RebuildLanguageList_Ch(vLS_Query_S: String) -> void:
	if not vSD_AllNodes_D.has("LanguageListVBox"):
		return
	var vL_ListNode: VBoxContainer = vSD_AllNodes_D["LanguageListVBox"]["Node"]
	for elL_Child in vL_ListNode.get_children():
		elL_Child.queue_free()

	var vLD_LocaleLabelKeys_D := {
		"ru": "LANG_RU", "en": "LANG_EN", "de": "LANG_DE", "fr": "LANG_FR", "es": "LANG_ES",
	}
	var vLS_CurrentLocale_S: String = vXD_Settings_D.get("Locale", "")
	var vLA_Matches_A: Array = Class_Localization.fC_FilterLocalesByInputScript_A(vLS_Query_S)
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_RebuildLanguageList_Ch", [vLS_Query_S, vLA_Matches_A.duplicate()], null)

	for elLS_Locale in vLA_Matches_A:
		var vL_Button := Button.new()
		vL_Button.text = vLD_LocaleLabelKeys_D.get(elLS_Locale, elLS_Locale)
		vL_Button.toggle_mode = true
		vL_Button.button_group = _vGC_LanguageButtonGroup
		vL_Button.button_pressed = (elLS_Locale == vLS_CurrentLocale_S)
		# Эти кнопки создаются напрямую (не через _fC_NodeCfg), поэтому размер
		# шрифта им нужно выставить отдельно — тем же стандартизированным
		# уровнем, что и у остального текста (см. changelog 2026-08-19 про
		# зависимость шрифта от размера окна)
		vL_Button.add_theme_font_size_override("font_size", _fC_FontSize_I("Body"))
		vL_Button.pressed.connect(Callable(self, "_fC_OnLanguageOptionPressed_Ch").bind(elLS_Locale))
		vL_ListNode.add_child(vL_Button)


#Функционал:
#	text_changed у LanguageSearchLineEdit — просто перестраивает список под
#	новый текст поиска
func _fC_OnLanguageSearchChanged_Ch(vLS_NewText_S: String) -> void:
	_fC_RebuildLanguageList_Ch(vLS_NewText_S)


#Функционал:
#	Нажатие на конкретный язык в списке — переключает locale движка (все
#	переводимые надписи на экране перерисовываются сами, см. changelog
#	Class_Localization.gd про auto_translate_mode) и сохраняет выбор в
#	Settings.json (п.5 ТЗ: "пока просто сохраняя его в настройках")
#Форматы данных:
#	Входные:
#		vLS_Locale: String — код локали ("ru"/"en"/...), привязан через .bind()
func _fC_OnLanguageOptionPressed_Ch(vLS_Locale: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnLanguageOptionPressed_Ch", [vLS_Locale], null)
	Class_Localization.fC_SetLocale_Ch(vLS_Locale)
	vXD_Settings_D["Locale"] = vLS_Locale
	_fC_SaveSettings_Ch(vXD_Settings_D)


#Функционал:
#	Enter ИЛИ потеря фокуса в поле ширины/высоты (и text_submitted, и
#	focus_exited подключены к одной и той же функции для обоих полей, см.
#	changelog 2026-08-19 п.2 — раньше срабатывало только по Enter) —
#	перечитывает ОБА поля напрямую, а не полагается на переданный текст (он от
#	того поля, где сработал сигнал, а нужны оба сразу)
#	⚠️ 2026-08-19: больше НЕ применяет размер сразу (было _fC_ApplyScreenSize_Ch
#	напрямую) — теперь только копит "хочу применить" через
#	_fC_QueueScreenSizeChange_Ch, реальное применение — при закрытии настроек
#	(см. changelog про окно "Применить/Отмена")
#Форматы данных:
#	Входные:
#		vLS_SubmittedText_S: String — текст/пустая строка от сработавшего
#			сигнала, не используется напрямую — см. выше
func _fC_OnScreenSizeChanged_Ch(vLS_SubmittedText_S: String) -> void:
	if not vSD_AllNodes_D.has("ScreenWidthLineEdit") or not vSD_AllNodes_D.has("ScreenHeightLineEdit"):
		return
	# strip_edges — иначе случайный пробел (частый при копипасте значения)
	# делает is_valid_int() ложноотрицательным, и поле выглядит "сломанным"
	var vLS_WidthText_S: String = vSD_AllNodes_D["ScreenWidthLineEdit"]["Node"].text.strip_edges()
	var vLS_HeightText_S: String = vSD_AllNodes_D["ScreenHeightLineEdit"]["Node"].text.strip_edges()
	if not vLS_WidthText_S.is_valid_int() or not vLS_HeightText_S.is_valid_int():
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenSizeChanged_Ch: invalid input, ignored", [vLS_WidthText_S, vLS_HeightText_S], null)
		return

	var vLI_Width_I: int = vLS_WidthText_S.to_int()
	var vLI_Height_I: int = vLS_HeightText_S.to_int()
	if vLI_Width_I <= 0 or vLI_Height_I <= 0:
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenSizeChanged_Ch: non-positive size, ignored", [vLI_Width_I, vLI_Height_I], null)
		return
	_fC_QueueScreenSizeChange_Ch(vLI_Width_I, vLI_Height_I)


#Функционал:
#	Enter ИЛИ потеря фокуса в поле "размер шрифта" (категория "Экран") —
#	сохраняет множитель в настройки и СРАЗУ пересчитывает все шрифты на экране
#	(_fC_RescaleFonts_Ch, тот же метод, что и при живом изменении размера окна).
#	⚠️ НЕ идёт через отложенный apply/cancel механизм ширины/высоты/полного
#	экрана — размер шрифта не входит в список настроек, требующих подтверждения
#	(см. changelog 2026-08-19 "список настроек которые будут вызывать
#	срабатывания этого окна" — там только ширина/высота/пресеты/fullscreen)
#Форматы данных:
#	Входные:
#		vLS_SubmittedText_S: String — текст/пустая строка от сработавшего
#			сигнала, не используется напрямую — поле перечитывается само
func _fC_OnFontScaleSubmitted_Ch(vLS_SubmittedText_S: String) -> void:
	if not vSD_AllNodes_D.has("ScreenFontScaleLineEdit"):
		return
	var vLS_Text_S: String = vSD_AllNodes_D["ScreenFontScaleLineEdit"]["Node"].text.strip_edges()
	if not vLS_Text_S.is_valid_float():
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnFontScaleSubmitted_Ch: invalid input, ignored", [vLS_Text_S], null)
		return
	# 0.5..3.0 — страховка от нечитаемо мелкого/чудовищно огромного текста
	# при случайной опечатке (например "10" вместо "1.0")
	var vLF_Value_F: float = clamp(vLS_Text_S.to_float(), 0.5, 3.0)
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnFontScaleSubmitted_Ch", [vLF_Value_F], null)
	vXD_Settings_D["FontScale"] = vLF_Value_F
	vSD_AllNodes_D["ScreenFontScaleLineEdit"]["Node"].text = "%.2f" % vLF_Value_F
	_fC_SaveSettings_Ch(vXD_Settings_D)
	_fC_RescaleFonts_Ch()


#Функционал:
#	Нажатие готового пресета размера — подставляет значения в поля ввода (чтобы
#	они отражали ОЖИДАЮЩИЙ применения размер, ещё не факт применённый — см.
#	changelog 2026-08-19 про отложенное применение) и копит его на применение
#Форматы данных:
#	Входные:
#		vLI_Width_I, vLI_Height_I: int — привязаны через .bind() на каждой кнопке-пресете
func _fC_OnScreenPresetPressed_Ch(vLI_Width_I: int, vLI_Height_I: int) -> void:
	if vSD_AllNodes_D.has("ScreenWidthLineEdit"):
		vSD_AllNodes_D["ScreenWidthLineEdit"]["Node"].text = str(vLI_Width_I)
	if vSD_AllNodes_D.has("ScreenHeightLineEdit"):
		vSD_AllNodes_D["ScreenHeightLineEdit"]["Node"].text = str(vLI_Height_I)
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenPresetPressed_Ch: preset chosen", [vLI_Width_I, vLI_Height_I], null)
	_fC_QueueScreenSizeChange_Ch(vLI_Width_I, vLI_Height_I)


#Функционал:
#	Раскрывает/сворачивает ряд конкретных разрешений внутри одной группы
#	соотношения сторон (2026-08-20, обратная связь: "варианты разрешения для
#	соотношений надо сделать появляющимся после нажатия на разрешения, а сами
#	разрешения сделать кнопками") — заголовок группы (например "4:3") раньше
#	был простым Label, теперь toggle-кнопка (ScreenPresetGroup{ratio}Title);
#	по умолчанию (button_pressed=false в конфиге) ряд кнопок-разрешений
#	(ScreenPresetGroup{ratio}HBox) свёрнут (visible=false) — нажатие
#	показывает его, повторное нажатие снова скрывает. Каждая группа —
#	независимый аккордеон, НЕ ButtonGroup — открытыми может быть сколько
#	угодно групп одновременно
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние кнопки-заголовка (сигнал toggled)
#		vLS_HBoxKey_S: String — имя ноды ряда кнопок-разрешений этой группы,
#			привязано через .bind() на каждой кнопке-заголовке
func _fC_OnPresetGroupToggled_Ch(vLB_Pressed: bool, vLS_HBoxKey_S: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnPresetGroupToggled_Ch", [vLS_HBoxKey_S, vLB_Pressed], null)
	if vSD_AllNodes_D.has(vLS_HBoxKey_S):
		vSD_AllNodes_D[vLS_HBoxKey_S]["Node"].visible = vLB_Pressed


#Функционал:
#	Копит "хочу применить размер экрана WxH" (2026-08-19, "окно применить/
#	отмена" — размер экрана больше НЕ применяется сразу при вводе/выборе
#	пресета) — выбор КОНКРЕТНОГО размера всегда подразумевает выход из
#	полноэкранного режима (сбрасывает vLB_PendingFullscreen_Bv), совсем как
#	раньше делала сама _fC_ApplyScreenSize_Ch. Взводит vLB_ScreenSizeDirty_Bv
#	через общую _fC_MarkScreenSizeDirty_Ch
#Форматы данных:
#	Входные:
#		vLI_Width_I, vLI_Height_I: int — ожидающий применения размер
func _fC_QueueScreenSizeChange_Ch(vLI_Width_I: int, vLI_Height_I: int) -> void:
	vLI_PendingScreenWidth_I = vLI_Width_I
	vLI_PendingScreenHeight_I = vLI_Height_I
	vLB_PendingFullscreen_Bv = false
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_QueueScreenSizeChange_Ch", [vLI_Width_I, vLI_Height_I], null)
	_fC_MarkScreenSizeDirty_Ch()


#Функционал:
#	Копит "хочу [не] полноэкранный режим" — 2026-08-19, обратная связь
#	"полноэкранный режим не работает через подтверждения": раньше
#	_fC_OnFullscreenToggled_Ch применяла get_window().mode СРАЗУ, в обход
#	всего механизма "применить/отмена". Теперь идёт тем же путём, что и
#	размер (см. _fC_QueueScreenSizeChange_Ch) — ширину/высоту НЕ трогает,
#	они останутся ждать своего часа (пригодятся, когда пользователь позже
#	выключит fullscreen)
#Форматы данных:
#	Входные:
#		vLB_Fullscreen_Bv: bool — новое желаемое состояние fullscreen
func _fC_QueueFullscreenChange_Ch(vLB_Fullscreen_Bv: bool) -> void:
	vLB_PendingFullscreen_Bv = vLB_Fullscreen_Bv
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_QueueFullscreenChange_Ch", [vLB_Fullscreen_Bv], null)
	_fC_MarkScreenSizeDirty_Ch()


#Функционал:
#	Общий хвост для обеих "копилок" выше (_fC_QueueScreenSizeChange_Ch/
#	_fC_QueueFullscreenChange_Ch) — взводит "грязный" флаг (проверяется в
#	_fC_CloseModal_Ch при закрытии настроек) и, если не отключено в категории
#	"Подтверждения", показывает info-окно "применится после закрытия"
func _fC_MarkScreenSizeDirty_Ch() -> void:
	vLB_ScreenSizeDirty_Bv = true
	if vXD_Settings_D.get("ShowScreenChangeInfoPopup", true):
		_fC_ShowScreenInfoPopup_Ch()


#Функционал:
#	Реально применяет размер окна ОС ИЛИ полноэкранный режим (взаимоисключающе)
#	и сохраняет выбор в настройки (п.6 ТЗ: "Надо и изменить окно и сохранить в
#	настройки")
#Форматы данных:
#	Входные:
#		vLI_Width_I, vLI_Height_I: int — размер окна (используется, только
#			если vLB_Fullscreen_Bv=false)
#		vLB_Fullscreen_Bv: bool — true — полноэкранный режим, ширина/высота
#			игнорируются (но сохраняются в настройки как есть, пригодятся,
#			когда пользователь позже выключит fullscreen)
#Принцип работы:
#	⚠️ 2026-08-19 (п.2 ТЗ — жалоба "экран не меняется в размерах"): между
#	переключением mode на MODE_WINDOWED и следующим же изменением .size — один
#	кадр ожидания (await process_frame). Это известная особенность Godot:
#	смена режима окна (особенно ИЗ fullscreen/maximized) обрабатывается ОС
#	асинхронно, и .size, выставленный в ТОМ ЖЕ кадре, что и .mode, иногда
#	тихо игнорируется, пока ОС ещё не закончила применять предыдущую смену
#	режима. Если после этой правки размер всё ещё не меняется — вероятная
#	причина уже не в коде: в редакторе Godot 4.3+ по умолчанию включён "Embed
#	Game" (запускаемая игра встраивается в док редактора вместо настоящего
#	окна ОС), и get_window() в этом режиме не управляет реальным окном —
#	стоит проверить Editor Settings → Run → Window Placement, либо
#	протестировать на экспортированной сборке, а не через "Play" в редакторе
func _fC_ApplyScreenSize_Ch(vLI_Width_I: int, vLI_Height_I: int, vLB_Fullscreen_Bv: bool = false) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_ApplyScreenSize_Ch: applying", [vLI_Width_I, vLI_Height_I, vLB_Fullscreen_Bv], null)

	if vLB_Fullscreen_Bv:
		get_window().mode = Window.MODE_FULLSCREEN
	else:
		get_window().mode = Window.MODE_WINDOWED
		await get_tree().process_frame
		get_window().size = Vector2i(vLI_Width_I, vLI_Height_I)

	if vSD_AllNodes_D.has("ScreenFullscreenButton"):
		vSD_AllNodes_D["ScreenFullscreenButton"]["Node"].button_pressed = vLB_Fullscreen_Bv

	vXD_Settings_D["ScreenWidth"] = vLI_Width_I
	vXD_Settings_D["ScreenHeight"] = vLI_Height_I
	vXD_Settings_D["Fullscreen"] = vLB_Fullscreen_Bv
	_fC_SaveSettings_Ch(vXD_Settings_D)


# ============================================================================
# 2026-08-19 — окна "применится после закрытия" (info) и "Применить/Отмена"
# (confirm) для изменения размера экрана. См. changelog в начале файла для
# общей картины; здесь — сами обработчики. Оба окна используют один и тот же
# оверлей-паттерн (_fC_BuildConfirmOverlay_Cr) и один и тот же материал
# размытия, что и большое модальное окно
# ============================================================================

#Функционал:
#	Показывает info-окно "размер экрана применится после закрытия настроек" —
#	вызывается из _fC_QueueScreenSizeChange_Ch при каждом изменении поля/пресета
#	(если не отключено галочкой "больше не показывать")
func _fC_ShowScreenInfoPopup_Ch() -> void:
	if vSD_AllNodes_D.has("ScreenInfoPopupOverlay"):
		vSD_AllNodes_D["ScreenInfoPopupOverlay"]["Node"].visible = true


#Функционал:
#	Кнопка "Понял" на info-окне — просто закрывает его, ничего не применяет
#	(применение размера происходит отдельно, при закрытии настроек)
func _fC_OnScreenInfoOkPressed_Ch() -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenInfoOkPressed_Ch", [], null)
	if vSD_AllNodes_D.has("ScreenInfoPopupOverlay"):
		vSD_AllNodes_D["ScreenInfoPopupOverlay"]["Node"].visible = false


#Функционал:
#	Галочка "больше не показывать" на info-окне — сохраняет выбор в настройки
#	и синхронизирует цветной переключатель в категории "Подтверждения" (если
#	она уже собрана и открывалась — переключатель должен быть в курсе)
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние галочки (сигнал toggled)
func _fC_OnScreenInfoDontShowToggled_Ch(vLB_Pressed: bool) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenInfoDontShowToggled_Ch", [vLB_Pressed], null)
	vXD_Settings_D["ShowScreenChangeInfoPopup"] = not vLB_Pressed
	_fC_SaveSettings_Ch(vXD_Settings_D)
	_fC_UpdateConfirmToggleVisual_Ch("ConfirmScreenInfoToggle", not vLB_Pressed)


#Функционал:
#	Показывает окно "Применить/Отмена" — вызывается из _fC_CloseModal_Ch, когда
#	пользователь пытается закрыть настройки при незаявленном изменении размера
#	экрана. Сбрасывает антидребезг И счётчик "побегов" кнопки "Применить" на
#	каждый новый показ (см. AutoClickDodgeLimit — лимит на показ, не на всю сессию)
#	⚠️ 2026-08-22: вместо сброса "времени прошлого клика" в прошлое (старый
#	баг, см. changelog у _vLF_ApplyButtonUnlockMsec_F) — здесь СТАВИТСЯ
#	дедлайн защиты (сейчас + AutoClickProtectionSeconds) и запускается видимый
#	обратный отсчёт (ScreenApplyConfirmCountdownTimer)
func _fC_ShowScreenApplyConfirm_Ch() -> void:
	if vSD_AllNodes_D.has("ScreenApplyConfirmOverlay"):
		vSD_AllNodes_D["ScreenApplyConfirmOverlay"]["Node"].visible = true
	var vLF_ProtectionSeconds_F: float = vXD_Settings_D.get("AutoClickProtectionSeconds", 0.5)
	_vLF_ApplyButtonUnlockMsec_F = Time.get_ticks_msec() / 1000.0 + vLF_ProtectionSeconds_F
	_vLI_ApplyButtonDodgeCount_I = 0
	_fC_UpdateApplyCountdownDisplay_Ch() # сразу, не дожидаясь первого тика таймера
	if vSD_AllNodes_D.has("ScreenApplyConfirmCountdownTimer") and vLF_ProtectionSeconds_F > 0.0:
		vSD_AllNodes_D["ScreenApplyConfirmCountdownTimer"]["Node"].start()


#Функционал:
#	Прячет окно "Применить/Отмена" — общая точка для обеих кнопок (Применить/
#	Отмена), чтобы не дублировать скрытие дважды. Заодно останавливает таймер
#	обратного отсчёта (2026-08-22) — иначе он тикал бы и после закрытия окна
func _fC_HideScreenApplyConfirm_Ch() -> void:
	if vSD_AllNodes_D.has("ScreenApplyConfirmOverlay"):
		vSD_AllNodes_D["ScreenApplyConfirmOverlay"]["Node"].visible = false
	if vSD_AllNodes_D.has("ScreenApplyConfirmCountdownTimer"):
		vSD_AllNodes_D["ScreenApplyConfirmCountdownTimer"]["Node"].stop()


#Функционал:
#	Тик таймера обратного отсчёта (каждые 0.1с, пока окно открыто и защита ещё
#	действует) — просто перерисовывает счётчик (2026-08-22, см. changelog у
#	_vLF_ApplyButtonUnlockMsec_F)
func _fC_OnApplyCountdownTick_Ch() -> void:
	_fC_UpdateApplyCountdownDisplay_Ch()


#Функционал:
#	Перерисовывает видимый счётчик у кнопки "Применить" — пока текущее время
#	меньше дедлайна защиты (_vLF_ApplyButtonUnlockMsec_F), показывает остаток
#	в секундах; как только дедлайн прошёл — прячет счётчик и останавливает
#	таймер (дальше тикать незачем, защита уже снята) — обратная связь
#	2026-08-22: "счётчик... который будет исчезать по истечении и позволять
#	нажать на кнопку"
func _fC_UpdateApplyCountdownDisplay_Ch() -> void:
	if not vSD_AllNodes_D.has("ScreenApplyConfirmCountdownHBox"):
		return
	var vL_HBox: Control = vSD_AllNodes_D["ScreenApplyConfirmCountdownHBox"]["Node"]
	var vLF_Remaining_F: float = _vLF_ApplyButtonUnlockMsec_F - Time.get_ticks_msec() / 1000.0
	if vLF_Remaining_F <= 0.0:
		vL_HBox.visible = false
		if vSD_AllNodes_D.has("ScreenApplyConfirmCountdownTimer"):
			vSD_AllNodes_D["ScreenApplyConfirmCountdownTimer"]["Node"].stop()
		return
	vL_HBox.visible = true
	if vSD_AllNodes_D.has("ScreenApplyConfirmCountdownValueLabel"):
		vSD_AllNodes_D["ScreenApplyConfirmCountdownValueLabel"]["Node"].text = "%.1f" % vLF_Remaining_F


#Функционал:
#	Нажатие кнопки "Применить" на окне подтверждения — с антидребезгом:
#	пока не прошёл дедлайн защиты (_vLF_ApplyButtonUnlockMsec_F, выставляется
#	при ПОКАЗЕ окна — см. _fC_ShowScreenApplyConfirm_Ch — а не при прошлом
#	клике, см. changelog у самого поля про найденный баг), клик НЕ
#	засчитывается за применение, кнопка "убегает" на случайное место внутри
#	своей зоны — но не больше AutoClickDodgeLimit раз за один показ этого
#	окна (по умолчанию 1), дальше клики проходят как обычные, чтобы
#	пользователь гарантированно мог когда-нибудь применить настройки
func _fC_OnScreenApplyButtonPressed_Ch() -> void:
	var vLI_DodgeLimit_I: int = int(vXD_Settings_D.get("AutoClickDodgeLimit", 1))
	var vLF_Now_F: float = Time.get_ticks_msec() / 1000.0
	if vLF_Now_F < _vLF_ApplyButtonUnlockMsec_F and _vLI_ApplyButtonDodgeCount_I < vLI_DodgeLimit_I:
		_vLI_ApplyButtonDodgeCount_I += 1
		_fC_DodgeApplyButton_Ch()
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenApplyButtonPressed_Ch: too fast, dodged", [_vLI_ApplyButtonDodgeCount_I, vLI_DodgeLimit_I], null)
		return
	_fC_CommitScreenSizeChange_Ch()


#Функционал:
#	Переставляет кнопку "Применить" на случайную позицию внутри её "арены"
#	(ScreenApplyConfirmArena — обычный Control, не контейнер, поэтому дети
#	не перестраиваются автоматически и position можно двигать вручную)
func _fC_DodgeApplyButton_Ch() -> void:
	if not vSD_AllNodes_D.has("ScreenApplyConfirmArena") or not vSD_AllNodes_D.has("ScreenApplyConfirmApplyButton"):
		return
	var vL_Arena: Control = vSD_AllNodes_D["ScreenApplyConfirmArena"]["Node"]
	var vL_Button: Control = vSD_AllNodes_D["ScreenApplyConfirmApplyButton"]["Node"]
	var vLV2_MaxPos_V2: Vector2 = vL_Arena.size - vL_Button.size
	vLV2_MaxPos_V2.x = max(vLV2_MaxPos_V2.x, 0.0)
	vLV2_MaxPos_V2.y = max(vLV2_MaxPos_V2.y, 0.0)
	vL_Button.position = Vector2(randf() * vLV2_MaxPos_V2.x, randf() * vLV2_MaxPos_V2.y)


#Функционал:
#	Реально применяет отложенный размер экрана (vLI_PendingScreenWidth_I/
#	Height_I) через уже существующую _fC_ApplyScreenSize_Ch, снимает "грязный"
#	флаг, прячет окно подтверждения и НАКОНЕЦ закрывает настройки — повторный
#	вызов _fC_CloseModal_Ch теперь пройдёт целиком, т.к. vLB_ScreenSizeDirty_Bv
#	уже false (см. changelog про _fC_CloseModal_Ch)
func _fC_CommitScreenSizeChange_Ch() -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_CommitScreenSizeChange_Ch", [vLI_PendingScreenWidth_I, vLI_PendingScreenHeight_I, vLB_PendingFullscreen_Bv], null)
	vLB_ScreenSizeDirty_Bv = false
	_fC_ApplyScreenSize_Ch(vLI_PendingScreenWidth_I, vLI_PendingScreenHeight_I, vLB_PendingFullscreen_Bv)
	_fC_HideScreenApplyConfirm_Ch()
	_fC_CloseModal_Ch()


#Функционал:
#	Нажатие кнопки "Отмена" на окне подтверждения — отбрасывает отложенное
#	изменение (размер И fullscreen), откатывает поля ввода/кнопку fullscreen к
#	реально применённым (сохранённым) значениям и закрывает настройки без применения
func _fC_OnScreenApplyCancelPressed_Ch() -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenApplyCancelPressed_Ch", [], null)
	vLB_ScreenSizeDirty_Bv = false
	vLB_PendingFullscreen_Bv = vXD_Settings_D.get("Fullscreen", false)
	if vSD_AllNodes_D.has("ScreenWidthLineEdit"):
		vSD_AllNodes_D["ScreenWidthLineEdit"]["Node"].text = str(int(vXD_Settings_D.get("ScreenWidth", vSV2_Scene_size_V2i.x)))
	if vSD_AllNodes_D.has("ScreenHeightLineEdit"):
		vSD_AllNodes_D["ScreenHeightLineEdit"]["Node"].text = str(int(vXD_Settings_D.get("ScreenHeight", vSV2_Scene_size_V2i.y)))
	if vSD_AllNodes_D.has("ScreenFullscreenButton"):
		vSD_AllNodes_D["ScreenFullscreenButton"]["Node"].button_pressed = vLB_PendingFullscreen_Bv
	_fC_HideScreenApplyConfirm_Ch()
	_fC_CloseModal_Ch()


#Функционал:
#	Галочка "больше не показывать" на окне "Применить/Отмена" — сохраняет
#	выбор и синхронизирует переключатель в категории "Подтверждения"
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние галочки (сигнал toggled)
func _fC_OnScreenApplyDontShowToggled_Ch(vLB_Pressed: bool) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnScreenApplyDontShowToggled_Ch", [vLB_Pressed], null)
	vXD_Settings_D["ShowScreenApplyConfirmPopup"] = not vLB_Pressed
	_fC_SaveSettings_Ch(vXD_Settings_D)
	_fC_UpdateConfirmToggleVisual_Ch("ConfirmScreenApplyToggle", not vLB_Pressed)


#Функционал:
#	Переключатель в категории "Подтверждения" — включает/выключает показ
#	конкретного окна-уведомления (сейчас их два: info и apply-confirm, см.
#	CategoryConfirmationsButton/ConfirmationsContentVBox в _fC_BuildScreenConfig_Cr)
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние кнопки-переключателя (сигнал toggled)
#		vLS_SettingsKey_S: String — ключ в vXD_Settings_D ("ShowScreenChangeInfoPopup"/
#			"ShowScreenApplyConfirmPopup"), привязан через .bind()
#		vLS_NodeKey_S: String — ключ ноды-кнопки в vSD_AllNodes_D для перекраски,
#			привязан через .bind()
func _fC_OnConfirmationToggle_Ch(vLB_Pressed: bool, vLS_SettingsKey_S: String, vLS_NodeKey_S: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnConfirmationToggle_Ch", [vLS_SettingsKey_S, vLB_Pressed], null)
	vXD_Settings_D[vLS_SettingsKey_S] = vLB_Pressed
	_fC_SaveSettings_Ch(vXD_Settings_D)
	_fC_UpdateConfirmToggleVisual_Ch(vLS_NodeKey_S, vLB_Pressed)


#Функционал:
#	Перекрашивает/переподписывает кнопку-переключатель в категории
#	"Подтверждения" под текущее состояние — переиспользует уже готовую
#	цветовую палитру статусов чек-листа (vGD_StatusColors_D["passed"/"failed"]),
#	чтобы не заводить отдельные "магические" цвета just for this
#Форматы данных:
#	Входные:
#		vLS_NodeKey_S: String — ключ ноды-кнопки в vSD_AllNodes_D
#		vLB_Enabled_Bv: bool — true = окно-уведомление показывается (зелёная),
#			false = скрыто (красная)
#Принцип работы:
#	⚠️ 2026-08-23, найден и починен баг ("окно про логи/реплеи вылезает само,
#	хотя в настройки не заходил"): раньше здесь стояло `vL_Button.button_pressed
#	= vLB_Enabled_Bv` — обычное присваивание свойства toggle-кнопки в Godot
#	САМО испускает сигнал "toggled", если значение реально меняется (см.
#	BaseButton.set_pressed в движке). Эта функция вызывается и как "чистая
#	визуальная синхронизация" из _fC_ApplySettingsToUI_Ch при первой сборке
#	экрана (переносит РАНЬШЕ СОХРАНЁННОЕ значение из vXD_Settings_D на кнопку,
#	созданную с др. дефолтом в статическом конфиге) — если сохранённое
#	значение отличается от дефолта в конфиге, присваивание считается "реальным
#	изменением", Godot испускает "toggled", а он подключён к
#	_fC_OnConfirmationToggle_Ch/_fC_OnLoggerLimitToggled_Ch (см. ниже), которые
#	ПОВТОРНО сохраняют настройки и (для логов/реплеев) показывают info-окно —
#	то есть окно вылезало на КАЖДОМ старте программы, если хоть раз до этого
#	один из переключателей был сохранён не-дефолтным значением, даже если
#	пользователь в текущем запуске в настройки не заходил вообще.
#	set_pressed_no_signal — тот же метод BaseButton, что и предназначен именно
#	для программной синхронизации состояния БЕЗ повторного срабатывания
#	сигнала; для реального клика пользователя ничего не меняется (сигнал
#	"toggled" в этом случае испускает сам движок ДО вызова
#	_fC_OnConfirmationToggle_Ch/_fC_OnLoggerLimitToggled_Ch, эта функция там
#	только докрашивает визуал уже правильного состояния)
func _fC_UpdateConfirmToggleVisual_Ch(vLS_NodeKey_S: String, vLB_Enabled_Bv: bool) -> void:
	if not vSD_AllNodes_D.has(vLS_NodeKey_S):
		return
	var vL_Button: Button = vSD_AllNodes_D[vLS_NodeKey_S]["Node"]
	vL_Button.set_pressed_no_signal(vLB_Enabled_Bv)
	vL_Button.text = "CONFIRM_TOGGLE_ON" if vLB_Enabled_Bv else "CONFIRM_TOGGLE_OFF"
	vL_Button.modulate = vGD_StatusColors_D["passed"] if vLB_Enabled_Bv else vGD_StatusColors_D["failed"]


#Функционал:
#	Нажатие одной из кнопок ВТОРОГО столбика внутри "Подтверждения"
#	(Настройки/Обучение) — тот же toggle-в-ButtonGroup паттерн, что и у
#	_fC_OnSettingsCategoryToggled_Ch (сигнал toggled приходит дважды при
#	переключении, нас интересует только true)
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние кнопки (сигнал toggled)
#		vLS_Source: String — "Settings"/"Training", привязано через .bind()
func _fC_OnConfirmSourceToggled_Ch(vLB_Pressed: bool, vLS_Source: String) -> void:
	if not vLB_Pressed:
		return
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnConfirmSourceToggled_Ch", [vLS_Source], null)
	vLS_ActiveConfirmSource_S = vLS_Source
	_fC_UpdateConfirmSourceVisibility_Ch()


#Функционал:
#	Показывает содержимое активного столбика (Настройки/Обучение) внутри
#	категории "Подтверждения", прячет остальное — та же идея, что и
#	_fC_UpdateModalContentVisibility_Ch, только на уровень глубже
func _fC_UpdateConfirmSourceVisibility_Ch() -> void:
	if vSD_AllNodes_D.has("ConfirmSourceSettingsVBox"):
		vSD_AllNodes_D["ConfirmSourceSettingsVBox"]["Node"].visible = (vLS_ActiveConfirmSource_S == "Settings")
	if vSD_AllNodes_D.has("ConfirmSourceTrainingVBox"):
		vSD_AllNodes_D["ConfirmSourceTrainingVBox"]["Node"].visible = (vLS_ActiveConfirmSource_S == "Training")


#Функционал:
#	Enter ИЛИ потеря фокуса в поле "защита от автоматического пропуска"
#	(раздел "Автоматическое нажатие человеком") — сохраняет значение в
#	настройки. Читается в _fC_OnScreenApplyButtonPressed_Ch
#	⚠️ 2026-08-20: было HSlider (value_changed) — по обратной связи заменено на
#	свободный ввод текста, тот же паттерн, что и у _fC_OnScreenSizeChanged_Ch
#	(и text_submitted, и focus_exited ведут сюда; переданный текст не
#	используется напрямую — поле перечитывается само, см. там же почему)
#Форматы данных:
#	Входные:
#		vLS_SubmittedText_S: String — текст/пустая строка от сработавшего
#			сигнала, не используется напрямую
func _fC_OnAutoClickProtectionSubmitted_Ch(vLS_SubmittedText_S: String) -> void:
	if not vSD_AllNodes_D.has("ConfirmAutoClickProtectionLineEdit"):
		return
	var vLS_Text_S: String = vSD_AllNodes_D["ConfirmAutoClickProtectionLineEdit"]["Node"].text.strip_edges()
	if not vLS_Text_S.is_valid_float():
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnAutoClickProtectionSubmitted_Ch: invalid input, ignored", [vLS_Text_S], null)
		return
	var vLF_Value_F: float = max(0.0, vLS_Text_S.to_float())
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnAutoClickProtectionSubmitted_Ch", [vLF_Value_F], null)
	vXD_Settings_D["AutoClickProtectionSeconds"] = vLF_Value_F
	vSD_AllNodes_D["ConfirmAutoClickProtectionLineEdit"]["Node"].text = "%.1f" % vLF_Value_F
	_fC_SaveSettings_Ch(vXD_Settings_D)


#Функционал:
#	Enter ИЛИ потеря фокуса в поле "количество телепортов" (раздел
#	"Автоматическое нажатие человеком") — сохраняет значение в настройки.
#	Читается в _fC_OnScreenApplyButtonPressed_Ch (лимит "побегов" кнопки
#	"Применить" за один показ окна подтверждения)
#	⚠️ 2026-08-20: было HSlider (value_changed) — по обратной связи заменено на
#	свободный ввод текста, см. changelog у _fC_OnAutoClickProtectionSubmitted_Ch
#Форматы данных:
#	Входные:
#		vLS_SubmittedText_S: String — текст/пустая строка от сработавшего
#			сигнала, не используется напрямую
func _fC_OnAutoClickDodgeLimitSubmitted_Ch(vLS_SubmittedText_S: String) -> void:
	if not vSD_AllNodes_D.has("ConfirmAutoClickDodgeLimitLineEdit"):
		return
	var vLS_Text_S: String = vSD_AllNodes_D["ConfirmAutoClickDodgeLimitLineEdit"]["Node"].text.strip_edges()
	if not vLS_Text_S.is_valid_int():
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnAutoClickDodgeLimitSubmitted_Ch: invalid input, ignored", [vLS_Text_S], null)
		return
	var vLI_Value_I: int = max(0, vLS_Text_S.to_int())
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnAutoClickDodgeLimitSubmitted_Ch", [vLI_Value_I], null)
	vXD_Settings_D["AutoClickDodgeLimit"] = vLI_Value_I
	vSD_AllNodes_D["ConfirmAutoClickDodgeLimitLineEdit"]["Node"].text = "%d" % vLI_Value_I
	_fC_SaveSettings_Ch(vXD_Settings_D)


#Функционал:
#	Кнопка "Полный экран" (toggle, теперь первая среди пресетов) — БОЛЬШЕ НЕ
#	переключает режим окна ОС сразу (обратная связь 2026-08-19: "полноэкранный
#	режим не работает через подтверждения" — раньше эта кнопка была
#	единственной в категории "Экран", которая обходила отложенное применение).
#	Теперь идёт тем же путём, что и размер — см. _fC_QueueFullscreenChange_Ch
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние кнопки (сигнал toggled)
func _fC_OnFullscreenToggled_Ch(vLB_Pressed: bool) -> void:
	_fC_QueueFullscreenChange_Ch(vLB_Pressed)


#Функционал:
#	Ползунок звука (Музыка/Интерфейс) сдвинут — обновляет подпись-процент
#	рядом и сохраняет значение в настройки. Пока НЕ подключено ни к какой
#	реальной аудио-шине (п.7 ТЗ: "Пока просто сохраняя его в настройках") —
#	подключение звука отдельная задача, вне этого экрана
#Форматы данных:
#	Входные:
#		vLF_Value_F: float — новое значение ползунка (сигнал value_changed;
#			шаг ползунка задан в конфиге как 1, поэтому всегда целое число)
#		vLS_SettingsKey_S: String — ключ в vXD_Settings_D ("MusicVolume"/
#			"InterfaceVolume"), привязан через .bind()
#		vLS_ValueLabelKey_S: String — ключ в vSD_AllNodes_D подписи-процента
#			рядом с ползунком, привязан через .bind()
func _fC_OnSoundSliderChanged_Ch(vLF_Value_F: float, vLS_SettingsKey_S: String, vLS_ValueLabelKey_S: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnSoundSliderChanged_Ch", [vLS_SettingsKey_S, int(vLF_Value_F)], null)
	vXD_Settings_D[vLS_SettingsKey_S] = int(vLF_Value_F)
	if vSD_AllNodes_D.has(vLS_ValueLabelKey_S):
		vSD_AllNodes_D[vLS_ValueLabelKey_S]["Node"].text = "%d%%" % int(vLF_Value_F)


#Функционал:
#	Галочка-квадрат слева от одного из 6 лимитов категории "Логи и реплеи" —
#	включает/выключает сам лимит (сохраняет и перекрашивает). Такая же
#	generic-схема через .bind(), что и у _fC_OnConfirmationToggle_Ch/
#	_fC_OnSoundSliderChanged_Ch — один обработчик на все 6 галочек, а не 6
#	отдельных функций
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние галочки (сигнал toggled)
#		vLS_SettingsKey_S: String — ключ *Enabled в vXD_Settings_D, привязан
#			через .bind() (см. vGD_LoggerRows_A)
#		vLS_NodeKey_S: String — ключ ноды-галочки в vSD_AllNodes_D для
#			перекраски, привязан через .bind()
func _fC_OnLoggerLimitToggled_Ch(vLB_Pressed: bool, vLS_SettingsKey_S: String, vLS_NodeKey_S: String) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnLoggerLimitToggled_Ch", [vLS_SettingsKey_S, vLB_Pressed], null)
	vXD_Settings_D[vLS_SettingsKey_S] = vLB_Pressed
	_fC_SaveSettings_Ch(vXD_Settings_D)
	_fC_UpdateLoggerCheckVisual_Ch(vLS_NodeKey_S, vLB_Pressed)
	# 2026-08-22: "добавь при изменении параметров информационное окно, что
	# изменения будут применены только после перезапуска" — на любое из 6
	# полей категории "Логи и реплеи" (галочка ИЛИ значение, см. также
	# _fC_OnLoggerLimitSubmitted_Ch ниже)
	if vXD_Settings_D.get("ShowLoggerRestartInfoPopup", true):
		_fC_ShowLoggerInfoPopup_Ch()


#Функционал:
#	Перекрашивает галочку-квадрат под текущее состояние — переиспользует ту же
#	палитру/символы статусов чек-листа (vGD_StatusColors_D/vGD_StatusSymbols_D
#	"passed"/"failed"), что и остальные цветные переключатели этого экрана
#	(см. _fC_UpdateConfirmToggleVisual_Ch — тот же принцип, но там текстовая
#	кнопка "Показывать/Скрыто", здесь квадрат без текста, символ ✓/✗ вместо
#	слова — обратная связь: "галочка (квадрат красно/зелёный)")
#Форматы данных:
#	Входные:
#		vLS_NodeKey_S: String — ключ ноды-галочки в vSD_AllNodes_D
#		vLB_Enabled_Bv: bool — true = лимит включён (зелёный ✓), false =
#			выключен (красный ✗)
#Принцип работы:
#	⚠️ 2026-08-23, найден и починен баг ("окно про логи/реплеи вылезает само
#	при каждом запуске, хотя в настройки не заходил") — та же причина и то же
#	исправление, что и в _fC_UpdateConfirmToggleVisual_Ch выше (см. её
#	подробный changelog): обычное `vL_Button.button_pressed = ...` само
#	испускает "toggled", если значение при программной синхронизации из
#	_fC_ApplySettingsToUI_Ch отличается от дефолта, зашитого в статический
#	конфиг кнопки — а "toggled" здесь подключён к _fC_OnLoggerLimitToggled_Ch,
#	который показывает info-окно "изменения применятся после перезапуска".
#	set_pressed_no_signal — программная синхронизация без повторного сигнала
func _fC_UpdateLoggerCheckVisual_Ch(vLS_NodeKey_S: String, vLB_Enabled_Bv: bool) -> void:
	if not vSD_AllNodes_D.has(vLS_NodeKey_S):
		return
	var vL_Button: Button = vSD_AllNodes_D[vLS_NodeKey_S]["Node"]
	vL_Button.set_pressed_no_signal(vLB_Enabled_Bv)
	vL_Button.text = vGD_StatusSymbols_D["passed"] if vLB_Enabled_Bv else vGD_StatusSymbols_D["failed"]
	vL_Button.modulate = vGD_StatusColors_D["passed"] if vLB_Enabled_Bv else vGD_StatusColors_D["failed"]


#Функционал:
#	Enter ИЛИ потеря фокуса в одном из 6 полей ввода категории "Логи и
#	реплеи" — сохраняет значение в настройки. Тот же generic-паттерн через
#	.bind(), что и у _fC_OnConfirmationToggle_Ch (один обработчик на все 6
#	полей), но текст/фокус сигналы дают строку не единообразно (text_submitted
#	даёт реальный текст, focus_exited — ничего), поэтому, как и у
#	_fC_OnScreenSizeChanged_Ch, поле ПЕРЕЧИТЫВАЕТСЯ САМО по ключу ноды, а не
#	берётся из переданного аргумента — vLS_SubmittedText_S тут не используется
#Форматы данных:
#	Входные:
#		vLS_SubmittedText_S: String — текст/пустая строка от сработавшего
#			сигнала, не используется напрямую (см. выше)
#		vLS_SettingsKey_S: String — числовой ключ в vXD_Settings_D, привязан
#			через .bind() (см. vGD_LoggerRows_A)
#		vLS_NodeKey_S: String — ключ ноды-поля в vSD_AllNodes_D, привязан
#			через .bind()
func _fC_OnLoggerLimitSubmitted_Ch(vLS_SubmittedText_S: String, vLS_SettingsKey_S: String, vLS_NodeKey_S: String) -> void:
	if not vSD_AllNodes_D.has(vLS_NodeKey_S):
		return
	var vLS_Text_S: String = vSD_AllNodes_D[vLS_NodeKey_S]["Node"].text.strip_edges()
	if not vLS_Text_S.is_valid_int():
		if vLS_StreamId != "":
			Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnLoggerLimitSubmitted_Ch: invalid input, ignored", [vLS_SettingsKey_S, vLS_Text_S], null)
		# Невалидный ввод — откатываем поле к реально сохранённому значению,
		# а не оставляем мусор в нём висеть (тот же принцип, что и у полей
		# ширины/высоты экрана)
		vSD_AllNodes_D[vLS_NodeKey_S]["Node"].text = "%d" % int(vXD_Settings_D.get(vLS_SettingsKey_S, 0))
		return
	var vLI_Value_I: int = max(1, vLS_Text_S.to_int()) # минимум 1 — лимит "0" бессмысленен, для отключения есть галочка
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnLoggerLimitSubmitted_Ch", [vLS_SettingsKey_S, vLI_Value_I], null)
	vXD_Settings_D[vLS_SettingsKey_S] = vLI_Value_I
	vSD_AllNodes_D[vLS_NodeKey_S]["Node"].text = "%d" % vLI_Value_I
	_fC_SaveSettings_Ch(vXD_Settings_D) # 2026-08-22: убран случайный повторный вызов этой же строки (был дважды подряд, без эффекта)
	# 2026-08-22: то же info-окно "применится после перезапуска", что и в
	# _fC_OnLoggerLimitToggled_Ch выше — только для реально валидного и
	# сохранённого значения, НЕ при откате невалидного ввода (см. return выше)
	if vXD_Settings_D.get("ShowLoggerRestartInfoPopup", true):
		_fC_ShowLoggerInfoPopup_Ch()


#Функционал:
#	Показывает info-окно "изменения (категория 'Логи и реплеи') применятся
#	только после перезапуска программы" — вызывается из
#	_fC_OnLoggerLimitToggled_Ch/_fC_OnLoggerLimitSubmitted_Ch при каждом
#	реальном изменении одной из 6 настроек (если не отключено галочкой
#	"больше не показывать"). Тот же паттерн, что и у _fC_ShowScreenInfoPopup_Ch
func _fC_ShowLoggerInfoPopup_Ch() -> void:
	if vSD_AllNodes_D.has("LoggerInfoPopupOverlay"):
		vSD_AllNodes_D["LoggerInfoPopupOverlay"]["Node"].visible = true


#Функционал:
#	Кнопка "Понял" на info-окне категории "Логи и реплеи" — просто закрывает
#	его, ничего больше не делает (значения уже сохранены в Settings.json к
#	моменту показа окна — само окно только предупреждает о СРОКЕ применения)
func _fC_OnLoggerInfoOkPressed_Ch() -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnLoggerInfoOkPressed_Ch", [], null)
	if vSD_AllNodes_D.has("LoggerInfoPopupOverlay"):
		vSD_AllNodes_D["LoggerInfoPopupOverlay"]["Node"].visible = false


#Функционал:
#	Галочка "больше не показывать" на info-окне категории "Логи и реплеи" —
#	сохраняет выбор в настройки и синхронизирует переключатель в категории
#	"Подтверждения" (тот же принцип, что и у _fC_OnScreenInfoDontShowToggled_Ch)
#Форматы данных:
#	Входные:
#		vLB_Pressed: bool — новое состояние галочки (сигнал toggled)
func _fC_OnLoggerInfoDontShowToggled_Ch(vLB_Pressed: bool) -> void:
	if vLS_StreamId != "":
		Class_Logger.fC_Adding_Buffer_Cr(vLS_StreamId, "_fC_OnLoggerInfoDontShowToggled_Ch", [vLB_Pressed], null)
	vXD_Settings_D["ShowLoggerRestartInfoPopup"] = not vLB_Pressed
	_fC_SaveSettings_Ch(vXD_Settings_D)
	_fC_UpdateConfirmToggleVisual_Ch("ConfirmLoggerInfoToggle", not vLB_Pressed)
